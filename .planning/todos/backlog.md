- [ ] Fix onboarding wizard UI issues: (1) Step 3 - 'Skip for now' button text wraps to 2 lines, needs whitespace-nowrap (2) Step 4 - toggle switches render incorrectly, thumb circle overflows outside track
- [ ] Auto currency conversion on deal form: When user selects a different currency, fetch live exchange rate from an API (e.g. Open Exchange Rates, ExchangeRate-API) and auto-update the price + conversion rate fields. Currently the conversion rate field is manual — need a server-side API route to fetch rates and auto-fill both the conversion rate and the converted price.
- [ ] Align deal form units with product units and pre-fill from product: Product creation uses `units.js` (UNIT_CATEGORIES + getUnitsByCategory with codes like PCE, KGM, TNE) while deal form uses `DEAL_UNITS` from dealConstants.js (kg, ton, pieces). These are mismatched — deal form should use the same unit system as products so the product's unit pre-fills correctly. Also pre-fill product price, currency, and stock quantity into the deal form defaults.
