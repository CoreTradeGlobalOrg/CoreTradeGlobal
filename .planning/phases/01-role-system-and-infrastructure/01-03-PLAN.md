---
phase: 01-role-system-and-infrastructure
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - middleware.js
  - src/app/(main)/forbidden/page.jsx
  - src/presentation/components/homepage/Navbar/Navbar.jsx
  - src/presentation/components/common/RoleBadge/RoleBadge.jsx
  - src/app/(main)/provider/page.jsx
  - src/app/(main)/lawyer/page.jsx
  - src/app/(main)/profile/[userId]/page.jsx
autonomous: true
requirements:
  - ROLE-02
  - ROLE-04

must_haves:
  truths:
    - "Member user sees only member-relevant menu items — provider and lawyer nav items are hidden"
    - "Provider user sees Provider Dashboard in nav — member-only items are hidden"
    - "Lawyer user sees Client Channels and Deal Review in nav — member-only items are hidden"
    - "Navigating to an unauthorized route (e.g., member tries /provider) shows a clear forbidden page with link back to dashboard"
    - "Other users' roles are shown with color-coded badges in interactions"
    - "User's own role is shown on their profile page"
    - "Buyer/seller distinction is per-deal, not at registration — no role change needed for members"
  artifacts:
    - path: "middleware.js"
      provides: "Route protection for provider, lawyer, and admin routes with role-based redirects"
      contains: "providerRoutes"
    - path: "src/app/(main)/forbidden/page.jsx"
      provides: "Access denied page with link back to dashboard"
      contains: "forbidden"
    - path: "src/presentation/components/homepage/Navbar/Navbar.jsx"
      provides: "Role-filtered navigation links"
      contains: "roles"
    - path: "src/presentation/components/common/RoleBadge/RoleBadge.jsx"
      provides: "Color-coded role badge component"
      contains: "RoleBadge"
    - path: "src/app/(main)/provider/page.jsx"
      provides: "Provider dashboard placeholder with tabs structure"
      contains: "provider"
    - path: "src/app/(main)/lawyer/page.jsx"
      provides: "Lawyer workspace placeholder page"
      contains: "lawyer"
  key_links:
    - from: "middleware.js"
      to: "src/app/(main)/forbidden/page.jsx"
      via: "Redirect unauthorized users to /forbidden"
      pattern: "redirect.*forbidden"
    - from: "src/presentation/components/homepage/Navbar/Navbar.jsx"
      to: "src/core/constants/roles.js"
      via: "NAV_LINKS filtered by user role using role constants"
      pattern: "roles.*includes.*user\\.role"
    - from: "src/presentation/components/common/RoleBadge/RoleBadge.jsx"
      to: "src/core/constants/roles.js"
      via: "Badge colors read from role config"
      pattern: "ROLE.*color"
---

<objective>
Build role-based navigation, route protection, role badge component, forbidden page, and provider/lawyer placeholder pages. Make the platform visually role-aware.

Purpose: Users must see only their role-relevant navigation (ROLE-04), unauthorized URL access must show a clear forbidden page, and buyer/seller distinction is contextual per deal not at registration (ROLE-02). This plan makes the platform visually enforce roles.

Output: Updated middleware with role routes, forbidden page, role-filtered Navbar, RoleBadge component, provider dashboard placeholder, lawyer workspace placeholder, profile page with role display.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-role-system-and-infrastructure/01-RESEARCH.md
@.planning/phases/01-role-system-and-infrastructure/01-CONTEXT.md
@.planning/phases/01-role-system-and-infrastructure/01-01-SUMMARY.md

# Existing files to read before modifying:
@middleware.js
@src/presentation/components/homepage/Navbar/Navbar.jsx
@src/app/(main)/profile/[userId]/page.jsx
@src/presentation/contexts/AuthContext.jsx
@src/core/constants/roles.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update middleware for role-based route protection and create forbidden page</name>
  <files>
    middleware.js
    src/app/(main)/forbidden/page.jsx
  </files>
  <action>
**1. Update `middleware.js`:**

Read the existing file to understand current route protection patterns. It already has `protectedRoutes` and `adminRoutes` with session cookie parsing.

Add role-based route arrays:
```
const providerRoutes = ['/provider']
const lawyerRoutes = ['/lawyer']
```

In the middleware function, after the existing session parsing:
- If path matches a providerRoute and session role is not `logistics_provider`, `insurance_provider`, or `admin` -> redirect to `/forbidden`
- If path matches a lawyerRoute and session role is not `lawyer` or `admin` -> redirect to `/forbidden`
- Keep existing admin route protection unchanged (already redirects non-admins)
- Keep existing protected routes (dashboard, messages, etc.) unchanged — they require authentication but not a specific role

Important: Admin should have access to all routes (existing pattern). Ensure the admin check comes first or admin is included in all role checks.

Do NOT change the existing session cookie structure or auth flow — just add role-based checks for the new route arrays.

**2. Create `src/app/(main)/forbidden/page.jsx`:**

Per user decision: "Unauthorized URL access shows a clear 'You don't have access' page with link back to dashboard."

- `'use client'` component (needs useAuth for user context)
- Clear heading: "Access Denied" or "You don't have access"
- Message: "You don't have permission to view this page."
- Show the user's current role (from useAuth) so they understand why
- Link: "Return to Dashboard" pointing to `/` (home/dashboard)
- Tailwind styling: centered on page, clean design consistent with existing pages
- Use lucide-react ShieldX or Lock icon for visual clarity
  </action>
  <verify>
1. Read updated middleware.js — verify providerRoutes and lawyerRoutes arrays exist with correct role checks
2. Verify admin role bypasses all restrictions
3. Verify forbidden page renders with access denied message and return link
4. Run `npx next lint middleware.js src/app/\\(main\\)/forbidden/page.jsx`
  </verify>
  <done>
Middleware redirects unauthorized users to /forbidden for provider and lawyer routes. Forbidden page shows clear access denied message with user's role and link back to dashboard. Admin has access to all routes. Existing route protection unchanged.
  </done>
</task>

<task type="auto">
  <name>Task 2: Role-filtered Navbar, RoleBadge component, and provider/lawyer placeholder pages</name>
  <files>
    src/presentation/components/homepage/Navbar/Navbar.jsx
    src/presentation/components/common/RoleBadge/RoleBadge.jsx
    src/app/(main)/provider/page.jsx
    src/app/(main)/lawyer/page.jsx
    src/app/(main)/profile/[userId]/page.jsx
  </files>
  <action>
**1. Create `RoleBadge.jsx`:**

Reusable component that displays a color-coded role badge.

Props: `role` (string), `size` (optional: 'sm' | 'md' | 'lg', default 'sm')

- Import role config (badge colors, display names) from `@/core/constants/roles.js`
- Render a small pill/badge with:
  - Background color per role decision: blue for member, green for logistics_provider, orange for insurance_provider, purple for lawyer, red for admin
  - Use Tailwind color classes (e.g., `bg-blue-100 text-blue-800` for member, `bg-green-100 text-green-800` for logistics, etc.)
  - Display the role's human-readable name (e.g., "Member", "Logistics Provider", "Insurance Provider", "Lawyer", "Admin")
  - Optional lucide-react icon per role (e.g., User for member, Truck for logistics, Shield for insurance, Scale for lawyer, Crown for admin)
- Size variants: sm (text-xs, px-2 py-0.5), md (text-sm, px-3 py-1), lg (text-base, px-4 py-1.5)
- If role is null/undefined, show "Member" (null fallback for existing accounts)
- Export as named export: `export function RoleBadge({ role, size = 'sm' })`

**2. Update `Navbar.jsx`:**

Read the existing file. The Navbar already has NAV_LINKS and reads `user` from useAuth.

Extend NAV_LINKS with a `roles` property for role-based filtering:
- Existing links (Products, RFQs, Fairs, Companies, etc.): `roles: null` (all roles see these) OR scope appropriately:
  - Products, Fairs, Companies, Categories: `roles: null` (all)
  - RFQs/Requests: `roles: ['member', 'admin']` (members create RFQs; providers/lawyers don't)
  - Dashboard: keep existing (authenticated users)
  - Messages: keep existing (authenticated users)
- New links:
  - "Provider Dashboard": `href: '/provider'`, `roles: ['logistics_provider', 'insurance_provider']`
  - "Client Channels": `href: '/lawyer/channels'`, `roles: ['lawyer']` (placeholder for Phase 5)
  - "Deal Review": `href: '/lawyer/deals'`, `roles: ['lawyer']` (placeholder for Phase 5)

Filter rendered links: `visibleLinks = NAV_LINKS.filter(link => link.roles === null || (user && link.roles.includes(user.role)))`

Per user decision: "Menu items hidden completely for unauthorized areas (not greyed out)." So filter out — do not render with disabled state.

Per user decision: "Existing navbar routes stay as-is; Phase 1 adds role-based filtering to show/hide items" and "Existing notification bell remains unchanged."

Keep the existing mobile menu working with the filtered links too.

**3. Create `src/app/(main)/provider/page.jsx`:**

Per user decision: "Providers (insurance/logistics): single dashboard page with tabs/sections for requests, submitted quotes, and history."

Create a placeholder page with the tab structure ready for Phase 4:
- `'use client'` component
- Page title: "Provider Dashboard"
- Tab structure (not functional yet — Phase 4 fills in content):
  - "Quote Requests" tab (empty state: "No quote requests yet")
  - "Submitted Quotes" tab (empty state: "No submitted quotes yet")
  - "History" tab (empty state: "No history yet")
- Show the user's role with RoleBadge (insurance_provider vs logistics_provider)
- Use existing Tailwind patterns for tabs (check if any tab patterns exist in the codebase)
- Simple tabs with useState — active tab highlighted, content area below

**4. Create `src/app/(main)/lawyer/page.jsx`:**

Per user decision: "Lawyers: separate nav items for client channels and deal review."

Since lawyer nav has two separate routes (/lawyer/channels and /lawyer/deals), create a landing page at /lawyer that redirects or shows a simple workspace:
- `'use client'` component
- Page title: "Lawyer Workspace"
- Two card links:
  - "Client Channels" card -> `/lawyer/channels` (placeholder for Phase 5): "View and manage your client communications"
  - "Deal Review" card -> `/lawyer/deals` (placeholder for Phase 5): "Review deals and provide legal analysis"
- Show role badge
- Empty state messaging: "Your channels and deals will appear here once clients hire you."

Note: The actual `/lawyer/channels` and `/lawyer/deals` pages are Phase 5 work. For now, these routes can show a "Coming soon" or redirect to the /lawyer landing page. Create simple placeholder pages at those paths if needed for the nav links to not 404.

**5. Update profile page `src/app/(main)/profile/[userId]/page.jsx`:**

Read the existing file. Per user decision: "User's own role shown on profile page only (not in navbar)."

- Import RoleBadge component
- In the profile display section, add the user's role badge
- Only show the role for the profile owner (the logged-in user viewing their own profile)
- For other users' profiles, also show the role badge (per decision: "Other users' roles shown with role badge in deal views, messages, and interactions")
- Place the role badge near the user's name or in the profile header area
  </action>
  <verify>
1. Verify RoleBadge renders with correct colors for each of the 5 roles and null fallback
2. Verify Navbar filters links by role — check that provider/lawyer links only appear for correct roles
3. Verify /provider page renders with 3 tabs (empty states)
4. Verify /lawyer page renders with workspace cards
5. Verify profile page shows RoleBadge for user's role
6. Run `npx next lint` on all modified/created files
  </verify>
  <done>
Navbar shows role-filtered links: members see member items, providers see Provider Dashboard, lawyers see Client Channels and Deal Review. RoleBadge component renders color-coded badges for all 5 roles. Provider dashboard has placeholder tab structure. Lawyer workspace has card links to channels and deals. Profile page shows user's role. All unauthorized nav items are completely hidden, not greyed out.
  </done>
</task>

</tasks>

<verification>
1. Member user sees Products, RFQs, etc. but NOT Provider Dashboard or Client Channels
2. Provider user sees Provider Dashboard but NOT RFQs
3. Lawyer user sees Client Channels and Deal Review but NOT Provider Dashboard
4. Navigating to /provider as a member redirects to /forbidden
5. Forbidden page shows clear message with link home
6. RoleBadge shows correct colors per role
7. Profile page displays role badge
</verification>

<success_criteria>
- Navbar renders different links for member vs provider vs lawyer roles
- Middleware redirects unauthorized access to /forbidden
- Forbidden page renders with access denied message
- RoleBadge works for all 5 roles + null fallback
- Provider placeholder page has 3 tab structure
- Lawyer placeholder page has workspace cards
- Profile shows role badge
</success_criteria>

<output>
After completion, create `.planning/phases/01-role-system-and-infrastructure/01-03-SUMMARY.md`
</output>
