---
phase: 05-legal-consulting
plan: 06
type: execute
wave: 5
depends_on: ["05-02", "05-04", "05-05"]
files_modified:
  - src/presentation/components/homepage/Navbar/Navbar.jsx
  - functions/index.js
  - firestore.indexes.json
autonomous: false
requirements:
  - LEGAL-08
  - LEGAL-03
  - LEGAL-04
  - LEGAL-05
  - LEGAL-06
  - LEGAL-07

must_haves:
  truths:
    - "Lawyer-specific navbar links appear for lawyer role users"
    - "Notification infrastructure sends in-app + email for all legal lifecycle events"
    - "Firestore-triggered Cloud Functions send message-level notifications (new_message, new_draft, risk_update) to the other participant"
    - "Deal can proceed through all stages without hiring a lawyer (optional flow verified)"
    - "Completed engagement channel is read-only at both UI and Firestore rule level"
    - "End-to-end legal consulting flow works: browse directory -> hire -> accept -> chat -> draft -> risk -> close -> review"
  artifacts:
    - path: "src/presentation/components/homepage/Navbar/Navbar.jsx"
      provides: "Lawyer-specific nav items (Dashboard, placeholder for Channels)"
    - path: "functions/index.js"
      provides: "Legal notification triggers wired to engagement lifecycle events"
  key_links:
    - from: "Navbar"
      to: "lawyer dashboard"
      via: "nav link for lawyer role"
      pattern: "lawyer.*dashboard"
    - from: "functions/index.js"
      to: "sendLegalNotification"
      via: "called from hireLayyer, respondToHireRequest, closeLegalEngagement"
      pattern: "sendLegalNotification"
    - from: "functions/index.js onLegalMessageCreated"
      to: "legalMessages subcollection"
      via: "onDocumentCreated trigger fires on new message, notifies other participant"
      pattern: "onDocumentCreated.*legalMessages"
    - from: "functions/index.js onContractDraftCreated"
      to: "contractDrafts subcollection"
      via: "onDocumentCreated trigger fires on new draft, notifies client"
      pattern: "onDocumentCreated.*contractDrafts"
    - from: "functions/index.js onRiskItemCreated"
      to: "riskItems subcollection"
      via: "onDocumentCreated trigger fires on new risk item, notifies client"
      pattern: "onDocumentCreated.*riskItems"
---

<objective>
Wire lawyer-specific navigation, verify notification delivery, and run an end-to-end verification of the complete legal consulting flow to confirm all requirements are met.

Purpose: Final integration and verification. This ensures the legal consulting feature is fully wired, notifications fire correctly, the navbar shows lawyer-relevant items, and the entire flow works end-to-end -- while confirming that legal consulting remains optional (deals can proceed without it).

Output: Updated Navbar for lawyer role, verified notification delivery, end-to-end verification checkpoint.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-legal-consulting/05-RESEARCH.md
@.planning/phases/05-legal-consulting/05-01-SUMMARY.md
@.planning/phases/05-legal-consulting/05-02-SUMMARY.md
@.planning/phases/05-legal-consulting/05-04-SUMMARY.md
@.planning/phases/05-legal-consulting/05-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire lawyer navbar links and verify notification integration</name>
  <files>
    src/presentation/components/homepage/Navbar/Navbar.jsx,
    functions/index.js
  </files>
  <action>
1. Update `src/presentation/components/homepage/Navbar/Navbar.jsx`:
   - Read the existing Navbar to understand the current nav items structure and role-based filtering
   - Add lawyer-specific nav links to the navigation items array:
     - "Lawyer Dashboard" -> `/lawyer/dashboard` (roles: ['lawyer'])
   - These should follow the existing pattern where nav items have a `roles` array and are filtered based on user role
   - Admin should also see lawyer links (existing admin bypass pattern: `user.role === ROLES.ADMIN || link.roles.includes(user.role)`)
   - Remove or update any placeholder lawyer links from Phase 1 (e.g., "Client Channels" -> now the dashboard covers this, and channels are accessed from within deals)

2. Verify notification wiring in `functions/index.js`:
   - Review that `sendLegalNotification` is called at these points:
     a) `hireLayyer` -> sends 'hire_request' to lawyer
     b) `respondToHireRequest` (accept) -> sends 'hire_accepted' to client
     c) `respondToHireRequest` (decline) -> sends 'hire_declined' to client
     d) `closeLegalEngagement` -> sends 'engagement_completed' to other participant
   - All notification calls must be OUTSIDE transactions (project pattern)
   - All notification calls must be non-blocking (try/catch with console.error)
   - If any notification call is missing from Plan 02 implementation, add it now

3. Add three Firestore-triggered Cloud Functions for message-level notifications (per CONTEXT.md locked decisions):

   a) `exports.onLegalMessageCreated = onDocumentCreated('legalEngagements/{engagementId}/legalMessages/{messageId}', async (event) => { ... })`:
      - Read the parent engagement doc at `legalEngagements/{engagementId}` to get `clientId`, `lawyerId`, `dealId`, `dealProductName`
      - Read the created message doc from `event.data` to get `senderId`
      - Determine recipient: if `senderId === clientId` then recipient is `lawyerId` (lawyer gets 'new_message'); if `senderId === lawyerId` then recipient is `clientId` (client gets 'new_message')
      - Skip notification for system messages (type === 'system') -- system messages are generated by Cloud Functions that already send their own notifications
      - Call `sendLegalNotification(engagementId, 'new_message', recipientId, dealProductName, dealId)`
      - Wrap entire body in try/catch -- trigger failures must not throw (log error only)

   b) `exports.onContractDraftCreated = onDocumentCreated('legalEngagements/{engagementId}/contractDrafts/{draftId}', async (event) => { ... })`:
      - Read the parent engagement doc to get `clientId`, `dealId`, `dealProductName`
      - The client always receives this notification (drafts are uploaded by the lawyer per Firestore rules)
      - Call `sendLegalNotification(engagementId, 'new_draft', clientId, dealProductName, dealId)`
      - Wrap entire body in try/catch

   c) `exports.onRiskItemCreated = onDocumentCreated('legalEngagements/{engagementId}/riskItems/{riskId}', async (event) => { ... })`:
      - Read the parent engagement doc to get `clientId`, `dealId`, `dealProductName`
      - The client always receives this notification (risk items are created by the lawyer per Firestore rules)
      - Call `sendLegalNotification(engagementId, 'risk_update', clientId, dealProductName, dealId)`
      - Wrap entire body in try/catch

   Import `onDocumentCreated` from `firebase-functions/v2/firestore` at the top of the file (alongside existing `onCall` import from `firebase-functions/v2/https`). Verify the import path is correct -- existing functions/index.js uses v2 patterns.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Navbar shows "Lawyer Dashboard" link for lawyer role users
    - Admin can see lawyer links via admin bypass
    - sendLegalNotification called from all 4 lifecycle Cloud Functions
    - All notification calls are outside transactions and non-blocking
    - 3 Firestore onDocumentCreated triggers added: onLegalMessageCreated, onContractDraftCreated, onRiskItemCreated
    - Message-level notifications (new_message, new_draft, risk_update) fire automatically when subcollection documents are created
    - System messages are excluded from triggering new_message notifications
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end legal consulting verification</name>
  <files>N/A (verification only)</files>
  <action>
Deploy and verify the complete legal consulting feature end-to-end.

**Pre-requisite:** Deploy Cloud Functions: `firebase deploy --only functions`. Deploy rules: `firebase deploy --only firestore:rules`. Deploy indexes: check `firebase firestore:indexes` first (per MEMORY.md -- never force), then `firebase deploy --only firestore:indexes`.

**What was built across 6 plans:**
- Data layer: entities, repositories, Firestore rules with channel isolation, composite indexes
- Cloud Functions: hireLayyer, respondToHireRequest, closeLegalEngagement, submitLawyerReview
- Lawyer directory: /lawyers page with search/filter, profile adaptation
- Legal banner: hire CTA on DealPage at all stages, active engagement badge
- Lawyer dashboard: /lawyer/dashboard with pending/active/completed sections
- Legal channel: 3-panel UI at /deals/[dealId]/legal with chat, contract drafts, risk analysis
- Navbar: lawyer-specific navigation items
- Notifications: in-app + email for lifecycle events + Firestore-triggered message-level notifications (new_message, new_draft, risk_update)

**Verification flows:**

Flow 1 - Directory: /lawyers shows searchable directory, cards link to /profile/[id] with lawyer content.

Flow 2 - Hiring: DealPage shows gold banner at all stages. "Find a Lawyer" links to directory. After hire, banner shows pending/active badge. Lawyer dashboard shows pending request with Accept/Decline.

Flow 3 - Channel: 3-panel layout at /deals/[dealId]/legal. Chat works with text, attachments, quick-actions. Contract tab shows latest draft with versioning. Risks tab has CRUD.

Flow 4 - Lifecycle: Close engagement -> read-only channel. Review prompt for client.

Flow 5 - Optional (LEGAL-08): Deal proceeds without lawyer involvement.

Flow 6 - Privacy: Opposing party sees no indication of lawyer engagement.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - All 6 verification flows pass
    - All 8 LEGAL requirements confirmed working
    - User approves the feature
  </done>
</task>

</tasks>

<verification>
- Navbar shows lawyer-specific links for lawyer role
- Cloud Functions deploy successfully
- Firestore rules deploy successfully
- Composite indexes created
- Full hire -> accept -> chat -> draft -> risk -> close -> review flow works
- Channel is read-only after completion
- Legal consulting is optional (deals proceed without lawyers)
- Privacy: opposing party cannot detect lawyer engagement
</verification>

<success_criteria>
All 8 LEGAL requirements verified:
- LEGAL-01: Buyer can hire a lawyer for a deal
- LEGAL-02: Seller can hire a different lawyer for the same deal
- LEGAL-03: Private channel isolated from opposing party
- LEGAL-04: Lawyer can view full deal details
- LEGAL-05: Contract drafts with version history
- LEGAL-06: Risk analysis with severity levels
- LEGAL-07: Quick-action buttons for both roles
- LEGAL-08: Legal consulting is optional
</success_criteria>

<output>
After completion, create `.planning/phases/05-legal-consulting/05-06-SUMMARY.md`
</output>
