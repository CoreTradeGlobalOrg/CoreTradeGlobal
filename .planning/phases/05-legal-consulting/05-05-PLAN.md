---
phase: 05-legal-consulting
plan: 05
type: execute
wave: 4
depends_on: ["05-01", "05-02", "05-04"]
files_modified:
  - src/app/(main)/deals/[dealId]/legal/page.jsx
  - src/presentation/hooks/legal/useLegalMessages.js
  - src/presentation/hooks/legal/useLegalChannel.js
  - src/presentation/components/features/legal/LegalChannel/LegalChannel.jsx
  - src/presentation/components/features/legal/LegalChannel/ChannelLeft.jsx
  - src/presentation/components/features/legal/LegalChannel/ChannelCenter.jsx
  - src/presentation/components/features/legal/LegalChannel/ChannelRight.jsx
  - src/presentation/components/features/legal/LegalChannel/QuickActionToolbar.jsx
autonomous: true
requirements:
  - LEGAL-03
  - LEGAL-04
  - LEGAL-05
  - LEGAL-06
  - LEGAL-07

must_haves:
  truths:
    - "Lawyer and client can exchange private messages in the legal channel"
    - "Lawyer can view full deal details in the left panel"
    - "Lawyer can upload contract draft files that auto-version (v1, v2, v3...)"
    - "Lawyer can add risk analysis items with severity levels (low/medium/high)"
    - "Quick-action toolbar shows role-appropriate buttons for both lawyer and client"
    - "Files attached in chat appear inline AND in the left panel documents list"
    - "Right panel has 3 tabs: Contract (latest draft), Revisions (version history), Risks (analysis cards)"
  artifacts:
    - path: "src/app/(main)/deals/[dealId]/legal/page.jsx"
      provides: "/deals/[dealId]/legal route with auth + participant guard"
    - path: "src/presentation/components/features/legal/LegalChannel/LegalChannel.jsx"
      provides: "3-panel layout: left (profile+deal+docs), center (chat+toolbar), right (contract/revisions/risks)"
    - path: "src/presentation/components/features/legal/LegalChannel/ChannelCenter.jsx"
      provides: "Encrypted chat with system messages, file attachments, quick-action toolbar"
    - path: "src/presentation/components/features/legal/LegalChannel/ChannelRight.jsx"
      provides: "3-tab panel: Contract, Revisions, Risks"
    - path: "src/presentation/components/features/legal/LegalChannel/QuickActionToolbar.jsx"
      provides: "Role-aware quick-action buttons above chat input"
  key_links:
    - from: "LegalChannel.jsx"
      to: "useLegalChannel hook"
      via: "orchestrates engagement, messages, drafts, risks subscriptions"
      pattern: "useLegalChannel"
    - from: "ChannelCenter.jsx"
      to: "LegalMessageRepository.sendMessage"
      via: "send message on form submit"
      pattern: "sendMessage"
    - from: "ChannelRight.jsx"
      to: "LegalEngagementRepository.subscribeToContractDrafts + subscribeToRiskItems"
      via: "real-time subscription to drafts and risks"
      pattern: "subscribeToContractDrafts|subscribeToRiskItems"
---

<objective>
Build the full 3-panel legal channel UI at /deals/[dealId]/legal: private encrypted chat between lawyer and client, deal info panel, contract draft workspace with versioning, and risk analysis panel.

Purpose: This is the core legal consulting experience -- where the lawyer and client communicate, share documents, review contract drafts, and track risks. The 3-panel layout follows the hukuk-danismanlik.html mockup exactly.

Output: Legal channel route, 3-panel layout component, chat with quick-actions, contract draft upload/versioning, risk analysis CRUD, file attachment support.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-legal-consulting/05-RESEARCH.md
@.planning/phases/05-legal-consulting/05-01-SUMMARY.md
@.planning/phases/05-legal-consulting/05-02-SUMMARY.md
@.planning/phases/05-legal-consulting/05-04-SUMMARY.md

<interfaces>
<!-- From Plan 01: Repository methods for channel -->
From src/data/repositories/LegalEngagementRepository.js:
```javascript
subscribeToEngagement(engagementId, callback)
subscribeToContractDrafts(engagementId, callback)
subscribeToRiskItems(engagementId, callback)
addContractDraft(engagementId, draftData)
addRiskItem(engagementId, riskData)
updateRiskItem(engagementId, riskId, updates)
getMaxDraftVersion(engagementId)
```

From src/data/repositories/LegalMessageRepository.js:
```javascript
subscribeToMessages(engagementId, callback, limitCount)
sendMessage(engagementId, messageData)
uploadAttachment(engagementId, file)
uploadDraftFile(engagementId, file)
```

<!-- From Plan 04: Hooks -->
From src/presentation/hooks/legal/useLegalEngagement.js:
```javascript
useLegalEngagement(dealId, clientId) -> { engagement, loading, error }
```

From src/presentation/hooks/legal/useLegalActions.js:
```javascript
useLegalActions() -> { hireLawyer, respondToHireRequest, closeLegalEngagement, submitReview, loading, error }
```

<!-- Existing deal data access -->
From src/data/repositories/DealRepository.js:
```javascript
getById(dealId) -> deal object
subscribeToOffer(dealId, callback)
```

<!-- Legal constants -->
From src/core/constants/legalConstants.js:
```javascript
QUICK_ACTIONS  // client and lawyer action definitions
LEGAL_MESSAGE_TYPE  // text, attachment, system, quick_action
ALLOWED_LEGAL_FILE_TYPES  // PDF, DOCX, DOC, XLSX, JPG, PNG
RISK_SEVERITY  // low, medium, high
RISK_STATUS  // open, resolved
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create legal channel route, hooks, and 3-panel layout shell</name>
  <files>
    src/app/(main)/deals/[dealId]/legal/page.jsx,
    src/presentation/hooks/legal/useLegalMessages.js,
    src/presentation/hooks/legal/useLegalChannel.js,
    src/presentation/components/features/legal/LegalChannel/LegalChannel.jsx,
    src/presentation/components/features/legal/LegalChannel/ChannelLeft.jsx
  </files>
  <action>
1. Create `src/app/(main)/deals/[dealId]/legal/page.jsx`:
   - 'use client' directive
   - Extract dealId from params (use React.use() for Next.js 15 params promise pattern if applicable, or existing useParams -- check how /deals/[dealId]/contract/page.jsx handles this)
   - Auth guard: redirect to /login if not authenticated
   - Load deal data via container.getDealRepository().getById(dealId)
   - Determine engagement: the engagement ID is `${dealId}_${currentUser.uid}` (deterministic from Plan 02). Check if current user is a deal participant (buyer/seller) -- use deal.buyerId/sellerId check. If user is a lawyer, find engagement where lawyerId === currentUser.uid (subscribe to engagement by querying where lawyerId === uid AND dealId === dealId).
   - For members (clients): use engagementId = `${dealId}_${currentUser.uid}` directly
   - For lawyers: query engagement by dealId + lawyerId match (may need a specific subscription method or derive from existing subscribeToEngagementsForLawyer filtered by dealId)
   - If no engagement found for current user -> show "No legal engagement found for this deal" with link back to deal page
   - If engagement status is pending -> show "Waiting for lawyer to accept" message
   - If active or completed -> render `LegalChannel` component
   - Pass: engagement, deal, currentUser, isLawyer (boolean), isReadOnly (engagement.isCompleted())

2. Create `src/presentation/hooks/legal/useLegalMessages.js`:
   - Subscribes to messages for an engagement
   - Parameters: `(engagementId)` -- required
   - Uses `container.getLegalMessageRepository().subscribeToMessages(engagementId, callback)`
   - Also provides `sendMessage(content, type, quickAction?, attachments?)` function that calls `LegalMessageRepository.sendMessage()` with proper data shape
   - Also provides `uploadAndSendAttachment(file)` function that:
     a) Validates file type against ALLOWED_LEGAL_FILE_TYPES
     b) Calls `uploadAttachment(engagementId, file)` to get { url, storagePath, name, type, size }
     c) Sends a message with type='attachment' and attachments array containing the upload result
   - Returns: `{ messages, sendMessage, uploadAndSendAttachment, loading, error, sending }`

3. Create `src/presentation/hooks/legal/useLegalChannel.js`:
   - Orchestrating hook that combines multiple subscriptions for the channel
   - Parameters: `(engagementId)`
   - Subscribes to:
     a) Contract drafts via `subscribeToContractDrafts(engagementId, callback)` -> `drafts` array
     b) Risk items via `subscribeToRiskItems(engagementId, callback)` -> `riskItems` array
   - Provides actions:
     a) `uploadDraft(file)` -- uploads file via `uploadDraftFile()`, gets max version, creates draft doc with version+1
     b) `addRisk(riskData)` -- calls `addRiskItem(engagementId, { ...riskData, createdByUid: currentUser.uid, createdAt: serverTimestamp(), updatedAt: serverTimestamp() })`
     c) `toggleRiskStatus(riskId, currentStatus)` -- calls `updateRiskItem(engagementId, riskId, { status: currentStatus === 'open' ? 'resolved' : 'open', updatedAt: serverTimestamp() })`
   - Returns: `{ drafts, riskItems, uploadDraft, addRisk, toggleRiskStatus, loading }`

4. Create `src/presentation/components/features/legal/LegalChannel/LegalChannel.jsx`:
   - Props: `engagement`, `deal`, `currentUser`, `isLawyer`, `isReadOnly`
   - 3-panel grid layout:
     - Left panel: ~25% width (hidden on mobile, toggleable)
     - Center panel: ~50% width (fills on mobile)
     - Right panel: ~25% width (hidden on mobile, toggleable)
   - Uses `useLegalMessages(engagement.id)` for chat
   - Uses `useLegalChannel(engagement.id)` for drafts and risks
   - Mobile: show center panel by default, toggle buttons for left/right panels
   - Header bar above panels: "Encrypted & Private" badge with Lock icon (purple themed per mockup), engagement status, close engagement button (if active and not read-only)
   - Renders: `<ChannelLeft>`, `<ChannelCenter>`, `<ChannelRight>`

5. Create `src/presentation/components/features/legal/LegalChannel/ChannelLeft.jsx`:
   - Props: `engagement`, `deal`, `messages`, `isLawyer`
   - Sections (collapsible):
     a) **Lawyer Profile**: lawyer photo/initials, name, specializations, availability badge
     b) **Deal Info**: product name, parties (buyer name, seller name), Incoterms, price (if visible -- always visible in legal channel since both parties are involved), deal status
     c) **Documents**: list all attachments from messages (filter messages where type === 'attachment'), show file name, type icon, download link. Also include contract drafts from the drafts list. Sorted by date descending.
     d) **Consulting Status**: engagement status, created date, "Close Engagement" button (if active)
   - Each section has a header with toggle chevron (ChevronDown/ChevronUp from lucide)
   - Scrollable within panel height (overflow-y-auto)
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /deals/[dealId]/legal route renders with auth + participant guard
    - Route handles both member (client) and lawyer access paths
    - useLegalMessages provides message subscription + send + upload
    - useLegalChannel provides drafts + risks subscriptions + CRUD actions
    - LegalChannel renders 3-panel layout with responsive mobile support
    - ChannelLeft shows lawyer profile, deal info, documents, and status
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ChannelCenter (chat + toolbar), ChannelRight (drafts/risks tabs), and QuickActionToolbar</name>
  <files>
    src/presentation/components/features/legal/LegalChannel/ChannelCenter.jsx,
    src/presentation/components/features/legal/LegalChannel/ChannelRight.jsx,
    src/presentation/components/features/legal/LegalChannel/QuickActionToolbar.jsx
  </files>
  <action>
1. Create `src/presentation/components/features/legal/LegalChannel/ChannelCenter.jsx`:
   - Props: `messages`, `sendMessage`, `uploadAndSendAttachment`, `currentUser`, `isLawyer`, `isReadOnly`, `engagement`
   - Message list:
     - Scrollable message container (ref-based auto-scroll to bottom on new messages)
     - Each message renders differently by type:
       a) `text`: chat bubble (right-aligned if own, left if other). Show sender name, timestamp (date-fns format), content.
       b) `attachment`: chat bubble with file icon + file name + download link + file size. Show preview thumbnail for images (JPG/PNG).
       c) `system`: centered, purple-themed system message (e.g., "Legal consulting session started..."). Use italic text, smaller font.
       d) `quick_action`: system-style message with action badge (e.g., "[Approve] Client approved the contract terms"). Show action icon + label.
     - Message grouping: consecutive messages from same sender within 5 minutes show sender name only on first message
     - Date separators between different days (e.g., "March 10, 2026")
   - Input area (hidden if isReadOnly):
     - Text input (textarea with auto-resize) + Send button
     - Attach file button (paperclip icon) opening file picker with ALLOWED_LEGAL_FILE_TYPES
     - On send: call `sendMessage(content, 'text')`
     - On file select: call `uploadAndSendAttachment(file)` with loading indicator
   - QuickActionToolbar rendered above the input area

2. Create `src/presentation/components/features/legal/LegalChannel/QuickActionToolbar.jsx`:
   - Props: `isLawyer`, `onAction`, `isReadOnly`
   - Import QUICK_ACTIONS from legalConstants
   - Filter actions by role: if isLawyer, show lawyer actions; if client, show client actions
   - Render as a horizontal row of icon buttons above the chat input:
     - Each button: icon (from lucide-react, mapped from QUICK_ACTIONS[].icon) + label text
     - On click: call `onAction(action)` which triggers `sendMessage` with type='quick_action' and quickAction payload
   - For "Attach File" action: trigger the file picker instead of sending a message
   - For "Send Draft" action (lawyer only): open a file picker filtered to DOCX/PDF, then call the draft upload flow (pass `onUploadDraft` callback)
   - For "Flag Risk" action (lawyer only): open a small inline form (title, description, severity dropdown) and call `addRisk`
   - Compact styling: small buttons with tooltips for labels on desktop, icon-only on mobile
   - Hidden when isReadOnly

3. Create `src/presentation/components/features/legal/LegalChannel/ChannelRight.jsx`:
   - Props: `drafts`, `riskItems`, `onUploadDraft`, `onAddRisk`, `onToggleRiskStatus`, `isLawyer`, `isReadOnly`
   - 3-tab layout using useState for activeTab: 'contract' | 'revisions' | 'risks'
   - Tab headers styled as a row of buttons at the top of the panel

   **Contract tab** (default):
   - Shows the LATEST draft (highest version number from drafts array)
   - If no drafts: empty state "No contract drafts yet" with upload CTA (lawyer only)
   - If draft exists: file name, version badge ("v3"), upload date (formatted), uploader name, file size
   - Download button linking to draft.fileUrl
   - "Upload New Draft" button (lawyer only, not read-only) -- triggers file picker, calls `onUploadDraft(file)`
   - File preview: for PDF files, show an iframe preview or just the download link (keep simple for v1)

   **Revisions tab**:
   - Lists ALL drafts ordered by version descending
   - Each revision: version badge, file name, uploader name, upload date, download link
   - Visual timeline connector between versions (vertical line with dots)
   - Highlight the latest version

   **Risks tab**:
   - Lists all risk items
   - Each risk card: title, description (truncatable), severity badge (red high, amber medium, green low), status badge (open/resolved)
   - Toggle button to mark resolved/open (lawyer only, calls `onToggleRiskStatus`)
   - "Add Risk" button (lawyer only, not read-only) opening inline form:
     - Title input (required)
     - Description textarea (required)
     - Severity select: Low / Medium / High
     - Submit button calling `onAddRisk({ title, description, severity, status: 'open' })`
   - Summary at top: "3 risks identified (1 high, 1 medium, 1 low)"
   - Empty state: "No risk items identified yet"
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - ChannelCenter renders chat with 4 message types: text, attachment, system, quick_action
    - Auto-scroll to bottom on new messages
    - Chat input with send button and file attachment
    - QuickActionToolbar shows role-appropriate buttons (client vs lawyer)
    - Quick actions send system-style messages with action payloads
    - ChannelRight has 3 tabs: Contract, Revisions, Risks
    - Contract tab shows latest draft with download and upload new
    - Revisions tab shows version history timeline
    - Risks tab shows risk cards with CRUD operations
    - All panels respect isReadOnly flag (hide input/actions when completed)
  </done>
</task>

</tasks>

<verification>
- Navigate to /deals/[dealId]/legal -- see 3-panel layout
- Left panel shows lawyer profile, deal info, documents list
- Center panel shows chat messages with proper styling per type
- Can send text messages (appear in chat immediately via real-time subscription)
- Can attach files (appear inline in chat + in documents list)
- Quick-action toolbar shows correct buttons per role
- Right panel Contract tab shows latest draft
- Right panel Revisions tab shows version history
- Right panel Risks tab shows risk cards with add/toggle
- Read-only mode hides all input/action elements
- `npx next build` succeeds
</verification>

<success_criteria>
- 3-panel legal channel fully functional at /deals/[dealId]/legal
- Private messaging works with real-time updates
- 4 message types rendered correctly (text, attachment, system, quick_action)
- Contract draft versioning works (auto-increment, version history)
- Risk analysis CRUD works (add, toggle status)
- Quick-action toolbar is role-aware
- Read-only enforcement on completed channels
- Responsive layout (panels toggle on mobile)
</success_criteria>

<output>
After completion, create `.planning/phases/05-legal-consulting/05-05-SUMMARY.md`
</output>
