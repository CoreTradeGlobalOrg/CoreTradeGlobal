---
phase: 05-legal-consulting
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/lawyer/channels/page.jsx
  - src/app/(main)/lawyer/deals/page.jsx
  - src/presentation/components/features/legal/LawyerChannels/LawyerChannels.jsx
  - src/presentation/components/features/legal/LawyerDeals/LawyerDeals.jsx
  - src/presentation/components/homepage/Navbar/Navbar.jsx
autonomous: true
requirements:
  - LEGAL-03
  - LEGAL-04
gap_closure: true

must_haves:
  truths:
    - "/lawyer/channels shows a list of all lawyer engagements with client name, deal product, status badge, last message preview, and click-through to /deals/{dealId}/legal"
    - "/lawyer/deals shows a deal-centric list of engaged deals with product, parties, deal status, engagement status, and Open Channel link"
    - "Navbar shows Client Channels and Deal Review links alongside Lawyer Dashboard for lawyer role"
  artifacts:
    - path: "src/app/(main)/lawyer/channels/page.jsx"
      provides: "Real channels list page (not redirect)"
      min_lines: 15
    - path: "src/app/(main)/lawyer/deals/page.jsx"
      provides: "Real deals review page (not redirect)"
      min_lines: 15
    - path: "src/presentation/components/features/legal/LawyerChannels/LawyerChannels.jsx"
      provides: "Engagement list UI with last message preview"
      min_lines: 40
    - path: "src/presentation/components/features/legal/LawyerDeals/LawyerDeals.jsx"
      provides: "Deal-centric engagement list UI"
      min_lines: 40
    - path: "src/presentation/components/homepage/Navbar/Navbar.jsx"
      provides: "Client Channels and Deal Review nav links for lawyer role"
      contains: "channels"
  key_links:
    - from: "Navbar"
      to: "/lawyer/channels"
      via: "nav link for lawyer role"
      pattern: "lawyer/channels"
    - from: "Navbar"
      to: "/lawyer/deals"
      via: "nav link for lawyer role"
      pattern: "lawyer/deals"
    - from: "LawyerChannels"
      to: "useLegalEngagements"
      via: "hook subscription for all lawyer engagements"
      pattern: "useLegalEngagements"
    - from: "LawyerChannels"
      to: "/deals/{dealId}/legal"
      via: "click-through link on each engagement card"
      pattern: "deals.*legal"
    - from: "LawyerDeals"
      to: "useLegalEngagements"
      via: "hook subscription for engagement data"
      pattern: "useLegalEngagements"
---

<objective>
Build real /lawyer/channels and /lawyer/deals pages to replace the redirect stubs, and add corresponding Navbar links for the lawyer role.

Purpose: UAT Test 13 reported that /lawyer/channels and /lawyer/deals were redirect stubs instead of functional pages. The user expected these as standalone pages for managing client channels and reviewing deal details -- not just redirects to the dashboard.

Output: Two new functional pages for lawyers: a channels list (engagement-centric) and a deals list (deal-centric), plus updated Navbar with all three lawyer links.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-legal-consulting/05-UAT.md
@.planning/phases/05-legal-consulting/05-04-SUMMARY.md

<interfaces>
<!-- From src/presentation/hooks/legal/useLegalEngagements.js -->
export function useLegalEngagements(lawyerId: string): {
  engagements: LegalEngagement[],
  pendingEngagements: LegalEngagement[],
  activeEngagements: LegalEngagement[],
  completedEngagements: LegalEngagement[],
  loading: boolean,
  error: string|null
}

<!-- From src/domain/entities/LegalEngagement.js (key fields on each engagement) -->
LegalEngagement {
  id: string,
  clientId: string,
  lawyerId: string,
  dealId: string,
  participants: string[],
  dealProductName: string,    // denormalized product name from deal
  clientDisplayName: string,  // denormalized client name
  status: string,             // ENGAGEMENT_STATUS: 'pending' | 'active' | 'completed'
  createdAt: Date,
  updatedAt: Date
}

<!-- From src/core/constants/legalConstants.js -->
ENGAGEMENT_STATUS = { PENDING: 'pending', ACTIVE: 'active', COMPLETED: 'completed' }

<!-- From src/presentation/components/features/legal/LawyerDashboard/EngagementCard.jsx -->
// StatusBadge component can be imported or pattern copied
// EngagementCard accepts { engagement, onAccept, onDecline, actionLoading }

<!-- Navbar nav items pattern (from Navbar.jsx) -->
// Items are objects: { label: string, href: string, roles: string[] }
// Current lawyer items: [{ label: 'Lawyer Dashboard', href: '/lawyer/dashboard', roles: [ROLES.LAWYER, ROLES.ADMIN] }]
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build LawyerChannels page -- engagement list with last message preview</name>
  <files>
    src/presentation/components/features/legal/LawyerChannels/LawyerChannels.jsx,
    src/app/(main)/lawyer/channels/page.jsx
  </files>
  <action>
1. **Create `src/presentation/components/features/legal/LawyerChannels/LawyerChannels.jsx`**:

   A client component ('use client') that displays all engagements for the logged-in lawyer as a list focused on channel communication.

   - Import `useLegalEngagements` from `@/presentation/hooks/legal/useLegalEngagements`
   - Import `useAuth` from `@/presentation/contexts/AuthContext`
   - Import `ENGAGEMENT_STATUS` from `@/core/constants/legalConstants`
   - Import `Link` from `next/link`, `formatDistanceToNow` from `date-fns`
   - Import icons: `MessageCircle, User, Package, ExternalLink, Clock` from `lucide-react`

   Component structure:
   - Call `const { user } = useAuth()` and `const { engagements, loading } = useLegalEngagements(user?.uid)`
   - Page header: "Client Channels" title with subtitle "Manage your legal consulting channels"
   - Loading state: 3-4 skeleton cards (same glass-card pattern as LawyerDashboard)
   - Empty state: "No channels yet" message with glass-card styling

   Engagement list (sorted by updatedAt desc, or createdAt desc as fallback):
   - Each item is a glass-card with:
     a) Client name (from `engagement.clientDisplayName`) with User icon
     b) Deal product (from `engagement.dealProductName`) with Package icon
     c) Status badge -- reuse the same color pattern from EngagementCard: pending=amber, active=green, completed=gray
     d) Time indicator: `formatDistanceToNow(engagement.updatedAt || engagement.createdAt, { addSuffix: true })`
     e) Click-through: The entire card (or an explicit "Open Channel" button) links to `/deals/${engagement.dealId}/legal`
   - For active/completed engagements, show a MessageCircle icon to indicate channel access
   - For pending engagements, show "Awaiting your response" text with a link to `/lawyer/dashboard` to accept/decline (channel is not available until accepted)

   Follow the project's glass-card, bg-radial-navy, and color token conventions. Use `pt-[100px]` for navbar clearance (same as all other pages).

2. **Replace `src/app/(main)/lawyer/channels/page.jsx`** redirect stub with a real page:
   - 'use client' directive
   - Auth guard: useAuth + useRouter redirect pattern (same as /lawyers/page.jsx and /lawyer/dashboard/page.jsx)
   - Role guard: only ROLES.LAWYER and ROLES.ADMIN (import from `@/core/constants/roles`)
   - Render `<LawyerChannels />` when authenticated and authorized
   - Loading spinner while auth resolves (same spinner pattern as other pages)
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /lawyer/channels renders a real page (not a redirect)
    - Shows all lawyer engagements with client name, deal product, status badge, and time
    - Active/completed engagements link to /deals/{dealId}/legal
    - Pending engagements indicate "Awaiting response" with link to dashboard
    - Auth + role guard prevents unauthorized access
    - Build succeeds
  </done>
</task>

<task type="auto">
  <name>Task 2: Build LawyerDeals page and update Navbar with all 3 lawyer links</name>
  <files>
    src/presentation/components/features/legal/LawyerDeals/LawyerDeals.jsx,
    src/app/(main)/lawyer/deals/page.jsx,
    src/presentation/components/homepage/Navbar/Navbar.jsx
  </files>
  <action>
1. **Create `src/presentation/components/features/legal/LawyerDeals/LawyerDeals.jsx`**:

   A client component ('use client') that displays engaged deals from the lawyer's perspective -- deal-centric rather than channel-centric.

   - Same imports pattern as LawyerChannels plus `ENGAGEMENT_STATUS`
   - Call `useLegalEngagements(user?.uid)` to get all engagements
   - Page header: "Deal Review" title with subtitle "Review deals you are consulting on"
   - Loading state: skeleton cards
   - Empty state: "No deals to review" message

   Deal list (show active and completed engagements only -- pending are not yet confirmed):
   - Each item is a glass-card showing:
     a) Deal product name (from `engagement.dealProductName`) as the primary heading with Package icon
     b) Client name (from `engagement.clientDisplayName`) with User icon
     c) Engagement status badge (active=green, completed=gray)
     d) Time: "Engaged {formatDistanceToNow(engagement.createdAt, { addSuffix: true })}"
     e) "Open Channel" link to `/deals/${engagement.dealId}/legal` for active engagements
     f) "View Channel" link (grayed) for completed engagements

   Follow existing glass-card, bg-radial-navy conventions. Use `pt-[100px]` for navbar clearance.

2. **Replace `src/app/(main)/lawyer/deals/page.jsx`** redirect stub with a real page:
   - Same auth + role guard pattern as /lawyer/channels/page.jsx
   - Render `<LawyerDeals />` when authenticated and authorized

3. **Update `src/presentation/components/homepage/Navbar/Navbar.jsx`**:
   Read the current nav items array. Add two new lawyer links AFTER the existing "Lawyer Dashboard" entry:
   ```
   { label: 'Client Channels', href: '/lawyer/channels', roles: [ROLES.LAWYER, ROLES.ADMIN] },
   { label: 'Deal Review', href: '/lawyer/deals', roles: [ROLES.LAWYER, ROLES.ADMIN] },
   ```
   Keep the existing "Lawyer Dashboard" link. The order should be: Lawyer Dashboard, Client Channels, Deal Review. Admin sees all three via the existing admin bypass pattern.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /lawyer/deals renders a real page (not a redirect) showing deal-centric engagement list
    - Active engagements show "Open Channel" link, completed show "View Channel"
    - Pending engagements are excluded (not yet confirmed deals)
    - Navbar shows 3 lawyer links: Lawyer Dashboard, Client Channels, Deal Review
    - Admin can see all 3 lawyer links
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
- /lawyer/channels shows engagement list with client info and channel links (not a redirect)
- /lawyer/deals shows deal-centric list with deal product and engagement status (not a redirect)
- Navbar shows all 3 lawyer links for lawyer and admin roles
- Both pages have auth + role guards
- Both pages follow existing glass-card design conventions
- Build succeeds with no errors
</verification>

<success_criteria>
UAT Test 13 passes: /lawyer/channels and /lawyer/deals are real functional pages, not redirects. Navbar shows all lawyer navigation links.
</success_criteria>

<output>
After completion, create `.planning/phases/05-legal-consulting/05-08-SUMMARY.md`
</output>
