---
phase: 05-legal-consulting
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx
  - src/app/(main)/lawyers/page.jsx
  - src/presentation/components/features/legal/LawyerDirectory/LawyerDirectory.jsx
  - src/presentation/components/features/legal/LawyerDirectory/LawyerCard.jsx
  - src/presentation/components/features/legal/LawyerProfile/LawyerProfileContent.jsx
autonomous: true
requirements:
  - LEGAL-01
  - LEGAL-02
gap_closure: true

must_haves:
  truths:
    - "Find a Lawyer button text is clearly readable on the gold background"
    - "dealId persists through the full navigation flow: deal page -> lawyer directory -> lawyer profile -> hire action"
    - "Clicking Hire This Lawyer on a lawyer profile with a dealId in the URL calls the hireLawyer Cloud Function"
  artifacts:
    - path: "src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx"
      provides: "Readable Find a Lawyer button text"
      contains: "text-black"
    - path: "src/app/(main)/lawyers/page.jsx"
      provides: "dealId extraction from URL searchParams"
      contains: "useSearchParams"
    - path: "src/presentation/components/features/legal/LawyerDirectory/LawyerDirectory.jsx"
      provides: "dealId prop passed through to LawyerCard"
      contains: "dealId"
    - path: "src/presentation/components/features/legal/LawyerDirectory/LawyerCard.jsx"
      provides: "dealId forwarded in profile link URL"
      contains: "dealId"
    - path: "src/presentation/components/features/legal/LawyerProfile/LawyerProfileContent.jsx"
      provides: "Working hire action that calls hireLawyer CF"
      contains: "hireLawyer"
  key_links:
    - from: "LegalBanner (Find a Lawyer)"
      to: "/lawyers?dealId=..."
      via: "Link href with dealId query param"
      pattern: "dealId"
    - from: "/lawyers/page.jsx"
      to: "LawyerDirectory"
      via: "useSearchParams extracts dealId, passes as prop"
      pattern: "searchParams.*dealId"
    - from: "LawyerDirectory"
      to: "LawyerCard"
      via: "dealId prop forwarded to each card"
      pattern: "dealId.*LawyerCard"
    - from: "LawyerCard"
      to: "/profile/{id}?dealId=..."
      via: "Link href appends dealId query param"
      pattern: "dealId"
    - from: "LawyerProfileContent"
      to: "useLegalActions.hireLawyer"
      via: "handleHireClick reads dealId from URL, calls hireLawyer(dealId, lawyerId)"
      pattern: "hireLawyer.*dealId"
---

<objective>
Fix the hire-a-lawyer flow so dealId persists through the full navigation chain (deal page -> lawyer directory -> lawyer profile -> hire action) and fix the LegalBanner button text contrast.

Purpose: UAT Tests 3 and 4 failed. Test 3: "Find a Lawyer" text not readable on gold background. Test 4: dealId is lost during navigation, causing the hire action to always show "Select a deal first" toast instead of calling the hireLawyer Cloud Function. This blocks Tests 5-10 and 14 (all dependent on a successful hire).

Output: Working end-to-end hire flow where a user clicks "Find a Lawyer" on a deal page, browses the directory, clicks a lawyer card, and successfully hires them -- with dealId preserved at every step.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-legal-consulting/05-UAT.md
@.planning/phases/05-legal-consulting/05-03-SUMMARY.md
@.planning/phases/05-legal-consulting/05-04-SUMMARY.md

<interfaces>
<!-- From src/presentation/hooks/legal/useLegalActions.js -->
export function useLegalActions(): {
  hireLawyer: (dealId: string, lawyerId: string) => Promise<Object|null>,
  respondToHireRequest: (engagementId: string, action: 'accept'|'decline') => Promise<Object|null>,
  closeLegalEngagement: (engagementId: string) => Promise<Object|null>,
  submitReview: (engagementId: string, rating: number, comment: string) => Promise<Object|null>,
  loading: boolean,
  error: string|null
}

<!-- From src/domain/entities/LegalEngagement.js (key fields) -->
LegalEngagement {
  id, clientId, lawyerId, dealId, participants, dealProductName,
  clientDisplayName, status, createdAt, updatedAt
}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix LegalBanner text contrast and wire dealId through /lawyers page and LawyerDirectory</name>
  <files>
    src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx,
    src/app/(main)/lawyers/page.jsx,
    src/presentation/components/features/legal/LawyerDirectory/LawyerDirectory.jsx,
    src/presentation/components/features/legal/LawyerDirectory/LawyerCard.jsx
  </files>
  <action>
1. **LegalBanner.jsx** (line 142): In the PromotionalBanner component, change `text-[#0F1C2E]` to `text-black` on the "Find a Lawyer" Link element. This fixes the contrast issue on the gold `bg-[#FFD700]` background. Only this one class change is needed -- the rest of the banner is fine.

2. **src/app/(main)/lawyers/page.jsx**: Add `useSearchParams` import from `next/navigation`. Extract `dealId` from search params: `const searchParams = useSearchParams(); const dealId = searchParams.get('dealId');`. Pass dealId as a prop to LawyerDirectory: `<LawyerDirectory dealId={dealId} />`. Wrap the component body in a Suspense boundary (required by Next.js app router for useSearchParams in a page component -- same pattern used in /deals/new). The Suspense fallback can be the existing loading spinner JSX.

3. **LawyerDirectory.jsx**: Accept `dealId` prop in the function signature: `export function LawyerDirectory({ dealId })`. Pass `dealId` to each LawyerCard: `<LawyerCard key={lawyer.id} lawyer={lawyer} dealId={dealId} />`. No other changes needed -- the directory filtering logic is unaffected.

4. **LawyerCard.jsx**: Accept `dealId` prop in the function signature: `export function LawyerCard({ lawyer, dealId })`. Update the Link href to forward dealId when present: `href={dealId ? \`/profile/\${id}?dealId=\${dealId}\` : \`/profile/\${id}\`}`. This ensures the dealId query param travels from directory to profile page.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - LegalBanner "Find a Lawyer" button uses text-black for clear readability on gold background
    - /lawyers page reads dealId from URL searchParams and passes to LawyerDirectory
    - LawyerDirectory forwards dealId to each LawyerCard
    - LawyerCard appends ?dealId= to profile link when dealId is present
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire LawyerProfileContent to read dealId from URL and call hireLawyer Cloud Function</name>
  <files>
    src/presentation/components/features/legal/LawyerProfile/LawyerProfileContent.jsx
  </files>
  <action>
Replace the hardcoded `handleHireClick` stub in LawyerProfileContent with a working implementation:

1. Add imports: `import { useSearchParams } from 'next/navigation';` and `import { useLegalActions } from '@/presentation/hooks/legal/useLegalActions';`

2. Inside the `LawyerProfileContent` component function, add:
   - `const searchParams = useSearchParams();`
   - `const dealId = searchParams.get('dealId');`
   - `const { hireLawyer, loading: hireLoading } = useLegalActions();`

3. Replace the `handleHireClick` function body:
   ```
   const handleHireClick = async () => {
     if (!dealId) {
       toast('Select a deal first to hire this lawyer', {
         icon: 'info',
         style: { background: '#1c304a', color: '#fff', border: '1px solid rgba(255,255,255,0.1)' },
       });
       return;
     }
     await hireLawyer(dealId, id);
   };
   ```
   This preserves the "no dealId" fallback toast (for users who navigate to the profile directly without a deal context) but actually calls the Cloud Function when dealId IS present.

4. Update the "Hire This Lawyer" button to show loading state:
   - Add `disabled={hireLoading}` to the button
   - Optionally change button text to "Hiring..." when `hireLoading` is true
   - Keep the existing styling classes

5. Do NOT add a Suspense boundary here -- this component is rendered inside the profile page which already has client-side rendering. useSearchParams in a 'use client' component that is NOT a page-level component does not require Suspense (it is the page.jsx route component that needs it, and the profile page already handles this).
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - LawyerProfileContent reads dealId from URL searchParams
    - When dealId is present, "Hire This Lawyer" calls hireLawyer(dealId, lawyerId) Cloud Function
    - When dealId is absent (direct navigation), the informational toast still appears
    - Button shows loading/disabled state during hire request
    - The full flow works: deal page -> Find a Lawyer -> directory -> card -> profile -> Hire This Lawyer -> engagement created
  </done>
</task>

</tasks>

<verification>
- LegalBanner "Find a Lawyer" text is black and clearly readable on gold background
- Clicking "Find a Lawyer" on deal page navigates to /lawyers?dealId={dealId}
- Lawyer cards in directory link to /profile/{lawyerId}?dealId={dealId}
- "Hire This Lawyer" on profile page calls hireLawyer CF and creates a pending engagement
- Navigating to a lawyer profile directly (no dealId) shows the informational toast on hire click
- Build succeeds with no errors
</verification>

<success_criteria>
UAT Test 3 passes: "Find a Lawyer" button text is readable (black on gold).
UAT Test 4 passes: dealId persists through navigation, hire action creates an engagement.
UAT Tests 5-10 and 14 are unblocked (depend on successful hire from Test 4).
</success_criteria>

<output>
After completion, create `.planning/phases/05-legal-consulting/05-07-SUMMARY.md`
</output>
