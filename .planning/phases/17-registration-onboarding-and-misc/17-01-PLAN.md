---
phase: 17-registration-onboarding-and-misc
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(auth)/register/page.jsx
  - src/core/constants/companyTypes.js
  - src/core/constants/countries.js
  - src/core/validation/registerSchema.js
  - src/presentation/components/features/auth/RegisterForm/RegisterForm.jsx
  - src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx
  - functions/index.js
autonomous: true
requirements: [REG-01, REG-02, REG-03, REG-04]

must_haves:
  truths:
    - "Registration form shows a Company Type dropdown with Trade/Logistics/Insurance options styled with SearchableSelect"
    - "Selecting Logistics or Insurance company type auto-assigns the corresponding provider role at registration"
    - "Phone input auto-fills country dial code when country is selected, user can override"
    - "Register page deploys on Vercel without SSR crash"
  artifacts:
    - path: "src/core/constants/companyTypes.js"
      provides: "COMPANY_TYPES options array and COMPANY_TYPE_TO_ROLE mapping"
      exports: ["COMPANY_TYPES", "COMPANY_TYPE_TO_ROLE"]
    - path: "src/core/constants/countries.js"
      provides: "COUNTRY_PHONE_CODES map added to existing countries constant file"
      contains: "COUNTRY_PHONE_CODES"
    - path: "src/app/(auth)/register/page.jsx"
      provides: "Dynamic import of RegisterForm with ssr:false"
      contains: "dynamic"
    - path: "functions/index.js"
      provides: "setRoleClaimOnRegistration callable CF for self-registering providers"
      contains: "setRoleClaimOnRegistration"
  key_links:
    - from: "src/presentation/components/features/auth/RegisterForm/RegisterForm.jsx"
      to: "src/core/constants/companyTypes.js"
      via: "import COMPANY_TYPE_TO_ROLE for role derivation in onSubmit"
      pattern: "COMPANY_TYPE_TO_ROLE\\[.*companyType"
    - from: "src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx"
      to: "src/core/constants/countries.js"
      via: "import COUNTRY_PHONE_CODES for phone prefix auto-fill"
      pattern: "COUNTRY_PHONE_CODES\\["
    - from: "src/presentation/components/features/auth/RegisterForm/RegisterForm.jsx"
      to: "functions/index.js"
      via: "httpsCallable('setRoleClaimOnRegistration') after registration for provider roles"
      pattern: "setRoleClaimOnRegistration"
---

<objective>
Add company type selection, phone country code auto-fill, and auto-role assignment to the registration form, and fix the Vercel preview crash.

Purpose: Enable providers (logistics/insurance) to self-register with correct role assignment, improve phone input UX with country-based prefix, and unblock Vercel deployments.
Output: Updated registration form with 2 new fields, a new Cloud Function for provider role claims, and SSR-safe register page.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-registration-onboarding-and-misc/17-CONTEXT.md
@.planning/phases/17-registration-onboarding-and-misc/17-RESEARCH.md

@src/app/(auth)/register/page.jsx
@src/presentation/components/features/auth/RegisterForm/RegisterForm.jsx
@src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx
@src/core/constants/countries.js
@src/core/constants/roles.js
@src/core/validation/registerSchema.js
@src/domain/usecases/auth/RegisterUseCase.js
@src/presentation/hooks/auth/useRegister.js
@functions/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create constants, extend schema, fix Vercel crash</name>
  <files>src/core/constants/companyTypes.js, src/core/constants/countries.js, src/core/validation/registerSchema.js, src/app/(auth)/register/page.jsx</files>
  <action>
1. Create `src/core/constants/companyTypes.js` with:
   - `COMPANY_TYPES` array: `[{ value: 'trade', label: 'Trade Company' }, { value: 'logistics', label: 'Logistics Company' }, { value: 'insurance', label: 'Insurance Company' }]`
   - `COMPANY_TYPE_TO_ROLE` map: `{ trade: 'member', logistics: 'logistics_provider', insurance: 'insurance_provider' }`

2. Add `COUNTRY_PHONE_CODES` map to `src/core/constants/countries.js` — a static object mapping ISO2 country codes to dial codes (e.g., `{ TR: '+90', US: '+1', GB: '+44', DE: '+49', ... }`). Cover all countries in the existing `COUNTRIES` array. Export it as a named export alongside the existing `COUNTRIES`.

3. Update `src/core/validation/registerSchema.js`:
   - Add `companyType` field with `z.enum(['trade', 'logistics', 'insurance'], { required_error: 'Please select a company type' })`
   - Keep existing phone validation (libphonenumber-js `isValidPhoneNumber`) — the phone field will receive the combined `+prefix number` string which is already valid E.164 format

4. Fix Vercel crash in `src/app/(auth)/register/page.jsx`:
   - Replace direct `import { RegisterForm }` with `next/dynamic` import using `ssr: false`
   - Use `dynamic(() => import(...).then(m => ({ default: m.RegisterForm })), { ssr: false, loading: () => <spinner> })`
   - Keep the existing Suspense boundary for useSearchParams
   - The spinner should match existing style: `<div className="animate-spin rounded-full h-12 w-12 border-t-2 border-b-2 border-[#FFD700]"></div>`
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>companyTypes.js exports COMPANY_TYPES and COMPANY_TYPE_TO_ROLE; countries.js exports COUNTRY_PHONE_CODES; registerSchema includes companyType enum; register/page.jsx uses dynamic import with ssr:false</done>
</task>

<task type="auto">
  <name>Task 2: Wire registration form fields, auto-role, and Cloud Function</name>
  <files>src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx, src/presentation/components/features/auth/RegisterForm/RegisterForm.jsx, functions/index.js</files>
  <action>
1. Update `RegisterFormFields.jsx`:
   - Import `COMPANY_TYPES` from `@/core/constants/companyTypes` and `COUNTRY_PHONE_CODES` from `@/core/constants/countries`
   - Add Company Type field in the Company Information section (above companyCategory). Use the existing `SearchableSelect` component (already imported for country and companyCategory). Wire it with `watch('companyType')` and `setValue('companyType', value, { shouldValidate: true })` — same pattern as the existing companyCategory field.
   - Modify the phone input to show a country code prefix segment: When `country` is selected, compute `phonePrefix = COUNTRY_PHONE_CODES[country] || ''`. Render the phone input as a compound field: a read-only prefix badge `[+90]` to the left of the text input. The prefix badge should have `bg-[#1A283B] border border-[#2A3B52] rounded-l-xl px-3 text-gray-400 text-sm flex items-center` styling. The text input gets `rounded-l-none` (rounded right only). When user types a phone number, the `onChange` should combine prefix + space + local number and set it via `setValue('phone', combined)`. If user manually types a full number starting with +, allow override (do not force prefix).

2. Update `RegisterForm.jsx` (onSubmit handler):
   - Import `COMPANY_TYPE_TO_ROLE` from `@/core/constants/companyTypes`
   - In `onSubmit`, derive `role` from `data.companyType`: `const role = COMPANY_TYPE_TO_ROLE[data.companyType] || 'member'`
   - Pass `role` and `companyType` into the `registerUser()` call alongside existing fields
   - After successful registration, if role is NOT 'member' (i.e., provider self-registering), call the `setRoleClaimOnRegistration` Cloud Function via `httpsCallable(functions, 'setRoleClaimOnRegistration')({ role })`. Then force token refresh: `await auth.currentUser.getIdToken(true)`. This ensures the provider's custom claims are set before they navigate to any protected route.
   - Wrap the CF call in try/catch — if it fails, log error but don't block registration (the user is created, claims can be set later by admin).

3. Add `setRoleClaimOnRegistration` callable CF in `functions/index.js`:
   - Accepts `{ role }` from authenticated caller
   - Guards: caller must be authenticated (`request.auth` required), role must be one of `['logistics_provider', 'insurance_provider']` (members don't need this — they get default claims), and the caller's UID must match the Firestore user doc's UID (prevents one user setting another's claims)
   - Reads the Firestore user doc `users/{uid}` to verify the `role` field matches the requested role — prevents claim escalation
   - Calls `admin.auth().setCustomUserClaims(uid, { role })`
   - Returns `{ success: true }`
   - This follows the same pattern as the existing `setUserRole` CF but is callable by the registering user themselves (not admin-only)
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Registration form shows Company Type dropdown using SearchableSelect, phone input has country code prefix badge, selecting Logistics/Insurance triggers setRoleClaimOnRegistration CF after registration, provider custom claims are set correctly</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds without errors
- Register page renders without SSR crash (no window/document errors)
- Company Type dropdown appears in Company Information section with 3 options
- Phone input shows prefix badge matching selected country
- RegisterUseCase receives role derived from companyType
</verification>

<success_criteria>
- Company Type field renders with SearchableSelect, styled consistently with other dropdowns
- Selecting "Logistics Company" results in role=logistics_provider in Firestore user doc and custom claims
- Phone prefix auto-fills from country (e.g., Turkey shows +90)
- Register page loads on Vercel preview without crash
</success_criteria>

<output>
After completion, create `.planning/phases/17-registration-onboarding-and-misc/17-01-SUMMARY.md`
</output>
