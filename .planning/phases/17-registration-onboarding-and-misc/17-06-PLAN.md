---
phase: 17-registration-onboarding-and-misc
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx
  - src/core/constants/countries.js
autonomous: true
requirements: [REG-03]
gap_closure: true

must_haves:
  truths:
    - "Phone country code is an independent selectable dropdown on the left of the phone input"
    - "User can search and select any country code regardless of company country"
    - "Dropdown shows flag emoji + dial code for each country"
  artifacts:
    - path: "src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx"
      provides: "Independent phone code dropdown with SearchableSelect"
    - path: "src/core/constants/countries.js"
      provides: "COUNTRY_PHONE_CODES map and PHONE_CODE_OPTIONS array"
  key_links:
    - from: "RegisterFormFields.jsx"
      to: "countries.js"
      via: "import PHONE_CODE_OPTIONS"
      pattern: "PHONE_CODE_OPTIONS"
---

<objective>
Replace the auto-filled read-only phone country code badge with an independent selectable dropdown on the left side of the phone input (like HubSpot forms). Users may want a different country code than their company country.

Purpose: Close GAP-2 from UAT — phone code should not be locked to company country.
Output: Independent phone code dropdown with flag + code display, searchable.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-registration-onboarding-and-misc/17-UAT.md
@src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx
@src/core/constants/countries.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build phone code options data and independent dropdown</name>
  <files>src/core/constants/countries.js, src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx</files>
  <action>
1. In `src/core/constants/countries.js`, add a pre-built `PHONE_CODE_OPTIONS` array alongside the existing `COUNTRY_PHONE_CODES` map. Each entry should have:
   - `value`: the ISO2 country code (e.g., 'TR')
   - `label`: flag emoji + country name + dial code (e.g., '🇹🇷 Turkey (+90)')
   - `dialCode`: the dial code string (e.g., '+90')

   Build this from the existing `COUNTRIES` array and `COUNTRY_PHONE_CODES` map. Use a helper to convert ISO2 to flag emoji (each letter maps to regional indicator symbol: char code 0x1F1E6 + (letter code - 65)).

   Sort PHONE_CODE_OPTIONS alphabetically by country name for easy searching.

2. In `RegisterFormFields.jsx`, replace the current phone prefix implementation:

   CURRENT behavior (remove):
   - `const phonePrefix = country ? (COUNTRY_PHONE_CODES[country] || '') : '';` — derives from company country
   - Read-only `<span>` badge showing the prefix
   - `handlePhoneChange` that auto-prepends the country-derived prefix

   NEW behavior (implement):
   - Add a new local state: `const [selectedPhoneCountry, setSelectedPhoneCountry] = useState('')`
   - Initialize `selectedPhoneCountry` from the company `country` field when it changes (as a default), BUT allow independent override. Use a `useEffect` that sets `selectedPhoneCountry` to `country` ONLY if `selectedPhoneCountry` is still empty (first time).
   - Render a `SearchableSelect` component on the LEFT side of the phone input:
     - `options={PHONE_CODE_OPTIONS}` (imported from countries.js)
     - `value={selectedPhoneCountry}`
     - `onChange={(val) => setSelectedPhoneCountry(val)}`
     - `placeholder="Code"`
     - Style: narrower width, same dark styling as other dropdowns, rounded-left corners
   - The SearchableSelect should display the selected value as just the flag + dial code (e.g., "🇹🇷 +90") when collapsed, showing the full "🇹🇷 Turkey (+90)" in the dropdown list.
   - To achieve the compact display, use a custom `renderValue` approach: after selection, show a compact label. One approach: use `SearchableSelect` as-is but with a wrapper that overrides the displayed text. If `SearchableSelect` does not support `renderValue`, an alternative is to place a small overlay `<span>` showing just the flag+code over the select trigger when a value is selected.
   - The phone input field sits to the RIGHT of this dropdown, with `rounded-l-none` on the input and `rounded-r-none` on the select wrapper to form a compound field.
   - `handlePhoneChange` should combine the selected dial code + local number into the `phone` form field value:
     ```
     const dialCode = COUNTRY_PHONE_CODES[selectedPhoneCountry] || '';
     if (raw.startsWith('+')) {
       setValue('phone', raw, { shouldValidate: false });
     } else if (dialCode) {
       setValue('phone', `${dialCode} ${raw}`, { shouldValidate: false });
     } else {
       setValue('phone', raw, { shouldValidate: false });
     }
     ```
   - Import `PHONE_CODE_OPTIONS` from countries.js (keep `COUNTRY_PHONE_CODES` import for dial code lookup).

3. Styling details:
   - The dropdown container should be approximately 110-120px wide (enough for flag + code like "🇹🇷 +90")
   - The phone input takes the remaining width via `flex-1`
   - Both elements share the same height and border color
   - The dropdown border-right should be removed or shared with the input border-left to avoid a double border
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -n "PHONE_CODE_OPTIONS\|selectedPhoneCountry\|SearchableSelect" src/presentation/components/features/auth/RegisterForm/RegisterFormFields.jsx | head -10; echo "---"; grep "PHONE_CODE_OPTIONS" src/core/constants/countries.js | head -3; echo "---"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Phone country code is an independent searchable dropdown on the left of the phone input. User can select any country code regardless of their company country selection. Default pre-fills from company country but can be changed independently. Flag emoji + dial code shown in compact view.</done>
</task>

</tasks>

<verification>
- Registration form shows independent phone code dropdown on left side of phone input
- Selecting a company country pre-fills the phone code dropdown (as default) but does not lock it
- User can search and select a different country code from the dropdown
- Combined phone value (dial code + local number) passes existing libphonenumber-js validation
- Build succeeds
</verification>

<success_criteria>
- Phone country code dropdown is independent and selectable
- Flag + dial code displayed in dropdown
- User can override the default country code
- Form validation still works with the combined phone value
</success_criteria>

<output>
After completion, create `.planning/phases/17-registration-onboarding-and-misc/17-06-SUMMARY.md`
</output>
