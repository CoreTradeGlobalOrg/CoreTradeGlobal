---
phase: 17-registration-onboarding-and-misc
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/page.js
  - src/presentation/components/features/onboarding/ProfileCompletionCard/ProfileCompletionCard.jsx
autonomous: true
requirements: [REG-06]
gap_closure: true

must_haves:
  truths:
    - "Profile completion card is visible on homepage in the right top corner below the marquee ticker"
    - "Card is a checklist with strikethrough + green check for completed items and empty circle for incomplete"
    - "Skip button dismisses per session via sessionStorage, card reappears after logout/login"
    - "Card does NOT permanently hide at 100% — always shows until user skips per session"
    - "Complete Profile gold button navigates to profile/settings"
  artifacts:
    - path: "src/app/(main)/page.js"
      provides: "ProfileCompletionCard positioned in right top corner below ticker"
    - path: "src/presentation/components/features/onboarding/ProfileCompletionCard/ProfileCompletionCard.jsx"
      provides: "Updated card with correct behavior (no permanent hide at 100%)"
  key_links:
    - from: "page.js"
      to: "ProfileCompletionCard.jsx"
      via: "dynamic import"
      pattern: "ProfileCompletionCard"
---

<objective>
Fix the ProfileCompletionCard position and behavior per UAT feedback. Move it from above the hero section to the RIGHT TOP corner below the marquee ticker. Change behavior: do not permanently hide at 100% — always show until user skips per session.

Purpose: Close GAP-4 from UAT — card is in wrong position and has wrong hide behavior.
Output: Correctly positioned and behaving profile completion checklist card.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-registration-onboarding-and-misc/17-UAT.md
@src/app/(main)/page.js
@src/presentation/components/features/onboarding/ProfileCompletionCard/ProfileCompletionCard.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reposition ProfileCompletionCard and fix hide behavior</name>
  <files>src/app/(main)/page.js, src/presentation/components/features/onboarding/ProfileCompletionCard/ProfileCompletionCard.jsx</files>
  <action>
1. In `ProfileCompletionCard.jsx`, fix the hide behavior:

   CURRENT (wrong): `if (percent === 100) return null;` — permanently hides at 100%.
   NEW (correct): REMOVE the `if (percent === 100) return null;` line. The card should ALWAYS render regardless of completion percentage. It only hides when the user clicks Skip/dismiss for the current session (sessionStorage). After logout/login (new session), it reappears.

   At 100% completion, the card should still show but with a congratulatory message or all items checked — the user can still dismiss it per session. The key point: the card is NOT auto-hidden based on completion percentage.

   Also rename the "Complete Profile" button text. Keep it as "Complete Profile" when < 100%. At 100%, change it to "View Profile" (since there's nothing left to complete). The button should link to the settings/profile page.

2. In `src/app/(main)/page.js`, reposition the ProfileCompletionCard:

   CURRENT (wrong position): Rendered in a `<div className="max-w-sm mx-auto px-4 pt-4">` above the HeroSection — this places it centered above the hero.

   NEW (correct position): Move the ProfileCompletionCard to render as a fixed/absolute positioned element in the RIGHT TOP corner of the page, below where the marquee ticker sits. Implementation:
   - Remove the current `<div className="max-w-sm mx-auto px-4 pt-4">` wrapper
   - Instead, render the ProfileCompletionCard inside a positioned container:
     ```jsx
     {!loading && user && (
       <div className="fixed top-[calc(var(--ticker-height,40px)+var(--navbar-height,64px)+12px)] right-4 z-[50] w-[320px]">
         <ProfileCompletionCard user={user} />
       </div>
     )}
     ```
   - The card should be `fixed` position so it stays visible as user scrolls
   - `top` value should account for the ticker height + navbar height + some gap (use CSS custom properties if available, or hardcoded values based on the existing layout: ticker ~40px + navbar ~64px = 104px + 12px gap = 116px top)
   - `right-4` for right-side positioning
   - `z-[50]` to stay above page content but below modals/overlays
   - `w-[320px]` for a fixed card width
   - On mobile (< lg breakpoint), hide the fixed card to avoid blocking content. Add `hidden lg:block` to the container.

3. In `ProfileCompletionCard.jsx`, update the dismiss button label from the X icon to "Skip" text button to match the spec ("Skip button dismisses per session"):
   - Keep the X icon but also add the word "Skip" next to it, OR replace with a text "Skip" button in the card footer area.
   - The dismiss still uses sessionStorage — same key pattern `profileCardDismissed_${user.uid}`.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -n "fixed\|right-4\|top-\|percent === 100" src/presentation/components/features/onboarding/ProfileCompletionCard/ProfileCompletionCard.jsx src/app/\(main\)/page.js 2>/dev/null; echo "---"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ProfileCompletionCard is fixed-positioned in the right top corner below the ticker/navbar. Card does NOT auto-hide at 100% — only hides on session dismiss. Skip button dismisses per session. Card reappears on next login.</done>
</task>

</tasks>

<verification>
- Card appears in right top corner of the homepage for authenticated users
- Card shows checklist with green checks and strikethrough for completed items
- Card does NOT disappear at 100% completion
- Skip/dismiss hides card for current session only
- Card reappears after logout/login (new browser session)
- Card is hidden on mobile to avoid blocking content
- Build succeeds
</verification>

<success_criteria>
- Profile completion card positioned in right top corner below marquee ticker
- No permanent hide at 100% — always shows until session dismiss
- Skip button works per session via sessionStorage
- Card reappears on next visit/login
</success_criteria>

<output>
After completion, create `.planning/phases/17-registration-onboarding-and-misc/17-07-SUMMARY.md`
</output>
