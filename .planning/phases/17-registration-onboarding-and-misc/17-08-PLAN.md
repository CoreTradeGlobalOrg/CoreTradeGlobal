---
phase: 17-registration-onboarding-and-misc
plan: "08"
type: execute
wave: 2
depends_on: ["07"]
files_modified:
  - src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.jsx
  - src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.css
  - src/app/(main)/page.js
  - src/app/(main)/about/page.jsx
  - src/app/(main)/faq/page.jsx
  - src/app/(main)/settings/page.jsx
autonomous: true
requirements: [REG-02, REG-05]
gap_closure: true

must_haves:
  truths:
    - "A '?' FAB button on the LEFT corner of the screen relaunches the tour anytime"
    - "Tour launch buttons exist on FAQ, About Us, and Settings pages"
    - "Three-part guided tour with Turkish content: Profile Creation (4 steps), Product Upload (3 steps), RFQ Creation (3 steps)"
    - "Each step has Next and Skip buttons, last step of each part has Finish"
    - "Tour transitions between parts with specific Turkish transition text"
  artifacts:
    - path: "src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.jsx"
      provides: "Redesigned 3-part tour with Turkish content and ? FAB button"
    - path: "src/app/(main)/about/page.jsx"
      provides: "Tour launch button on About Us page"
    - path: "src/app/(main)/faq/page.jsx"
      provides: "Tour launch button on FAQ page"
    - path: "src/app/(main)/settings/page.jsx"
      provides: "Tour launch button on Settings page"
  key_links:
    - from: "page.js (homepage)"
      to: "OnboardingTour.jsx"
      via: "dynamic import + showTour state"
      pattern: "OnboardingTour"
    - from: "about/faq/settings pages"
      to: "OnboardingTour.jsx"
      via: "dynamic import + tour launch button"
      pattern: "OnboardingTour"
---

<objective>
Complete redesign of the onboarding tour to match user specification: 3-part guided tour with specific Turkish content, a "?" FAB button on the left corner to relaunch anytime, and tour launch buttons on FAQ, About Us, and Settings pages.

Purpose: Close GAP-3 from UAT — current tour implementation does not match the detailed specification.
Output: Redesigned 3-part tour with Turkish content, relaunch capability, and multi-page tour buttons.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-registration-onboarding-and-misc/17-UAT.md
@.planning/phases/17-registration-onboarding-and-misc/17-07-SUMMARY.md
@src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.jsx
@src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.css
@src/app/(main)/page.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign OnboardingTour with 3-part Turkish content and ? FAB</name>
  <files>src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.jsx, src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.css</files>
  <action>
Completely rewrite `OnboardingTour.jsx` to implement the 3-part guided tour with Turkish content.

**Tour Structure — 3 Parts:**

**Part 1: Profile Creation Guide (Profil Oluşturma Rehberi) — 4 steps**

Initial popup (before steps):
- Title: "CoreTG Kullanma Rehberine Hoşgeldiniz"
- Description: "Bu rehber, platformumuzu en verimli şekilde kullanmanız için size adım adım yol gösterecek. Haydi başlayalım!"
- Single button: "Başla" (Start)

Step 1/4:
- Title: "Şirket Profilinizi Tamamlayın"
- Description: "Kuruluş yılı, faaliyet sektörü ve ihracat pazarlarınız gibi temel bilgileri girerek profilinizi oluşturun."
- Target: profile/settings related nav link or section
- Buttons: (Sonraki) (Atla)

Step 2/4:
- Title: "Güven Oluşturun"
- Description: "Sahip olduğunuz sertifikaları (ISO, CE, vb.), üretim kapasitenizi ve kalite belgelerinizi ekleyin. Bu bilgiler, potansiyel alıcıların size güven duymasını sağlar."
- Target: profile certificates section
- Buttons: (Sonraki) (Atla)

Step 3/4:
- Title: "Görsel Prestij"
- Description: "Şirket logonuzu ve kapak fotoğrafınızı yükleyin. Profesyonel bir görünüm, ilk izleniminizi güçlendirir."
- Target: profile logo/cover section
- Buttons: (Sonraki) (Atla)

Step 4/4:
- Title: "Profilinizi Özetleyin"
- Description: "Hakkımızda bölümünü yazın ve ihracat yaptığınız pazarları belirtin. Tamamlanmış bir profil, görünürlüğünüzü artırır."
- Target: profile about section
- Buttons: (Bitir) (Atla)

**Transition between Part 1 and Part 2:**
- Centered modal popup (no spotlight):
- Text: "Profiliniz tamamlandı! Şimdi nasıl ürün ve Talep (RFQ) oluşturacağınızı göstereceğiz."
- Button: "Devam Et" (Continue)

**Part 2: Product Upload Guide (Urun Ekleme Rehberi) — 3 steps**

Step 1/3:
- Title: "CoreTG'ye Hoşgeldiniz"
- Description: "Ürünlerinizi platformumuza eklemek için 'Ürün Ekle' butonunu kullanın."
- Target: product add button or marketplace nav link (highlight the "Urun Ekle" button if visible)
- Buttons: (Sonraki) (Atla)

Step 2/3:
- Title: "Ürün Bilgilerini Doldurun"
- Description: "Ürün adı, açıklaması, kategorisi, fiyatı ve görsellerini ekleyin. Detaylı bilgi, alıcıların ilgisini çeker."
- Target: centered (no specific element — informational step)
- Buttons: (Sonraki) (Atla)

Step 3/3:
- Title: "Ürününüzü Yayınlayın"
- Description: "Tüm bilgileri kontrol ettikten sonra 'Yayınla' butonuna tıklayarak ürününüzü görünür hale getirin."
- Target: centered
- Buttons: (Bitir) (Atla)

**Transition between Part 2 and Part 3:**
- Centered modal popup:
- Text: "Harika! Şimdi bir RFQ (Talep Teklifi) nasıl oluşturacağınızı görelim."
- Button: "Devam Et"

**Part 3: RFQ Creation Guide (RFQ Oluşturma Rehberi) — 3 steps**

Step 1/3:
- Title: "İhtiyacınızı Yayınlayın"
- Description: "RFQ oluşturarak tedarikçilerden teklif alın. 'RFQ Oluştur' butonuna tıklayın."
- Target: RFQ/requests nav link or button
- Buttons: (Sonraki) (Atla)

Step 2/3:
- Title: "Miktar ve Teslimat Detayları"
- Description: "İhtiyacınız olan ürün miktarını, teslimat tarihini ve konumunu belirtin."
- Target: centered
- Buttons: (Sonraki) (Atla)

Step 3/3:
- Title: "Talebinizi Yayınlayın"
- Description: "Tüm detayları kontrol edip 'Yayınla' butonuyla talebinizi tedarikçilere ulaştırın."
- Target: centered
- Buttons: (Bitir) (Atla)

**Implementation details:**

1. Define all steps as a flat array with a `part` field (1, 2, or 3) and a `type` field ('intro' | 'step' | 'transition'). This makes navigation simple — just increment an index through the flat array.

2. For 'intro' and 'transition' type entries: render a centered modal popup (no spotlight overlay). For 'step' type entries: render the spotlight + panel as currently implemented.

3. Each part's last step shows "Bitir" (Finish) instead of "Sonraki" (Next). "Atla" (Skip) on any step completes the tour and writes the Firestore flag.

4. When "Bitir" is clicked on the last step of Part 1 or Part 2, advance to the transition screen. When "Bitir" is clicked on Part 3's last step, complete the tour (write Firestore flag, call onComplete).

5. The step counter should show within each part: "Adım 1/4", "Adım 2/3", etc. (not global step count).

6. Keep the existing Portal rendering, spotlight/overlay CSS, and panel positioning logic. Just replace the step data and add the intro/transition screens.

7. **"?" FAB button:** Export a separate `TourHelpButton` component from this file:
   - Fixed position: `bottom-6 left-6` (LEFT corner, matching the spec)
   - Circular gold button with "?" text (or HelpCircle icon from lucide-react)
   - `z-[100]` to match FAB z-index level
   - On click: launches the tour (calls the `onLaunch` callback prop)
   - Only shown for authenticated users
   - Style: same gold gradient as other FAB buttons (`bg-gradient-to-r from-[#FFD700] to-[#FDB931] !text-black`)
   - Size: `w-12 h-12 rounded-full`

8. Update `OnboardingTour.css` if needed for any new styles (transition screens, intro modal).
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -c "part:" src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.jsx; echo "---"; grep "TourHelpButton\|CoreTG Kullanma\|Profiliniz tamamlandı\|Bitir\|Sonraki\|Atla" src/presentation/components/features/onboarding/OnboardingTour/OnboardingTour.jsx | head -15; echo "---"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>OnboardingTour rewritten with 3-part Turkish content (Profile 4 steps, Product 3 steps, RFQ 3 steps), intro/transition screens, Next/Skip/Finish buttons per spec. TourHelpButton component exported for "?" FAB.</done>
</task>

<task type="auto">
  <name>Task 2: Wire tour launch on homepage, FAQ, About Us, and Settings pages</name>
  <files>src/app/(main)/page.js, src/app/(main)/about/page.jsx, src/app/(main)/faq/page.jsx, src/app/(main)/settings/page.jsx</files>
  <action>
IMPORTANT: Plan 17-07 modifies page.js before this plan runs. Read page.js at execution time to pick up those changes (ProfileCompletionCard repositioning). Do NOT overwrite Plan 07's changes.

1. In `src/app/(main)/page.js` (homepage):
   - Import `TourHelpButton` from the OnboardingTour module (dynamic import, ssr:false)
   - Add a `showTourManual` state: `const [showTourManual, setShowTourManual] = useState(false)`
   - Render the `TourHelpButton` for authenticated users: `{!loading && user && <TourHelpButton onLaunch={() => setShowTourManual(true)} />}`
   - Update the OnboardingTour rendering condition: show tour when either `showTour` (first login auto) OR `showTourManual` (manual relaunch via ? button)
   - On tour complete, reset both: `onComplete={() => setShowTourManual(false)}`
   - The auto-start logic remains: `showTour = !loading && user && !user.onboardingTourCompleted`

2. In `src/app/(main)/about/page.jsx`:
   - Add a "Rehberi Başlat" (Start Guide) button somewhere visible on the page (e.g., in the header area or as a floating element)
   - Import `OnboardingTour` and `TourHelpButton` dynamically (ssr:false)
   - Add tour state management: `const [showTour, setShowTour] = useState(false)`
   - Use `useAuth()` to get the user — only show the button for authenticated users
   - Render: `{showTour && <OnboardingTour user={user} onComplete={() => setShowTour(false)} />}`
   - Also render the `TourHelpButton` for authenticated users: `{user && <TourHelpButton onLaunch={() => setShowTour(true)} />}`

3. In `src/app/(main)/faq/page.jsx`:
   - Same pattern as About Us page: add TourHelpButton + OnboardingTour with tour state
   - Import useAuth, dynamic imports for tour components
   - Render TourHelpButton for authenticated users + OnboardingTour when active

4. In `src/app/(main)/settings/page.jsx`:
   - Same pattern: add TourHelpButton + OnboardingTour with tour state
   - Settings page already has auth context — reuse it

For pages 2-4, if the page is currently a Server Component (no 'use client'), you have two options:
   a) Convert to client component (add 'use client') if it is lightweight
   b) Extract a `TourLauncher` client component wrapper and render it within the server page

Choose the approach that best fits each page's current structure. If the page already uses 'use client', just add the tour state and imports directly.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -l "TourHelpButton\|OnboardingTour" src/app/\(main\)/page.js src/app/\(main\)/about/page.jsx src/app/\(main\)/faq/page.jsx src/app/\(main\)/settings/page.jsx 2>/dev/null; echo "---"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>"?" FAB button appears on homepage, FAQ, About Us, and Settings pages for authenticated users. Clicking it launches the 3-part onboarding tour. Tour auto-starts on first login (existing behavior preserved). Build succeeds.</done>
</task>

</tasks>

<verification>
- Tour shows 3 parts with Turkish content (Profile 4 steps, Product 3 steps, RFQ 3 steps)
- Intro screen shows "CoreTG Kullanma Rehberine Hoşgeldiniz" before Part 1
- Transition screens appear between parts
- Each step has Sonraki (Next) and Atla (Skip) buttons
- Last step of each part shows Bitir (Finish) instead of Sonraki
- "?" FAB button visible on homepage, FAQ, About, and Settings pages
- Clicking "?" relaunches the tour
- Tour completion writes Firestore flag (existing behavior preserved)
- Build succeeds
</verification>

<success_criteria>
- 3-part guided tour with all Turkish text content from the specification
- "?" FAB button on LEFT corner of screen on 4 pages
- Tour launch buttons functional on FAQ, About Us, and Settings pages
- Tour auto-starts on first login for new users
- Tour can be relaunched anytime via the "?" button
</success_criteria>

<output>
After completion, create `.planning/phases/17-registration-onboarding-and-misc/17-08-SUMMARY.md`
</output>