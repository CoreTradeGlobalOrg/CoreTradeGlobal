---
phase: 17-registration-onboarding-and-misc
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
  - src/presentation/components/common/MessagesWidget/MessagesWidget.css
  - src/app/(main)/layout.jsx
  - src/presentation/components/common/ZohoSalesIQ/ZohoSalesIQButton.jsx
autonomous: true
requirements: [REG-07]
user_setup:
  - service: zoho-salesiq
    why: "Live chat support widget"
    env_vars:
      - name: NEXT_PUBLIC_ZOHO_WIDGET_KEY
        source: "Zoho SalesIQ Dashboard -> Settings -> Brands -> Installation -> Widget Code"
    dashboard_config:
      - task: "Create Zoho SalesIQ account and obtain widget key"
        location: "https://www.zoho.com/salesiq/"

must_haves:
  truths:
    - "FAB messaging widget has two tabs: Messages and Support"
    - "Support tab shows Zoho SalesIQ chat when widget key is configured"
    - "Public pages show a standalone Support chat button when no FAB exists"
    - "Zoho default floating button is suppressed globally"
  artifacts:
    - path: "src/presentation/components/common/MessagesWidget/MessagesWidget.jsx"
      provides: "Two-tab FAB widget with Messages and Support tabs"
      contains: "Support"
    - path: "src/presentation/components/common/ZohoSalesIQ/ZohoSalesIQButton.jsx"
      provides: "Standalone Zoho chat button for public pages"
      exports: ["ZohoSalesIQButton"]
  key_links:
    - from: "src/app/(main)/layout.jsx"
      to: "Zoho SalesIQ script"
      via: "next/script with strategy=lazyOnload"
      pattern: "zoho-salesiq"
    - from: "src/presentation/components/common/MessagesWidget/MessagesWidget.jsx"
      to: "window.$zoho.salesiq"
      via: "JS API calls to show/hide Zoho chat panel"
      pattern: "\\$zoho\\.salesiq"
---

<objective>
Integrate Zoho SalesIQ as a "Support" tab in the FAB messaging widget, and show a standalone chat button on public pages.

Purpose: Provide live chat support to all visitors (logged-in and public) via Zoho SalesIQ, integrated natively into the existing FAB widget.
Output: Two-tab FAB (Messages + Support), Zoho script embed in layout, standalone public page button.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/17-registration-onboarding-and-misc/17-CONTEXT.md
@.planning/phases/17-registration-onboarding-and-misc/17-RESEARCH.md

@src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
@src/presentation/components/common/MessagesWidget/MessagesWidget.css
@src/app/(main)/layout.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Embed Zoho SalesIQ script and add Support tab to FAB widget</name>
  <files>src/app/(main)/layout.jsx, src/presentation/components/common/MessagesWidget/MessagesWidget.jsx, src/presentation/components/common/MessagesWidget/MessagesWidget.css</files>
  <action>
1. Add Zoho SalesIQ script to `src/app/(main)/layout.jsx`:
   - Import `Script` from `next/script`
   - Add a conditional `<Script>` tag that only renders when `process.env.NEXT_PUBLIC_ZOHO_WIDGET_KEY` is set
   - Script: `id="zoho-salesiq"`, `strategy="lazyOnload"`
   - Script content initializes `window.$zoho.salesiq` with the widget key, creates the script element pointing to `https://salesiq.zohopublic.com/widget`, and in the `$zoho.salesiq.ready` callback calls `$zoho.salesiq.floatbutton.visible('hide')` to suppress the default Zoho floating button
   - Place the Script tag at the end of the body content (after CookieConsent, before closing tags)

2. Update `MessagesWidget.jsx` to add two-tab UI:
   - Add state: `const [activeTab, setActiveTab] = useState('messages')` — 'messages' or 'support'
   - Add state: `const [zohoReady, setZohoReady] = useState(false)` — tracks if Zoho script has loaded
   - Check `process.env.NEXT_PUBLIC_ZOHO_WIDGET_KEY` — if not set, do not show the Support tab at all (single-tab mode, no tab bar)
   - When widget key IS set, show a tab bar at the top of the expanded FAB panel:
     - Two tab buttons: "Messages" and "Support"
     - Active tab: gold underline border-b-2 border-[#FFD700], text-white
     - Inactive tab: text-gray-400, hover:text-gray-300
   - Messages tab: renders existing conversation list / message thread content (no changes to existing logic)
   - Support tab content:
     - On tab activation (`activeTab === 'support'`), call `window.$zoho?.salesiq?.floatwindow?.visible('show')` to open the Zoho chat window programmatically
     - On tab deactivation (switching back to Messages), call `window.$zoho?.salesiq?.floatwindow?.visible('hide')`
     - Show a centered "Connecting to support..." spinner with `text-gray-400` while `!zohoReady`
     - Set `zohoReady` by checking `window.$zoho?.salesiq` existence in a useEffect with polling (check every 500ms until ready, max 10 attempts)
     - If Zoho never loads (after 5s), show fallback text: "Support chat is currently unavailable. Please try again later."
   - Guard ALL `$zoho.salesiq.*` calls with `if (window.$zoho?.salesiq)` to prevent TypeError if script hasn't loaded

3. Update `MessagesWidget.css`:
   - Add styles for the tab bar: `.messages-widget-tabs` with flex, border-bottom
   - Tab active/inactive states
   - Ensure the tab bar does not interfere with existing widget layout when there's only one tab (Messages)
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Zoho SalesIQ script loads in layout with lazyOnload, default float button suppressed, FAB widget shows Messages/Support tabs when widget key is configured, Support tab triggers Zoho chat panel</done>
</task>

<task type="auto">
  <name>Task 2: Standalone Zoho chat button for public pages</name>
  <files>src/presentation/components/common/ZohoSalesIQ/ZohoSalesIQButton.jsx, src/app/(main)/layout.jsx</files>
  <action>
1. Create `ZohoSalesIQButton.jsx`:
   - A simple button component that shows on public pages where the FAB widget is not present
   - Only renders when `process.env.NEXT_PUBLIC_ZOHO_WIDGET_KEY` is set
   - Styled as a floating button: `fixed bottom-6 right-6 z-[100]` (below the FAB z-index of 9000+)
   - Gold-accented circle button with a chat/headset icon (use `Headphones` or `MessageCircle` from lucide-react)
   - Size: `w-14 h-14 rounded-full bg-gradient-to-r from-[#FFD700] to-[#FDB931] !text-black shadow-lg hover:shadow-[0_0_20px_rgba(255,215,0,0.3)]`
   - On click: call `window.$zoho?.salesiq?.floatwindow?.visible('show')` to open Zoho chat
   - If `$zoho.salesiq` is not available, show toast "Support chat is loading, please try again in a moment"
   - Guard with `typeof window !== 'undefined'` for SSR safety

2. Integrate into `src/app/(main)/layout.jsx`:
   - The FAB (MessagesWidget) already conditionally renders based on auth state — it only shows for authenticated users
   - Import ZohoSalesIQButton with `next/dynamic` and `ssr: false`
   - Render `<ZohoSalesIQButton />` conditionally: show it when the user is NOT authenticated (public visitor) AND the Zoho widget key is configured
   - When user IS authenticated, the FAB handles Zoho via its Support tab — no standalone button needed
   - If the layout already has auth context available, use it. If not, the ZohoSalesIQButton can be rendered unconditionally and the FAB's own z-index (higher) will overlap it — but cleaner to conditionally render based on auth state.

NOTE: The CONTEXT.md says public pages (homepage, about, FAQ) "where no FAB exists" should show standalone button. The FAB only shows for logged-in users, so rendering ZohoSalesIQButton for unauthenticated users covers this requirement.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Public pages show a standalone gold chat button that opens Zoho SalesIQ chat; authenticated users get the Support tab in the FAB instead; no duplicate chat buttons</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds
- FAB widget shows Messages/Support tabs when NEXT_PUBLIC_ZOHO_WIDGET_KEY is set
- Support tab triggers Zoho chat window
- Public pages (homepage, about, FAQ) show standalone chat button for unauthenticated visitors
- Zoho default floating button is globally suppressed
</verification>

<success_criteria>
- Two-tab FAB (Messages + Support) works for authenticated users
- Support tab gracefully handles Zoho script loading delays
- Standalone button appears for unauthenticated visitors on public pages
- No console errors when widget key is not configured (graceful degradation)
</success_criteria>

<output>
After completion, create `.planning/phases/17-registration-onboarding-and-misc/17-03-SUMMARY.md`
</output>
