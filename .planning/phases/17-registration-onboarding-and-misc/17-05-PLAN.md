---
phase: 17-registration-onboarding-and-misc
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/core/constants/companyTypes.js
  - src/app/(main)/layout.jsx
  - src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
  - src/presentation/components/common/ZohoSalesIQ/ZohoSalesIQButton.jsx
autonomous: true
requirements: [REG-01, REG-07]
gap_closure: true

must_haves:
  truths:
    - "Company type dropdown shows 'Supplier' instead of 'Trade Company'"
    - "Zoho SalesIQ code is completely removed from the codebase"
    - "Firestore rules for productUploadRequests are deployed to production"
  artifacts:
    - path: "src/core/constants/companyTypes.js"
      provides: "Updated label 'Supplier' for trade company type"
      contains: "Supplier"
    - path: "src/app/(main)/layout.jsx"
      provides: "Layout without Zoho SalesIQ script or button"
  key_links: []
---

<objective>
Fix company type label rename (Trade Company -> Supplier), remove all Zoho SalesIQ code (user wants custom AI chatbot in a future phase instead), and deploy Firestore rules to fix productUploadRequests permission error.

Purpose: Close 3 gaps from UAT (GAP-1, GAP-5, and Zoho scope change) with minimal targeted fixes.
Output: Updated label, clean codebase without Zoho, working Firestore rules.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-registration-onboarding-and-misc/17-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename company type label and remove all Zoho SalesIQ code</name>
  <files>src/core/constants/companyTypes.js, src/app/(main)/layout.jsx, src/presentation/components/common/MessagesWidget/MessagesWidget.jsx, src/presentation/components/common/ZohoSalesIQ/ZohoSalesIQButton.jsx</files>
  <action>
1. In `src/core/constants/companyTypes.js`, change the label for the `trade` value from `'Trade Company'` to `'Supplier'`. Do NOT change the value key — only the display label.

2. Remove all Zoho SalesIQ code from the codebase:

   a) `src/app/(main)/layout.jsx`:
      - Remove the `ZohoSalesIQButton` dynamic import and its `const ZohoSalesIQButton = dynamic(...)` block
      - Remove the `const ZOHO_WIDGET_KEY = process.env.NEXT_PUBLIC_ZOHO_WIDGET_KEY` line
      - Remove the `{!isAuthenticated && <ZohoSalesIQButton />}` JSX line
      - Remove the entire Zoho `<Script>` block (the `{ZOHO_WIDGET_KEY && (<Script id="zoho-salesiq" ...>...</Script>)}` block)
      - Remove the `import Script from 'next/script'` if it is no longer used by anything else in the file

   b) `src/presentation/components/common/MessagesWidget/MessagesWidget.jsx`:
      - Remove the `const ZOHO_WIDGET_KEY = process.env.NEXT_PUBLIC_ZOHO_WIDGET_KEY` line
      - Remove the `zohoReady`, `zohoFailed`, `zohoCheckRef` state and ref declarations
      - Remove the Zoho readiness polling `useEffect` (the one checking `window.$zoho?.salesiq` with attempts)
      - Remove the Zoho chat panel visibility `useEffect` (the one toggling `floatwindow.visible`)
      - Remove the Zoho hide-on-close `useEffect`
      - Remove the tab bar JSX: the `{ZOHO_WIDGET_KEY && !activeConversationId && (...)}` block containing Messages/Support tabs
      - Remove the Support tab content block: the `{ZOHO_WIDGET_KEY && activeTab === 'support' && ...}` JSX
      - Remove the `activeTab` and `setActiveTab` state since only one tab remains
      - Update the JSDoc comment at the top to remove mention of "Two tabs: Messages and Support"

   c) Delete the entire file `src/presentation/components/common/ZohoSalesIQ/ZohoSalesIQButton.jsx` and its parent directory if empty.

3. Verify no other files reference Zoho or NEXT_PUBLIC_ZOHO_WIDGET_KEY by searching the codebase. Remove any remaining references.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -r "zoho\|salesiq\|ZOHO\|ZohoSalesIQ" src/ --include="*.jsx" --include="*.js" --include="*.ts" -l; echo "---"; grep -r "Supplier" src/core/constants/companyTypes.js; echo "---"; npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Company type label shows "Supplier". All Zoho SalesIQ code removed from layout, MessagesWidget, and standalone button component. No Zoho references remain in source.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy Firestore rules to fix productUploadRequests permissions</name>
  <files></files>
  <action>
Deploy Firestore rules to production so the `productUploadRequests` collection rules (already defined in `firestore.rules`) take effect on the live Firestore instance.

Run: `firebase deploy --only firestore:rules --project core-trade-global`

IMPORTANT: Per MEMORY.md — never use `--force` on `firestore:indexes`. This task deploys ONLY rules, not indexes. This is safe.

After deployment, verify the rules are live by checking the output for a success message.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && firebase deploy --only firestore:rules --project core-trade-global 2>&1 | tail -5</automated>
  </verify>
  <done>Firestore rules deployed. productUploadRequests collection is accessible: authenticated users can create requests (own uid), admins can read/update.</done>
</task>

</tasks>

<verification>
- `grep -ri "zoho\|salesiq" src/` returns no results
- `grep "Supplier" src/core/constants/companyTypes.js` shows the updated label
- Build succeeds without errors
- Firestore rules deploy succeeds
</verification>

<success_criteria>
- "Trade Company" label replaced with "Supplier" in registration dropdown
- All Zoho SalesIQ integration code removed (layout script, FAB Support tab, standalone button)
- Firestore rules deployed — productUploadRequests permissions work in production
</success_criteria>

<output>
After completion, create `.planning/phases/17-registration-onboarding-and-misc/17-05-SUMMARY.md`
</output>
