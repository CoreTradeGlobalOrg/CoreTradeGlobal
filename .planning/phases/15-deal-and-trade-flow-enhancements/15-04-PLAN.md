---
phase: 15-deal-and-trade-flow-enhancements
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/deal/ProductHero/ProductHero.jsx
  - src/presentation/components/common/DatePicker/DatePicker.jsx
  - src/presentation/components/features/deal/DealForm/DealFormFields.jsx
  - src/presentation/components/features/deal/DealForm/DealFormSidebar.jsx
  - src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx
  - src/presentation/components/features/deal/DealSidebar/FreightEstimatorWidget.jsx
  - src/presentation/components/features/provider/QuoteFormLogistics/QuoteFormLogistics.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/CargoMarineSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/CommercialRiskSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/PremiumAdditionsSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/PoliticalRiskSection.jsx
  - src/presentation/components/features/request/SubmitQuoteDialog/SubmitQuoteDialog.jsx
autonomous: true
requirements:
  - DEAL-14
  - DEAL-15
  - DEAL-16
  - DEAL-17
  - DEAL-18

must_haves:
  truths:
    - "Deal ID is visible in the page header as 'Deal #XXXXXXXX'"
    - "Product PDF download link is visible with gold accent styling"
    - "DatePicker gold accent color is available and used on deal form date inputs"
    - "All number inputs across deal/trade pages auto-select content on focus"
    - "All Zod validation messages across deal/trade schemas are in English"
  artifacts:
    - path: "src/presentation/components/features/deal/ProductHero/ProductHero.jsx"
      provides: "Deal ID display and PDF download link"
      contains: "Deal #"
    - path: "src/presentation/components/common/DatePicker/DatePicker.jsx"
      provides: "Gold accent color in ACCENT_MAP"
      contains: "gold"
  key_links:
    - from: "ProductHero.jsx"
      to: "deal.id"
      via: "truncated uppercase formatting"
      pattern: "slice.*toUpperCase"
    - from: "DatePicker.jsx"
      to: "DealFormFields.jsx"
      via: "accentColor='gold' prop"
      pattern: "accentColor"
---

<objective>
Form input polish sweep: add Deal ID breadcrumb and PDF download to ProductHero, add gold accent to DatePicker, add onFocus auto-select to all number inputs across deal/trade pages, and audit Zod validation messages for English.

Purpose: Polish the deal form experience with small UX improvements that add up to a professional feel — visible deal identifiers, consistent date styling, efficient number editing, and clear validation messages.
Output: ProductHero with Deal ID + PDF link, DatePicker gold accent, onFocus on all number inputs.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-CONTEXT.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-RESEARCH.md
@src/presentation/components/features/deal/ProductHero/ProductHero.jsx
@src/presentation/components/common/DatePicker/DatePicker.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deal ID breadcrumb, PDF download, and DatePicker gold accent</name>
  <files>src/presentation/components/features/deal/ProductHero/ProductHero.jsx, src/presentation/components/common/DatePicker/DatePicker.jsx, src/presentation/components/features/deal/DealForm/DealFormFields.jsx, src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx</files>
  <action>
**Part A: Deal ID in ProductHero**

1. In ProductHero.jsx, compute a short Deal ID: `const shortDealId = deal?.id ? deal.id.slice(0, 8).toUpperCase() : null;`
2. Add a small breadcrumb/label above or near the product name:
   ```jsx
   {shortDealId && (
     <p className="text-[10px] text-[#4A5B6E] mb-1">
       Deal <span className="text-[#8899AA] font-mono">#{shortDealId}</span>
     </p>
   )}
   ```
3. Position it above the product category badge or as the first element in the product info area.

**Part B: PDF download link in ProductHero**

1. Check if the deal object has a product PDF URL. Inspect the Deal entity (`src/core/entities/Deal.js`) and product data on deals to find the correct field name. Likely candidates: `deal.productPdfUrl`, `deal.product?.pdfUrl`, or a field from the product document denormalized onto the deal.
2. If the field exists, add a gold-accented download link:
   ```jsx
   {pdfUrl && (
     <a href={pdfUrl} target="_blank" rel="noopener noreferrer"
       className="inline-flex items-center gap-1 text-xs text-[#FFD700] hover:text-[#FFE44D] underline transition-colors"
       title="Download product PDF">
       <Download size={12} />
       Product PDF
     </a>
   )}
   ```
   Import `Download` from lucide-react.
3. If no PDF field exists on the deal or product data, render nothing (conditional render). Document the finding in the summary.

**Part C: DatePicker gold accent**

1. In `DatePicker.jsx`, add a `gold` entry to `ACCENT_MAP`:
   ```js
   gold: {
     selected: 'bg-[#FFD700] text-[#0F1C2E]',
     today: 'border-[#FFD700]',
     focus: 'focus:border-[#FFD700]/50',
     icon: 'text-[#FFD700]',
     chevron: 'fill-[#FFD700]',
   },
   ```
2. Update the JSDoc `@param` for `accentColor` to include `'gold'` in the allowed values list.

3. In `DealFormFields.jsx`, find the DatePicker usage and change `accentColor` to `"gold"` (or add it if not set — it currently defaults to blue).
4. In `CounterOfferForm.jsx`, find the DatePicker usage and change `accentColor` to `"gold"`.

Per CONTEXT: gold for product-related contexts (deal forms), blue for RFQ-related contexts (existing blue usage in SubmitQuoteDialog stays unchanged).
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Deal ID "Deal #XXXXXXXX" visible in ProductHero. PDF download link shown if PDF field exists (gold accent styling). DatePicker gold accent added and used on deal form date inputs.</done>
</task>

<task type="auto">
  <name>Task 2: Number input onFocus auto-select and Zod validation audit</name>
  <files>src/presentation/components/features/deal/DealForm/DealFormFields.jsx, src/presentation/components/features/deal/DealForm/DealFormSidebar.jsx, src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx, src/presentation/components/features/deal/DealSidebar/FreightEstimatorWidget.jsx, src/presentation/components/features/provider/QuoteFormLogistics/QuoteFormLogistics.jsx, src/presentation/components/features/provider/QuoteFormInsurance/sections/CargoMarineSection.jsx, src/presentation/components/features/provider/QuoteFormInsurance/sections/CommercialRiskSection.jsx, src/presentation/components/features/provider/QuoteFormInsurance/sections/PremiumAdditionsSection.jsx, src/presentation/components/features/provider/QuoteFormInsurance/sections/PoliticalRiskSection.jsx, src/presentation/components/features/request/SubmitQuoteDialog/SubmitQuoteDialog.jsx</files>
  <action>
**Part A: Add onFocus={e => e.target.select()} to all type="number" inputs**

For each of the following files, find every `<input type="number" ...>` element and add the `onFocus` handler. If the input uses `{...register('fieldName')}`, add `onFocus` AFTER the spread so it does not get overridden:

1. `DealFormFields.jsx` — 3 number inputs (price, quantity, weight or similar)
2. `DealFormSidebar.jsx` — 1 number input
3. `CounterOfferForm.jsx` — 3 number inputs (price, quantity, etc.)
4. `FreightEstimatorWidget.jsx` — 2 number inputs (weight, dimensions)
5. `QuoteFormLogistics.jsx` — 2 number inputs
6. `CargoMarineSection.jsx` — 5 number inputs
7. `CommercialRiskSection.jsx` — 3 number inputs
8. `PremiumAdditionsSection.jsx` — 1 number input
9. `PoliticalRiskSection.jsx` — 2 number inputs
10. `SubmitQuoteDialog.jsx` — 2 number inputs

Pattern to apply on each input:
```jsx
<input
  type="number"
  {...register('fieldName')}
  onFocus={e => e.target.select()}
  className="..."
/>
```

If the input already has an onFocus handler, merge the behaviors: call `e.target.select()` after the existing handler logic.

Total: ~24 number inputs across 10 files.

**Part B: Zod validation message audit**

1. Search all Zod schemas in the codebase that are used in deal/trade/quote forms:
   - `src/core/validation/` or similar paths for `offerSchema`, `dealSchema`, `submitQuoteSchema`, `counterOfferSchema`
   - Any inline Zod schemas in form components

2. Verify all `.message()`, `.min()`, `.max()`, `.refine()` error messages are in English. The research found no Turkish messages, but confirm this.

3. Check the specific DatePicker null issue: find where `deliveryDate` (or equivalent date field) is validated with Zod. If the schema uses `z.string()` without handling null, change to `z.string({ required_error: 'Please select a delivery date' }).min(1, 'Please select a delivery date')`. Ensure the form default value for date fields is `''` (empty string) not `null`.

4. If any validation messages are found in a non-English language, translate them to English.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -rn 'type="number"' src/presentation/components/features/deal/ src/presentation/components/features/provider/ src/presentation/components/features/request/SubmitQuoteDialog/ | grep -v 'onFocus' | wc -l</automated>
  </verify>
  <done>All type="number" inputs across 10 deal/trade/quote files have onFocus={e => e.target.select()}. Zod validation messages are all in English. Date field null validation shows "Please select a delivery date" instead of "Expected string, received null".</done>
</task>

</tasks>

<verification>
- Build succeeds without errors
- Deal ID visible in ProductHero as "Deal #XXXXXXXX"
- PDF download link visible (if PDF field exists) with gold styling
- DatePicker on deal forms uses gold accent color
- Clicking into any number input auto-selects the current value
- grep for type="number" without onFocus returns 0 results in deal/trade files
- All Zod validation messages in English
</verification>

<success_criteria>
- Deal ID breadcrumb in ProductHero
- PDF download link (gold accent) or documented as no PDF field available
- DatePicker gold accent in ACCENT_MAP, used on DealFormFields and CounterOfferForm
- onFocus auto-select on all 24 number inputs across 10 files
- All Zod validation messages in English, date null error has friendly message
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-deal-and-trade-flow-enhancements/15-04-SUMMARY.md`
</output>
