---
phase: 15-deal-and-trade-flow-enhancements
plan: 02
type: execute
wave: 2
depends_on: ["15-01", "15-03"]
files_modified:
  - src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx
  - src/presentation/components/common/Tooltip/Tooltip.jsx
  - src/presentation/components/features/contract/ContractPage/ContractPage.jsx
  - src/app/(main)/deals/[dealId]/quotes/page.jsx
  - src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx
  - src/presentation/components/features/deal/DealPage/DealPage.jsx
autonomous: true
requirements:
  - DEAL-05
  - DEAL-06
  - DEAL-07
  - DEAL-08

must_haves:
  truths:
    - "Hire a Lawyer card appears on all 4 trade stages: Negotiation, Contract, Quotes, Trade Summary"
    - "Dismissing the card collapses it to a slim gold-accented one-line banner"
    - "Clicking the collapsed banner re-expands the full card"
    - "Dismissal resets on each page -- navigating to a new trade page shows the full card fresh"
    - "When a lawyer is already hired, card transforms to show the lawyer's name, status, and channel link"
    - "Tooltip icons on key trade page elements show brief explanations on hover/click"
  artifacts:
    - path: "src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx"
      provides: "Per-page dismiss with slim collapsed banner"
      contains: "Need legal advice"
    - path: "src/presentation/components/common/Tooltip/Tooltip.jsx"
      provides: "Reusable tooltip component"
      contains: "Info"
  key_links:
    - from: "ContractPage.jsx"
      to: "LegalBanner"
      via: "import and render"
      pattern: "LegalBanner"
    - from: "QuotesPage route"
      to: "LegalBanner"
      via: "import and render"
      pattern: "LegalBanner"
---

<objective>
Overhaul LegalBanner behavior (per-page dismiss, slim collapsed state, all trade pages), create a reusable Tooltip component, and place tooltips on key trade page elements.

Purpose: Make legal services discoverable at every deal stage without being intrusive, and help users new to international trade understand domain terminology.
Output: Modified LegalBanner on all 4 trade pages, new Tooltip component with placements.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-CONTEXT.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-RESEARCH.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-01-SUMMARY.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-03-SUMMARY.md
@src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx
@src/presentation/components/features/deal/DealPage/DealPage.jsx
@src/presentation/components/features/contract/ContractPage/ContractPage.jsx
@src/app/(main)/deals/[dealId]/quotes/page.jsx
@src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: LegalBanner per-page dismiss with slim collapsed banner</name>
  <files>src/presentation/components/features/legal/LegalBanner/LegalBanner.jsx</files>
  <action>
Rewrite LegalBanner dismiss behavior:

1. **Remove ALL localStorage usage.** Delete the `DISMISS_KEY` constant, the `useEffect` that reads from localStorage on mount, and the `localStorage.setItem` call in `handleDismiss`. The dismissed state must be local React state only: `const [dismissed, setDismissed] = useState(false)`.

2. **Replace null return with slim collapsed banner.** When `dismissed === true` AND no engagement exists, instead of returning `null`, render a slim one-line banner:
   - Full-width button with `onClick={() => setDismissed(false)}`
   - Styling: `w-full flex items-center gap-2 px-3 py-2 rounded-lg border-l-4 border-amber-500/60 bg-amber-900/10 text-left hover:bg-amber-900/20 transition-colors`
   - Content: scales emoji, "Need legal advice?" text in `text-xs text-[#8899AA]`, "Hire a Lawyer" text in `text-xs font-semibold text-[#FFD700] hover:text-[#FFE44D] ml-auto`

3. **Keep EngagementBadge unchanged.** When `engagement` exists (pending/active/completed), continue showing the EngagementBadge -- this is the "hired state" transformation per CONTEXT.md.

4. **Update component JSDoc** to reflect the new behavior: per-render dismiss, no localStorage, slim collapsed banner.

The result: every page mount starts with `dismissed = false`, showing the full promotional card. User clicks "No thanks" -> sees slim banner. Clicking slim banner re-expands. Navigating to another page creates a new component instance with `dismissed = false` again.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>LegalBanner uses local state only (no localStorage), shows slim gold-accented banner when dismissed, re-expands on click, resets on page navigation.</done>
</task>

<task type="auto">
  <name>Task 2: Tooltip component + LegalBanner on all trade pages + tooltip placements</name>
  <files>src/presentation/components/common/Tooltip/Tooltip.jsx, src/presentation/components/features/contract/ContractPage/ContractPage.jsx, src/app/(main)/deals/[dealId]/quotes/page.jsx, src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx, src/presentation/components/features/deal/DealPage/DealPage.jsx</files>
  <action>
**Part A: Create Tooltip component**

Create `src/presentation/components/common/Tooltip/Tooltip.jsx`:
- A simple info-icon tooltip that shows/hides on hover and click.
- Props: `content` (string or JSX), `children` (optional, defaults to `<Info size={13} />` from lucide-react).
- Implementation: `useState(false)` for open state. `onMouseEnter` opens, `onMouseLeave` closes, `onClick` toggles (for mobile).
- Styling: absolute-positioned popup above the trigger, `bg-[#1A283B] border border-[#2A3B52] rounded-lg p-2.5 text-xs text-[#8899AA] shadow-xl`, max-width `w-56`, with a small arrow/caret pointing down toward the trigger icon.
- The tooltip wrapper is `relative inline-flex` to position the popup.
- Export as named export: `export function Tooltip({ content, children })`.

**Part B: Add LegalBanner to ContractPage, QuotesPage route, and TradeSummaryTab**

LegalBanner is already on DealPage. Add it to:

1. **ContractPage.jsx**: Import `LegalBanner` from `@/presentation/components/features/legal/LegalBanner/LegalBanner`. Add `<LegalBanner dealId={deal.id} currentUserUid={currentUserUid} />` after the clause sections but before the sticky bottom progress bar -- positioned so it does not interfere with clause review flow.

2. **QuotesPage route** (`src/app/(main)/deals/[dealId]/quotes/page.jsx`): Import and add `<LegalBanner dealId={dealId} currentUserUid={currentUid} />` below the quote grids area. NOTE: This file was modified by Plan 03 (skip buttons) -- read current state before adding LegalBanner.

3. **TradeSummaryTab.jsx**: Import and add `<LegalBanner dealId={deal.id} currentUserUid={currentUserUid} />`. On Trade Summary, per CONTEXT, this should show the engagement summary (lawyer name, hire date, key actions). The existing EngagementBadge already shows name + status + channel link -- this satisfies the requirement. Place it near the LegalConsultingSection.

**Part C: Add tooltips on key trade page elements**

Place `<Tooltip>` next to these labels (Claude's discretion on exact placement):

1. **DealPage (negotiation)**: Next to the Incoterms label in the offer display area -- content: "Incoterms define which party bears the cost and risk of shipping. Common terms: FOB (seller delivers to port), CIF (seller covers insurance + freight), DAP (seller delivers to destination)." NOTE: DealPage was modified by Plan 03 (message icon) -- read current state.

2. **ContractPage**: Next to the "Contract Clauses" section header -- content: "Each clause must be reviewed and accepted individually by both parties. The deal cannot advance until all clauses are approved."

3. **QuotesPage**: Next to the section headers for Insurance and Logistics -- content for insurance: "Insurance quotes cover cargo damage, loss, and delays during transit. ICC-A provides the broadest coverage." Content for logistics: "Logistics quotes cover transportation from origin to destination. Compare rates, transit times, and transport modes."

4. **TradeSummaryTab**: Next to the "Trade Summary" header -- content: "A complete overview of your deal including agreed terms, selected providers, costs, and shipment status."

Use `<Tooltip content="...">` inline next to header text elements. The Info icon from lucide-react is used as the default child.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Tooltip component created. LegalBanner rendered on all 4 trade pages (DealPage, ContractPage, QuotesPage, TradeSummaryTab). Tooltip icons placed on Incoterms, contract clauses header, insurance/logistics headers, and trade summary header with explanatory content.</done>
</task>

</tasks>

<verification>
- Build succeeds without errors
- LegalBanner visible on DealPage, ContractPage, QuotesPage, and TradeSummaryTab
- Dismissing LegalBanner shows slim gold banner (not null)
- Clicking slim banner re-expands full card
- Navigating between trade pages resets the banner to full card
- When lawyer is hired, EngagementBadge shows instead of promotional card
- Tooltip component renders on hover/click with correct content
</verification>

<success_criteria>
- LegalBanner on all 4 trade stages with per-page dismiss (no localStorage)
- Slim collapsed banner with gold accent, re-expandable
- Reusable Tooltip component created
- Tooltips on Incoterms, contract clauses, insurance/logistics headers, trade summary header
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-deal-and-trade-flow-enhancements/15-02-SUMMARY.md`
</output>
