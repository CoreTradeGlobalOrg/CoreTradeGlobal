---
phase: 15-deal-and-trade-flow-enhancements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/contract/ClauseAccordion/ClauseAccordion.jsx
  - src/presentation/components/features/contract/ContractPage/ContractPage.jsx
  - src/presentation/hooks/contract/useContractActions.js
autonomous: true
requirements:
  - DEAL-01
  - DEAL-02
  - DEAL-03
  - DEAL-04

must_haves:
  truths:
    - "All contract clauses are always visible without accordion expand -- no hidden clauses"
    - "Unaccepted clauses show yellow background tint with instruction text"
    - "Accepted clauses show green checkmark with muted text"
    - "Progress indicator 'X of Y clauses accepted' appears at top and bottom of clause list"
    - "Bottom progress bar is sticky and sits near the approve button"
    - "When both parties approve all clauses, deal auto-advances to next stage with a toast"
    - "A party can un-accept a clause anytime before both parties have fully approved"
  artifacts:
    - path: "src/presentation/components/features/contract/ClauseAccordion/ClauseAccordion.jsx"
      provides: "Always-expanded clause rows with yellow/green visual states"
      contains: "bg-yellow-900"
    - path: "src/presentation/components/features/contract/ContractPage/ContractPage.jsx"
      provides: "ClauseProgressBar at top and bottom, sticky bottom bar"
      contains: "ClauseProgressBar"
    - path: "src/presentation/hooks/contract/useContractActions.js"
      provides: "hasEverExpanded initialized to all sections"
      contains: "hasExpanded"
  key_links:
    - from: "ContractPage.jsx"
      to: "ClauseAccordion.jsx"
      via: "hasEverExpanded always true"
      pattern: "hasEverExpanded.*true"
    - from: "ContractPage.jsx"
      to: "useContractActions"
      via: "localApprovedClauses.size for progress"
      pattern: "localApprovedClauses"
---

<objective>
Overhaul the contract approval UX: remove accordion collapse so all clauses are always visible, add yellow/green clause highlighting, add dual progress indicators (top + sticky bottom), and implement auto-advance with toast when both parties approve.

Purpose: Make the clause review process clearer and faster -- users see all clauses at once with visual state cues, know their progress, and advance automatically when done.
Output: Modified ContractPage, ClauseAccordion, and useContractActions with enhanced UX.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-CONTEXT.md
@.planning/phases/15-deal-and-trade-flow-enhancements/15-RESEARCH.md
@src/presentation/components/features/contract/ContractPage/ContractPage.jsx
@src/presentation/components/features/contract/ClauseAccordion/ClauseAccordion.jsx
@src/presentation/hooks/contract/useContractActions.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Always-expanded clause rows with yellow/green visual states</name>
  <files>src/presentation/components/features/contract/ClauseAccordion/ClauseAccordion.jsx, src/presentation/hooks/contract/useContractActions.js</files>
  <action>
1. In `useContractActions.js`: Initialize `hasExpanded` Set to contain ALL section IDs from the contract on mount (so all checkboxes are active from the start). This can be done by initializing from `contract.clauses` sections when contract loads. Remove the "must expand before approve" gating -- all clauses are immediately interactable.

2. In `ClauseAccordion.jsx`:
   - Remove the accordion toggle button/chevron UI -- section headers remain as labels but no expand/collapse behavior.
   - Remove the `isExpanded` conditional rendering of clause content -- all clause content is always rendered.
   - Add visual state to each clause row:
     - **Unaccepted**: `bg-yellow-900/15 border-l-2 border-yellow-500/50` with a small instruction text below the clause: `<p className="text-xs text-yellow-500/80 mt-1">Please review and accept this clause</p>`
     - **Accepted**: `bg-green-900/10 border-l-2 border-green-500/40` with a `Check` icon (from lucide-react, size 14, `text-green-400`) next to the clause label and `text-[#8899AA]` muted clause text.
   - Keep the checkbox functionality intact -- clicking toggles the clause approval state via the existing `onToggleClause` handler.
   - Ensure the un-accept capability works: checkboxes remain clickable (not disabled) as long as the current user has not submitted AND the contract is not fully approved by both parties. This already works via `isReadOnly` prop -- verify it is only true when `hasISubmitted || isFullyApproved`.

CRITICAL PITFALL: Do NOT remove the `hasEverExpanded` logic entirely -- just initialize it to contain all section IDs. The `isCheckboxActive` check references it, and removing it without updating the conditional would break all checkboxes.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>All clauses always visible (no accordion collapse), unaccepted clauses have yellow tint + instruction text, accepted clauses have green check + muted text, checkboxes remain functional for toggling approval state.</done>
</task>

<task type="auto">
  <name>Task 2: Dual progress indicator and auto-advance with toast</name>
  <files>src/presentation/components/features/contract/ContractPage/ContractPage.jsx</files>
  <action>
1. Create a `ClauseProgressBar` inline component (or at the top of ContractPage.jsx) that renders:
   - "X of Y clauses accepted" text left-aligned, percentage right-aligned in gold `text-[#FFD700]`
   - A progress bar: `h-1.5 bg-[#2A3B52] rounded-full` container, inner `bg-[#FFD700] rounded-full transition-all` fill
   - Props: `approved` (number), `total` (number)

2. Add `ClauseProgressBar` in TWO locations:
   - **Top**: Above the clause sections list, after the contract header area
   - **Bottom**: In a sticky bottom bar (`sticky bottom-4 z-10`) inside a `bg-[#1A283B] border border-[#2A3B52] rounded-xl p-3 shadow-lg shadow-black/40` container. Place the submit/approve button below the progress bar in this sticky container.

3. Wire progress data: `approved = actions.localApprovedClauses?.size ?? 0`, `total = contract?.clauses?.length ?? 0`.

4. Auto-advance with toast: After `actions.handleSubmitApproval()` succeeds AND both parties have now approved all clauses, the deal status changes to `CONTRACT_APPROVED` server-side. The existing `useDeal` hook will pick up the status change. Add a `useEffect` in ContractPage (or the route page) that watches `deal.status` -- when it transitions to `CONTRACT_APPROVED`, show a toast via `react-hot-toast`: `toast.success('Contract approved! Moving to provider quotes...')` and redirect to `/deals/${dealId}/quotes` after a short delay (1.5s).

NOTE: The auto-advance may already partially work because the contract route page redirects non-ACCEPTED/non-CONTRACT_APPROVED statuses back to the deal page. Verify the flow: after both approve, deal becomes CONTRACT_APPROVED, the contract page redirect fires (it allows CONTRACT_APPROVED), but the user should be shown the toast and then navigated to quotes. Add a new check: if deal transitions TO CONTRACT_APPROVED while user is on the page, toast + navigate to quotes.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Progress bar visible at top and bottom of clause list showing "X of Y clauses accepted" with gold fill. Bottom bar is sticky. When both parties fully approve, toast notification appears and user is redirected to quotes page.</done>
</task>

</tasks>

<verification>
- Build succeeds without errors
- ContractPage renders all clauses without accordion (no expand/collapse)
- Yellow/green visual distinction on clause rows
- Progress indicator at top and bottom of clause list
- Bottom progress bar is sticky within viewport
- Checkboxes toggle clause approval; un-accept works before submission
</verification>

<success_criteria>
- All clause rows always visible -- no accordion toggle
- Unaccepted clauses: yellow background + "Please review and accept this clause" text
- Accepted clauses: green background + check icon + muted text
- Progress bar "X of Y clauses accepted" at top and sticky bottom
- Auto-advance toast + redirect to /deals/[dealId]/quotes on full dual-party approval
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/15-deal-and-trade-flow-enhancements/15-01-SUMMARY.md`
</output>
