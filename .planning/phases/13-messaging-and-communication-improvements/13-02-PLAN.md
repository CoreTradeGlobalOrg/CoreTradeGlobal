---
phase: 13-messaging-and-communication-improvements
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
  - src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx
autonomous: true
requirements: [MSG-04, MSG-05]

must_haves:
  truths:
    - "FAB widget is not visible when user is on /messages or /messages/* routes"
    - "Message notification click on /messages page opens conversation inline instead of opening FAB overlay"
    - "FAB widget remains visible and functional on all other routes"
  artifacts:
    - path: "src/presentation/components/common/MessagesWidget/MessagesWidget.jsx"
      provides: "Route-aware FAB hiding"
      contains: "usePathname"
    - path: "src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx"
      provides: "Smart notification routing for message notifications"
      contains: "startsWith('/messages')"
  key_links:
    - from: "src/presentation/components/common/MessagesWidget/MessagesWidget.jsx"
      to: "usePathname"
      via: "import from next/navigation, early return null on /messages"
      pattern: "pathname.*startsWith.*messages"
    - from: "NotificationCenterPage.jsx"
      to: "router.push('/messages')"
      via: "conditional routing based on current pathname"
      pattern: "startsWith.*messages"
---

<objective>
Fix the dual chat opening bug by hiding the FAB widget on /messages routes, and fix notification routing so message notifications open conversations in the correct view based on current route.

Purpose: When users are on the /messages page, the FAB widget should not appear because it creates a confusing duplicate UI. Message notifications should route to the correct view depending on where the user currently is.
Output: FAB hidden on /messages, notification click routing fixed.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-messaging-and-communication-improvements/13-CONTEXT.md
@.planning/phases/13-messaging-and-communication-improvements/13-RESEARCH.md

@src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
@src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx
@src/presentation/contexts/MessagesContext.jsx

<interfaces>
From MessagesWidget.jsx:
```javascript
// Currently: early return if !isAuthenticated
// Need to add: early return if pathname starts with /messages
export function MessagesWidget() {
  const { user, isAuthenticated } = useAuth();
  // ... renders FAB button and expanded conversation panel
}
```

From NotificationCenterPage.jsx (line ~166):
```javascript
// Currently: notification click handler for message-type notifications
// calls openConversation() which sets isWidgetOpen: true
// Bug: on /messages page, this opens FAB overlay on top of the page
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hide FAB on /messages routes and fix notification routing</name>
  <files>
    src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
    src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx
  </files>
  <action>
1. In MessagesWidget.jsx:
   - Import `usePathname` from `next/navigation`
   - Add `const pathname = usePathname();` inside the component (after useAuth/useMessages hooks)
   - Add an early return AFTER the existing `if (!isAuthenticated) return null;` guard:
     `if (pathname?.startsWith('/messages')) return null;`
   - This ensures the FAB (both the floating button and expanded panel) never renders on /messages or /messages/[conversationId]

2. In NotificationCenterPage.jsx:
   - Import `usePathname` from `next/navigation` (if not already imported)
   - In the notification click handler for message-type notifications (around line 166), add pathname-aware routing:
     - If `pathname?.startsWith('/messages')`: use `router.push('/messages?conversation=' + conversationId)` instead of calling `openConversation(conversationId)` -- this keeps the user on the full-page messages view and selects the conversation there
     - If NOT on /messages: call `openConversation(conversationId)` as currently done (opens FAB widget)
   - Also check NotificationFilterTabs.jsx and NotificationBell for similar notification click handlers that may call openConversation -- apply the same fix if found
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>FAB widget is hidden on /messages and /messages/* routes. Message notification clicks on /messages page route to the correct conversation inline. FAB continues to work normally on all other pages.</done>
</task>

</tasks>

<verification>
- Navigate to /messages: FAB button should NOT be visible in bottom-right corner
- Navigate to /messages/[conversationId]: FAB button should NOT be visible
- Navigate to /deals or /products: FAB button IS visible and functional
- On /messages page, click a message notification: conversation opens inline on /messages, NOT in FAB overlay
- On any other page, click a message notification: FAB opens with that conversation
</verification>

<success_criteria>
- FAB widget completely hidden (both button and panel) on /messages and all sub-routes
- Notification click routing is context-aware: /messages uses inline navigation, other pages use FAB
- No dual-panel bug (FAB on top of /messages page)
</success_criteria>

<output>
After completion, create `.planning/phases/13-messaging-and-communication-improvements/13-02-SUMMARY.md`
</output>
