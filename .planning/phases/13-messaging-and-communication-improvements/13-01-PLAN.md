---
phase: 13-messaging-and-communication-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/usecases/messaging/CreateConversationUseCase.js
  - src/presentation/components/features/messaging/ConversationProfileCard/ConversationProfileCard.jsx
  - src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
  - src/app/(main)/messages/[conversationId]/page.jsx
autonomous: true
requirements: [MSG-01, MSG-02, MSG-03]

must_haves:
  truths:
    - "Every open conversation in the FAB widget shows a compact profile card header with avatar, name, company, country, and role badge"
    - "Every open conversation on /messages/[conversationId] page shows the same compact profile card header"
    - "Clicking the profile card navigates to /profile/[userId]"
    - "When product/RFQ context exists, profile card appears above the context banner"
  artifacts:
    - path: "src/presentation/components/features/messaging/ConversationProfileCard/ConversationProfileCard.jsx"
      provides: "Shared profile card component"
      exports: ["ConversationProfileCard"]
    - path: "src/domain/usecases/messaging/CreateConversationUseCase.js"
      provides: "Country field in participantDetails"
      contains: "country"
  key_links:
    - from: "src/presentation/components/common/MessagesWidget/MessagesWidget.jsx"
      to: "ConversationProfileCard"
      via: "import and render when activeConversationId is set"
      pattern: "ConversationProfileCard"
    - from: "src/app/(main)/messages/[conversationId]/page.jsx"
      to: "ConversationProfileCard"
      via: "import and render at top of conversation view"
      pattern: "ConversationProfileCard"
---

<objective>
Create a shared ConversationProfileCard component and integrate it into both the FAB chat widget and the /messages/[conversationId] page. Also enrich participantDetails with the country field so the card can display it.

Purpose: Users need to see who they are chatting with at a glance -- avatar, name, company, country, and role -- in every conversation view. The profile card is clickable to navigate to the user's profile.
Output: ConversationProfileCard component rendered in both FAB widget and messages page, with country field enrichment in CreateConversationUseCase.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-messaging-and-communication-improvements/13-CONTEXT.md
@.planning/phases/13-messaging-and-communication-improvements/13-RESEARCH.md

@src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
@src/app/(main)/messages/[conversationId]/page.jsx
@src/domain/usecases/messaging/CreateConversationUseCase.js

<interfaces>
From src/domain/usecases/messaging/CreateConversationUseCase.js:
```javascript
// participantDetails currently stores per-user:
participantDetails[participantId] = {
  displayName: user.displayName || user.email,
  photoURL: user.companyLogo || user.photoURL || null,
  email: user.email,
  role: user.role,
  companyId: user.companyId || null,
  companyName: user.companyName || null,
  // MISSING: country — needs to be added
};
```

From src/presentation/contexts/MessagesContext.jsx:
```javascript
// Available via useMessages():
// activeConversationId, getActiveConversation(), conversations, openConversation(), closeConversation()
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ConversationProfileCard and enrich participantDetails with country</name>
  <files>
    src/presentation/components/features/messaging/ConversationProfileCard/ConversationProfileCard.jsx
    src/domain/usecases/messaging/CreateConversationUseCase.js
  </files>
  <action>
1. In CreateConversationUseCase.js, add `country: user.country || null` to the participantDetails object (line ~69, after companyName). This follows the existing denormalization pattern. Existing conversations without country will gracefully show no country line.

2. Create ConversationProfileCard/ConversationProfileCard.jsx as a 'use client' component:
   - Props: `{ otherUserId, participantDetails, onNavigate }` where otherUserId is the ID of the OTHER participant (not the current user)
   - Reads details from `participantDetails[otherUserId]`
   - Renders as a Next.js Link to `/profile/${otherUserId}` with onClick calling onNavigate (to close widget/panel if needed)
   - Layout: horizontal flex row with:
     - Avatar: 40x40 rounded-full with photoURL image or initial letter fallback (bg-[#1E2D3D])
     - Info column: displayName (white, font-semibold, text-sm, truncate) + role badge (text-xs, gold accent bg-[rgba(255,215,0,0.1)] text-[#FFD700] border-[rgba(255,215,0,0.3)]) on same line
     - companyName below name (text-[#94a3b8] text-xs truncate)
     - country below company (text-[#6b7a8d] text-xs) -- only render if truthy
   - Container: px-4 py-3 border-b border-[rgba(255,255,255,0.08)] hover:bg-[rgba(255,255,255,0.03)] transition-colors
   - Entire card is the Link — clickable area is the full width
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ConversationProfileCard component exists and exports correctly. CreateConversationUseCase stores country in participantDetails for new conversations.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate ConversationProfileCard into FAB widget and /messages page</name>
  <files>
    src/presentation/components/common/MessagesWidget/MessagesWidget.jsx
    src/app/(main)/messages/[conversationId]/page.jsx
  </files>
  <action>
1. In MessagesWidget.jsx, when activeConversationId is set and the conversation panel is showing:
   - Import ConversationProfileCard from the new location
   - Determine the otherUserId: from the active conversation's participants array, filter out the current user.uid
   - Render ConversationProfileCard ABOVE the existing context banner (product/RFQ banner) but BELOW the header controls (back arrow, minimize button)
   - Pass onNavigate={() => { closeConversation(); setIsWidgetOpen(false); }} so clicking the card closes the FAB
   - Only show for conversation types that have real users ('direct', 'provider_quote') -- NOT for 'contact' or 'system' types
   - For multi-participant conversations (provider_quote with 3 users), show the profile card for the first non-current-user participant, or skip the card if ambiguous

2. In /messages/[conversationId]/page.jsx:
   - Import ConversationProfileCard
   - When the conversation is loaded and the conversation view is showing messages, render ConversationProfileCard at the top of the conversation content area
   - Determine otherUserId the same way (filter current user from participants)
   - Pass onNavigate as null or a no-op (navigating away from /messages is fine)
   - Position: above message thread, below any header/navigation controls
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Profile card with avatar, name, company, country, and role badge appears at the top of every conversation in both the FAB widget and the /messages/[conversationId] page. Clicking the card navigates to /profile/[userId]. When product/RFQ context exists, profile card appears above the context banner.</done>
</task>

</tasks>

<verification>
- Open a conversation in the FAB widget: profile card header visible with avatar, name, company, country (if available), role badge
- Click profile card: navigates to /profile/[userId], FAB closes
- Open same conversation on /messages/[conversationId]: same profile card visible
- New conversation created: participantDetails includes country field
- Conversation with product context: profile card renders above the product/RFQ banner
</verification>

<success_criteria>
- ConversationProfileCard renders in both FAB and /messages views
- Profile card shows avatar, displayName, companyName, country (when available), role badge
- Clicking navigates to /profile/[userId]
- Profile card positioned above context banners when both present
- Country field stored in participantDetails for new conversations
</success_criteria>

<output>
After completion, create `.planning/phases/13-messaging-and-communication-improvements/13-01-SUMMARY.md`
</output>
