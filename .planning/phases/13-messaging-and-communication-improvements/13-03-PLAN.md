---
phase: 13-messaging-and-communication-improvements
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - src/domain/usecases/messaging/CreateConversationUseCase.js
  - src/data/repositories/ConversationRepository.js
  - src/presentation/components/features/quote/ProviderQuoteChatSidebar/ProviderQuoteChatSidebar.jsx
  - src/app/(main)/deals/[dealId]/quotes/page.jsx
  - src/presentation/components/features/quote/QuotesPage/QuotesPage.jsx
  - src/app/(main)/provider/quotes/[requestId]/page.jsx
  - firestore.indexes.json
autonomous: true
requirements: [MSG-06, MSG-07, MSG-08]

must_haves:
  truths:
    - "A chat sidebar is visible on the right side of the buyer's quote comparison page at /deals/[dealId]/quotes"
    - "A chat sidebar is visible on the provider's quote detail page at /provider/quotes/[requestId]"
    - "Both buyer and provider sides share the same conversation thread for a given (dealId, providerId) pair"
    - "Buyer can see separate chat threads per provider and switch between them"
    - "3-party conversation includes buyer, seller, and the specific provider"
  artifacts:
    - path: "src/presentation/components/features/quote/ProviderQuoteChatSidebar/ProviderQuoteChatSidebar.jsx"
      provides: "Chat sidebar component for quote pages"
      exports: ["ProviderQuoteChatSidebar"]
    - path: "src/data/repositories/ConversationRepository.js"
      provides: "findProviderQuoteConversation and createWithId methods"
      contains: "findProviderQuoteConversation"
    - path: "src/domain/usecases/messaging/CreateConversationUseCase.js"
      provides: "provider_quote type validation"
      contains: "provider_quote"
  key_links:
    - from: "ProviderQuoteChatSidebar"
      to: "ConversationRepository.findProviderQuoteConversation"
      via: "lookup or create conversation by dealId + providerId"
      pattern: "findProviderQuoteConversation"
    - from: "src/app/(main)/deals/[dealId]/quotes/page.jsx"
      to: "ProviderQuoteChatSidebar"
      via: "render sidebar alongside QuotesPage"
      pattern: "ProviderQuoteChatSidebar"
    - from: "src/app/(main)/provider/quotes/[requestId]/page.jsx"
      to: "ProviderQuoteChatSidebar"
      via: "render sidebar alongside quote detail"
      pattern: "ProviderQuoteChatSidebar"
---

<objective>
Add a provider-buyer-seller chat sidebar to both the buyer's quote comparison page and the provider's quote detail page. Create a new 'provider_quote' conversation type supporting 3-party conversations with deterministic deduplication.

Purpose: Providers, buyers, and sellers need to communicate directly on quote pages without leaving the quote context. Each provider gets a separate conversation thread with both deal parties.
Output: ProviderQuoteChatSidebar on both quote pages, new conversation type and repository methods, Firestore composite index.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-messaging-and-communication-improvements/13-CONTEXT.md
@.planning/phases/13-messaging-and-communication-improvements/13-RESEARCH.md
@.planning/phases/13-messaging-and-communication-improvements/13-01-SUMMARY.md

@src/domain/usecases/messaging/CreateConversationUseCase.js
@src/data/repositories/ConversationRepository.js
@src/app/(main)/deals/[dealId]/quotes/page.jsx
@src/app/(main)/provider/quotes/[requestId]/page.jsx
@src/presentation/components/features/quote/QuotesPage/QuotesPage.jsx
@src/presentation/components/features/messaging/MessageThread/MessageThread.jsx
@src/presentation/components/features/messaging/MessageInput/MessageInput.jsx

<interfaces>
From ConversationRepository.js:
```javascript
// Existing methods:
async create(conversationData) { ... }
async getById(conversationId) { ... }
async findDirectConversation(userId1, userId2, context) { ... }
// NEED TO ADD: findProviderQuoteConversation(dealId, providerId)
// NEED TO ADD: createWithId(docId, conversationData)
```

From FirestoreDataSource.js:
```javascript
// Already exists:
async createWithId(collectionName, docId, data) { ... }
// Can be used directly by ConversationRepository
```

From CreateConversationUseCase.js:
```javascript
// validTypes currently: ['direct', 'contact', 'system']
// Need to add: 'provider_quote'
// validateInputs needs to allow 3 participants for provider_quote type
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add provider_quote conversation type and repository methods</name>
  <files>
    src/domain/usecases/messaging/CreateConversationUseCase.js
    src/data/repositories/ConversationRepository.js
    firestore.indexes.json
  </files>
  <action>
1. In CreateConversationUseCase.js:
   - Add `'provider_quote'` to the `validTypes` array in `validateInputs()` (line 161)
   - In `validateInputs()`, add a check: if `type === 'provider_quote'` and `participantIds.length !== 3`, throw 'Provider quote conversations require exactly 3 participants'
   - In `execute()`, after the existing direct conversation dedup check (line 40-56), add a parallel block for provider_quote:
     ```
     if (type === 'provider_quote' && metadata.dealId && metadata.providerId) {
       const deterministicId = `providerquote_${metadata.dealId}_${metadata.providerId}`;
       const existing = await this.conversationRepository.getById(deterministicId);
       if (existing) return existing;
       // If not existing, create with deterministic ID
       // Build participantDetails and conversationData as normal, then:
       const conversation = await this.conversationRepository.createWithId(deterministicId, conversationData);
       // Handle initial message if present (same logic as current)
       return conversation;
     }
     ```
   - Keep the existing `create()` call for non-provider_quote types unchanged

2. In ConversationRepository.js:
   - Add `createWithId(docId, conversationData)` method that calls `this.firestoreDataSource.createWithId(COLLECTIONS.CONVERSATIONS, docId, conversationData)` and returns the result with the id attached
   - Add `findProviderQuoteConversation(dealId, providerId)` method:
     Query conversations collection where type == 'provider_quote' AND metadata.dealId == dealId AND metadata.providerId == providerId. Return first match or null.
     NOTE: This is a fallback lookup -- the primary path uses deterministic ID via getById. This method is useful for listing all provider_quote conversations for a deal.
   - Add `getProviderQuoteConversationsForDeal(dealId)` method:
     Query conversations where type == 'provider_quote' AND metadata.dealId == dealId. Return all matches. This powers the buyer's sidebar provider list.

3. In firestore.indexes.json:
   - Add composite index on conversations collection: `type` (ASC) + `metadata.dealId` (ASC) + `metadata.providerId` (ASC) for the findProviderQuoteConversation query
   - Add composite index: `type` (ASC) + `metadata.dealId` (ASC) for getProviderQuoteConversationsForDeal query
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>CreateConversationUseCase accepts provider_quote type with 3 participants. ConversationRepository has createWithId, findProviderQuoteConversation, and getProviderQuoteConversationsForDeal methods. Firestore indexes defined.</done>
</task>

<task type="auto">
  <name>Task 2: Build ProviderQuoteChatSidebar and integrate into both quote pages</name>
  <files>
    src/presentation/components/features/quote/ProviderQuoteChatSidebar/ProviderQuoteChatSidebar.jsx
    src/app/(main)/deals/[dealId]/quotes/page.jsx
    src/presentation/components/features/quote/QuotesPage/QuotesPage.jsx
    src/app/(main)/provider/quotes/[requestId]/page.jsx
  </files>
  <action>
1. Create ProviderQuoteChatSidebar/ProviderQuoteChatSidebar.jsx as a 'use client' component:
   - Props: `{ dealId, buyerId, sellerId, providerId, providerType, currentUserId }`
   - For BUYER view (when currentUserId === buyerId or sellerId):
     - Fetch all provider_quote conversations for this deal using getProviderQuoteConversationsForDeal(dealId)
     - Show a list of providers (from conversation participantDetails) at the top, each clickable to select that thread
     - Active thread shows MessageThread + MessageInput for the selected provider conversation
     - If no conversation exists yet for a provider, create one on first message send using CreateConversationUseCase with type: 'provider_quote', participants: [buyerId, sellerId, providerId], metadata: { dealId, providerId, providerType }
   - For PROVIDER view (when currentUserId === providerId):
     - Show single conversation thread (their own conversation with the deal parties)
     - No provider selector needed -- they only see their own thread
   - Layout: w-[380px] or ~1/3 width, fixed height matching the quote content area, border-l border-[#1E2D3D], bg-[#0A1628]
   - Header: "Chat" title with provider name or "Provider Chats" for buyer multi-view
   - Reuse existing MessageThread and MessageInput components from src/presentation/components/features/messaging/
   - Subscribe to messages in real-time using MessageRepository pattern
   - Use the conversation's ConversationProfileCard (from Plan 13-01) at the top of the active thread

2. In /deals/[dealId]/quotes/page.jsx (buyer's quote comparison page):
   - Wrap existing QuotesPage content in a flex row layout
   - Add ProviderQuoteChatSidebar on the right side
   - Pass dealId from route params, buyerId/sellerId from deal data, currentUserId from auth
   - The sidebar should be visible by default with a toggle button to collapse/expand on smaller screens
   - The existing QuotesPage gets flex-1 to fill remaining width

3. In QuotesPage.jsx:
   - No changes needed to the component itself -- the layout change happens in the page.jsx wrapper
   - If QuotesPage currently has a max-width constraint, ensure it uses flex-1 instead so it fills available space alongside the sidebar

4. In /provider/quotes/[requestId]/page.jsx (provider's quote detail page):
   - Add ProviderQuoteChatSidebar on the right side in a similar flex row layout
   - Pass dealId (from the quote request data), buyerId, sellerId, providerId (current user), providerType
   - Provider sees only their single conversation thread
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ProviderQuoteChatSidebar renders on both buyer's /deals/[dealId]/quotes and provider's /provider/quotes/[requestId] pages. Buyer can switch between provider chat threads. Provider sees their single thread. Both sides share the same conversation. Messages send and receive in real-time.</done>
</task>

</tasks>

<verification>
- On buyer's /deals/[dealId]/quotes page: chat sidebar visible on right side with provider list
- Click a provider in the list: their conversation thread loads
- Send a message as buyer: appears in real-time
- On provider's /provider/quotes/[requestId] page: chat sidebar visible with single thread
- Send message as provider: appears on buyer's view of same thread
- Conversation is 3-party: buyer, seller, and provider all participate
- Creating the same conversation from both sides results in ONE conversation (deterministic ID)
</verification>

<success_criteria>
- Chat sidebar renders on both quote pages (~1/3 width)
- Buyer sees switchable list of provider threads
- Provider sees single thread with deal parties
- Same conversation thread shared between buyer and provider views
- New provider_quote conversation type with 3 participants works correctly
- Deterministic conversation ID prevents duplicates
</success_criteria>

<output>
After completion, create `.planning/phases/13-messaging-and-communication-improvements/13-03-SUMMARY.md`
</output>
