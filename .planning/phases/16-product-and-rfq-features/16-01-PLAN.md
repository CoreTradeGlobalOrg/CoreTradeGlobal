---
phase: 16-product-and-rfq-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/product/ProductCard/ProductCard.jsx
  - src/presentation/hooks/product/useFavoriteProduct.js
  - src/app/(main)/product/[productId]/page.jsx
  - src/app/(main)/product/[productId]/ProductGallery.jsx
  - src/app/(main)/product/[productId]/ProductImageZoom.jsx
  - src/app/(main)/favorites/page.jsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-04
  - PROD-09

must_haves:
  truths:
    - "User can click a star icon on any product card to add/remove it from favorites"
    - "User can see all their favorited products on a dedicated /favorites page"
    - "User can share a product via Web Share API or clipboard copy on product detail page"
    - "User can hover over the product image on desktop to see a magnified zoom overlay"
  artifacts:
    - path: "src/presentation/hooks/product/useFavoriteProduct.js"
      provides: "Favorite toggle hook with arrayUnion/arrayRemove"
      exports: ["useFavoriteProduct"]
    - path: "src/app/(main)/favorites/page.jsx"
      provides: "Favorites listing page"
      min_lines: 40
    - path: "src/app/(main)/product/[productId]/ProductImageZoom.jsx"
      provides: "Hover magnifier component"
      min_lines: 30
  key_links:
    - from: "src/presentation/components/features/product/ProductCard/ProductCard.jsx"
      to: "Firestore users/{uid}"
      via: "useFavoriteProduct toggle"
      pattern: "arrayUnion|arrayRemove"
    - from: "src/app/(main)/favorites/page.jsx"
      to: "Firestore users/{uid}.favoriteProductIds"
      via: "read user doc then fetch products"
      pattern: "favoriteProductIds"
---

<objective>
Add product favorites (star toggle on cards + detail page, /favorites page), Web Share API share button on product detail, and desktop hover image zoom on product gallery.

Purpose: These are independent UI enhancements to the product browsing experience that touch product cards, product detail page, and a new favorites page.
Output: useFavoriteProduct hook, updated ProductCard with star, updated product detail with share + star + zoom, new /favorites page.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-product-and-rfq-features/16-CONTEXT.md
@.planning/phases/16-product-and-rfq-features/16-RESEARCH.md

@src/presentation/components/features/product/ProductCard/ProductCard.jsx
@src/app/(main)/product/[productId]/page.jsx
@src/app/(main)/product/[productId]/ProductGallery.jsx
@src/app/(main)/news/[newsId]/NewsDetailClient.jsx

<interfaces>
<!-- Existing ProductCard props -->
From src/presentation/components/features/product/ProductCard/ProductCard.jsx:
```javascript
export function ProductCard({ product, isOwnProfile, onEdit, onDelete, onToggleStatus })
```

<!-- Existing ProductGallery -->
From src/app/(main)/product/[productId]/ProductGallery.jsx:
```javascript
// Receives images array, handles image carousel
```

<!-- Web Share API pattern from Phase 12 -->
From src/app/(main)/news/[newsId]/NewsDetailClient.jsx:
```javascript
// navigator.share({ title, url }) with clipboard fallback + toast
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Favorites hook, star icon on ProductCard and product detail, share button</name>
  <files>
    src/presentation/hooks/product/useFavoriteProduct.js,
    src/presentation/components/features/product/ProductCard/ProductCard.jsx,
    src/app/(main)/product/[productId]/page.jsx
  </files>
  <action>
1. Create `src/presentation/hooks/product/useFavoriteProduct.js`:
   - Hook takes no args, returns `{ favoriteIds, toggleFavorite, isFavorited }`.
   - On mount, subscribe to the current user's doc (`users/{uid}`) and read `favoriteProductIds` array (default `[]`).
   - `toggleFavorite(productId)` uses `updateDoc` with `arrayUnion` (if not favorited) or `arrayRemove` (if favorited). Optimistically update local `favoriteIds` state before Firestore write completes.
   - `isFavorited(productId)` returns boolean from local state.
   - Use `useAuth()` or equivalent to get current user. If no user logged in, `toggleFavorite` shows a toast "Sign in to save favorites" and returns early.

2. Update `ProductCard.jsx`:
   - Accept new optional props: `isFavorited`, `onToggleFavorite`.
   - Move the existing status badge from `absolute top-2 right-2` to `absolute top-2 left-2` to make room for the star.
   - Add a star button at `absolute top-2 right-2 z-10` with `stopPropagation` on click. Use `Star` from lucide-react. When favorited: `fill-[#FFD700] text-[#FFD700]`. When not: `text-white`. Background: `bg-black/40 hover:bg-black/60 rounded-full p-1.5`. Only render star if `onToggleFavorite` is provided (so admin/profile views can opt out).

3. Update product detail page (`page.jsx`):
   - Import `useFavoriteProduct`. Add star icon toggle near the product title area.
   - Add a Share button (Share2 icon from lucide-react) next to the star. On click: use `navigator.share({ title: product.name, url: window.location.href })` if available, otherwise `navigator.clipboard.writeText(url)` + `toast.success('Link copied!')`. Follow exact pattern from `NewsDetailClient.jsx`.

4. Wire favorites into the products listing pages that render ProductCard — the All Products page (`products/page.jsx`) should import `useFavoriteProduct` and pass `isFavorited`/`onToggleFavorite` to each ProductCard.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Star icon toggles on product cards and detail page. Share button on detail page with Web Share API + clipboard fallback. useFavoriteProduct hook writes to Firestore atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Favorites page and product image hover zoom</name>
  <files>
    src/app/(main)/favorites/page.jsx,
    src/app/(main)/product/[productId]/ProductImageZoom.jsx,
    src/app/(main)/product/[productId]/ProductGallery.jsx
  </files>
  <action>
1. Create `src/app/(main)/favorites/page.jsx`:
   - "use client" page. Auth guard: if no user, redirect to /login.
   - Import `useFavoriteProduct` to get `favoriteIds`.
   - Fetch all favorited products using `Promise.all(favoriteIds.map(id => productRepository.getById(id)))`. Filter out null results (deleted products).
   - Render a grid of `ProductCard` components with `isFavorited={true}` and `onToggleFavorite` wired up (removing from favorites removes the card from the page).
   - Show empty state when no favorites: "No favorites yet. Browse products to add some!" with a link to /products.
   - Page title: "My Favorites". Add loading skeleton while fetching.

2. Create `src/app/(main)/product/[productId]/ProductImageZoom.jsx`:
   - Component receives `imageSrc` (string) and `alt` (string).
   - On desktop (`min-width: 1024px` or similar), when user hovers over the image container:
     - Show a zoom panel (300x300px) positioned to the right of the image container.
     - The zoom panel uses the same image as `background-image` with `background-size: 200%`.
     - `background-position` tracks mouse position: calculate `x%` and `y%` from `offsetX`/`offsetY` relative to the image container.
     - Show a small lens indicator (dashed border square) on the source image at the hover position.
   - On mouse leave, hide the zoom panel.
   - On mobile/tablet, do not show zoom at all (just render the image normally).

3. Update `ProductGallery.jsx`:
   - Wrap the main image display with `ProductImageZoom` component.
   - Pass the current image URL and product name as props.
   - Ensure the gallery's prev/next buttons still work alongside the zoom.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>/favorites page shows user's favorited products with remove capability. Product detail image has desktop hover zoom magnifier. Build passes with no errors.</done>
</task>

</tasks>

<verification>
- Visit /products — star icons visible on product cards, toggle works
- Visit /product/[id] — star icon, share button, and image zoom all functional
- Visit /favorites — shows favorited products, empty state when none
- Share button uses native share sheet on mobile, clipboard on desktop
- Image zoom only activates on desktop hover, not on mobile
</verification>

<success_criteria>
- Star icon on product cards toggles favoriteProductIds in Firestore via arrayUnion/arrayRemove
- /favorites page lists all favorited products with loading and empty states
- Share button follows Phase 12 Web Share API pattern with clipboard fallback
- Hover zoom shows magnified image panel on desktop only
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-product-and-rfq-features/16-01-SUMMARY.md`
</output>
