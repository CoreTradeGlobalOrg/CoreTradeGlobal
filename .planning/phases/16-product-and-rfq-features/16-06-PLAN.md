---
phase: 16-product-and-rfq-features
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/product/[productId]/ProductImageZoom.jsx
  - src/app/(main)/product/[productId]/ProductGallery.jsx
  - src/app/(main)/product/[productId]/page.jsx
  - src/presentation/components/homepage/Hero/HeroSection.jsx
  - src/presentation/components/homepage/StrategicCTA/StrategicCTA.jsx
  - src/presentation/components/features/admin/ProductsRequestsManager/ProductsRequestsManager.jsx
autonomous: true
gap_closure: true
requirements: [PROD-04, PROD-06, PROD-08]

must_haves:
  truths:
    - "Hovering over the main product image on desktop shows a magnified zoom panel"
    - "Start Deal button has gold styling and is positioned below Contact Seller and View Profile buttons"
    - "RFQ modal does not close when clicking the backdrop at any call site"
  artifacts:
    - path: "src/app/(main)/product/[productId]/ProductImageZoom.jsx"
      provides: "Working desktop hover zoom"
    - path: "src/app/(main)/product/[productId]/page.jsx"
      provides: "Repositioned Start Deal button"
    - path: "src/presentation/components/homepage/Hero/HeroSection.jsx"
      provides: "RFQ modal with no-op backdrop close"
  key_links:
    - from: "ProductGallery.jsx"
      to: "ProductImageZoom.jsx"
      via: "component rendering"
      pattern: "ProductImageZoom"
    - from: "page.jsx"
      to: "ProductSellerCard.jsx"
      via: "Start Deal button placement after seller card"
      pattern: "Start Deal"
---

<objective>
Fix three UAT gaps: repair the product image hover zoom (GAP-4), restyle and reposition the Start Deal button (GAP-5), and prevent RFQ modal from closing on backdrop click at all remaining call sites (GAP-6).

Purpose: Close UAT gaps 4, 5, and 6 from Phase 16 user testing.
Output: Working image zoom, gold Start Deal button under seller card, RFQ modals that only close via Cancel/X.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-product-and-rfq-features/16-01-SUMMARY.md
@.planning/phases/16-product-and-rfq-features/16-02-SUMMARY.md
@.planning/phases/16-product-and-rfq-features/16-03-SUMMARY.md

<interfaces>
<!-- ProductImageZoom component at src/app/(main)/product/[productId]/ProductImageZoom.jsx
     Already has correct logic: onMouseMove tracks coordinates, zoom panel appears with
     background-image CSS. The zoom panel uses `hidden lg:block` for desktop-only.

     Likely failure cause: The parent container in ProductGallery has `overflow-hidden`
     on the glass-card div (line 30), and the zoom panel is positioned with
     `left-[calc(100%+12px)]` which places it OUTSIDE the overflow-hidden container,
     making it invisible. The container also has a fixed height of 500px.

     The ProductGallery wrapper div has `relative` and `overflow-hidden` which clips
     the absolutely-positioned zoom panel that extends beyond the container bounds. -->

<!-- ProductSellerCard at src/app/(main)/product/[productId]/ProductSellerCard.jsx
     Contains Contact Seller and View Profile buttons in a flex row.
     Currently rendered in the left column (under gallery).
     Start Deal button is in the right column (under product details).
     GAP-5 wants Start Deal positioned under Contact Seller and View Profile. -->

<!-- RFQ Modal call sites that still use onClose={() => setXxxOpen(false)} on backdrop:
     1. HeroSection.jsx line 252-253: onClose={() => setRequestModalOpen(false)}
     2. StrategicCTA.jsx line 131-132: onClose={() => setRequestModalOpen(false)}
     3. ProductsRequestsManager.jsx line 195: onClose={closeModal}
     (ProfileRequests.jsx and request/[requestId]/page.jsx are already fixed with onClose={() => {}}) -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix product image hover zoom and reposition Start Deal button</name>
  <files>src/app/(main)/product/[productId]/ProductImageZoom.jsx, src/app/(main)/product/[productId]/ProductGallery.jsx, src/app/(main)/product/[productId]/page.jsx</files>
  <action>
1. Fix ProductImageZoom visibility issue:
   - The root cause is that `ProductGallery` wraps the image in a div with `overflow-hidden` (line 30: `className="relative glass-card rounded-2xl overflow-hidden group"`). The zoom panel is absolutely positioned with `left-[calc(100%+12px)]` which places it outside this clipped container.
   - Fix approach: Move the zoom panel rendering OUT of the overflow-hidden container. In `ProductGallery.jsx`, restructure so the main image container is wrapped in an outer `relative` div WITHOUT `overflow-hidden`, and only the inner image area has `overflow-hidden`.
   - Specifically in `ProductGallery.jsx`:
     a. Wrap the main image `div` (line 30) in an outer container: `<div className="relative">`
     b. Keep the existing glass-card div with overflow-hidden for the image itself
     c. Move the `<ProductImageZoom>` rendering so the zoom PANEL (the 300x300 magnifier result) renders inside the outer container (not inside the overflow-hidden div)
   - Alternative simpler fix: Change ProductImageZoom so the zoom panel uses `fixed` or `portal` positioning instead of `absolute`. The simplest fix is to change the zoom panel from `absolute left-[calc(100%+12px)]` to use a CSS approach that escapes the overflow clip. The cleanest approach: add `overflow-visible` to the ProductImageZoom's own wrapper div, and change the glass-card container to NOT clip the zoom panel.
   - Recommended fix: In `ProductGallery.jsx` line 30, change `overflow-hidden` to `overflow-visible` and add `overflow-hidden` only to the inner image container. Or: set the glass-card div to have `overflow: visible` and clip only the img via a nested div.
   - Test by hovering over product image on desktop — zoom panel should appear to the right.

2. Reposition Start Deal button under seller card:
   - In `src/app/(main)/product/[productId]/page.jsx`, move the Start Deal button from the right column (lines 234-245) to the left column, after the `ProductSellerCard` component (after line 157).
   - Keep the exact same button JSX but place it inside the left column's `<div className="flex flex-col gap-4">` block.
   - The button already has gold styling (`bg-[#FFD700]`). Verify it has `!text-black` for contrast.
   - The condition `currentUser?.uid && !isOwnProduct` stays the same.
   - Remove the old Start Deal button JSX from the right column to avoid duplication.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Image hover zoom panel appears to the right of the product image on desktop. Start Deal button is positioned below Contact Seller and View Profile buttons in the left column with gold styling.</done>
</task>

<task type="auto">
  <name>Task 2: Prevent RFQ modal backdrop close at all remaining call sites</name>
  <files>src/presentation/components/homepage/Hero/HeroSection.jsx, src/presentation/components/homepage/StrategicCTA/StrategicCTA.jsx, src/presentation/components/features/admin/ProductsRequestsManager/ProductsRequestsManager.jsx</files>
  <action>
Fix the three remaining call sites where the RFQ/Request modal still closes on backdrop click. The pattern is the same as the already-fixed ProfileRequests.jsx: change `onClose` to a no-op `() => {}` on the Modal wrapping RequestForm.

1. In `src/presentation/components/homepage/Hero/HeroSection.jsx`:
   - Line ~252: Change `onClose={() => setRequestModalOpen(false)}` to `onClose={() => {}}`
   - The `onCancel` prop on `RequestForm` (line ~260) already handles explicit close via Cancel button — keep it as-is.
   - IMPORTANT: Do NOT change the Product modal's `onClose` — only the Request/RFQ modal.

2. In `src/presentation/components/homepage/StrategicCTA/StrategicCTA.jsx`:
   - Line ~132: Change `onClose={() => setRequestModalOpen(false)}` to `onClose={() => {}}`
   - Keep the `onCancel` prop on `RequestForm` (line ~139) — it already handles explicit close.
   - IMPORTANT: Do NOT change the Product modal's `onClose` — only the Request/RFQ modal.

3. In `src/presentation/components/features/admin/ProductsRequestsManager/ProductsRequestsManager.jsx`:
   - Line ~193-195: The Modal wraps BOTH product and request forms via `modalType` state. Since the Modal's `onClose` is `closeModal` which handles both types, we need a conditional approach.
   - Change line ~195 from `onClose={closeModal}` to `onClose={modalType === 'request' ? () => {} : closeModal}`
   - This prevents backdrop close only when the request/RFQ form is shown, while keeping backdrop close for the product form.
   - The `onCancel={closeModal}` props on both forms (lines ~250, ~259) already provide explicit close — keep them.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>RFQ/Request modals at all 5 call sites (HeroSection, StrategicCTA, ProductsRequestsManager, ProfileRequests, request detail page) only close via Cancel/X button, never via backdrop click.</done>
</task>

</tasks>

<verification>
- Hovering over product detail image on desktop shows magnified zoom panel to the right
- Start Deal button appears below Contact Seller / View Profile buttons with gold styling
- RFQ modal on homepage hero does not close on backdrop click
- RFQ modal on homepage StrategicCTA does not close on backdrop click
- RFQ modal in admin Products tab does not close on backdrop click
- Product modal in admin still closes on backdrop click (not affected)
- Build passes with zero errors
</verification>

<success_criteria>
- GAP-4 closed: Image hover zoom works on product detail page
- GAP-5 closed: Start Deal button has gold color and is positioned under seller action buttons
- GAP-6 closed: RFQ modal does not close on outside/backdrop click at any call site
</success_criteria>

<output>
After completion, create `.planning/phases/16-product-and-rfq-features/16-06-SUMMARY.md`
</output>
