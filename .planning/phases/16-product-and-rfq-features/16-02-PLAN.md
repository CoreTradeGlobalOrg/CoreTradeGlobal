---
phase: 16-product-and-rfq-features
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/product/ProductCategorySidebar/ProductCategorySidebar.jsx
  - src/app/(main)/products/page.jsx
  - src/app/(main)/product/[productId]/page.jsx
  - src/app/(main)/deals/new/page.jsx
autonomous: true
requirements:
  - PROD-03
  - PROD-08

must_haves:
  truths:
    - "User can browse products by category using a persistent left sidebar on the All Products page"
    - "User can search category names in the sidebar search box"
    - "User can click Start Deal on a product detail page and land on /deals/new with product pre-filled"
  artifacts:
    - path: "src/presentation/components/features/product/ProductCategorySidebar/ProductCategorySidebar.jsx"
      provides: "Category navigation sidebar with search"
      min_lines: 60
  key_links:
    - from: "src/presentation/components/features/product/ProductCategorySidebar/ProductCategorySidebar.jsx"
      to: "URL query params"
      via: "router.replace with categoryId param"
      pattern: "categoryId"
    - from: "src/app/(main)/product/[productId]/page.jsx"
      to: "/deals/new"
      via: "Start Deal button navigation"
      pattern: "deals/new.*productId.*sellerId"
    - from: "src/app/(main)/deals/new/page.jsx"
      to: "product + seller data"
      via: "query param reading for sellerId"
      pattern: "sellerId"
---

<objective>
Add product category sidebar to All Products page and "Start Deal" button on product detail page that navigates to deal creation with pre-filled product data.

Purpose: Category sidebar improves product discovery. Start Deal button enables direct deal initiation from product browsing without needing an existing conversation.
Output: ProductCategorySidebar component, updated products page layout, Start Deal button on product detail, extended /deals/new to accept sellerId param.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-product-and-rfq-features/16-CONTEXT.md
@.planning/phases/16-product-and-rfq-features/16-RESEARCH.md

@src/app/(main)/products/page.jsx
@src/app/(main)/product/[productId]/page.jsx
@src/app/(main)/deals/new/page.jsx
@src/data/repositories/CategoryRepository.js

<interfaces>
<!-- CategoryRepository methods -->
From src/data/repositories/CategoryRepository.js:
```javascript
getAll()
getParentCategories()
getSubCategories(parentId)
```

<!-- Products page uses URL query params for filtering -->
From src/app/(main)/products/page.jsx:
```javascript
// useSearchParams reads ?category= and ?categoryId= for filtering
```

<!-- deals/new currently requires conversationId + productId -->
From src/app/(main)/deals/new/page.jsx:
```javascript
// Reads conversationId and productId from searchParams
// Needs extension to support productId + sellerId without conversationId
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Product category sidebar with search and URL filtering</name>
  <files>
    src/presentation/components/features/product/ProductCategorySidebar/ProductCategorySidebar.jsx,
    src/app/(main)/products/page.jsx
  </files>
  <action>
1. Create `src/presentation/components/features/product/ProductCategorySidebar/ProductCategorySidebar.jsx`:
   - Fetch categories on mount using `CategoryRepository.getParentCategories()` and for each parent, `getSubCategories(parentId)`.
   - Render a sidebar with:
     - Search box at top (filters category names client-side as user types).
     - "All Products" item at top that clears the category filter.
     - Parent categories as bold section headers.
     - Sub-categories indented below each parent.
     - Active category highlighted with gold left border and `bg-[#FFD700]/10` or similar gold accent.
   - On category click: call `onCategorySelect(categoryId)` prop.
   - On mobile (`< lg` breakpoint): hide the sidebar entirely. The existing top search bar and category behavior remain for mobile users.
   - Styling: `w-56 flex-shrink-0` container, dark card background consistent with platform theme (`bg-[#1a2332]` or similar from existing card patterns), scrollable if categories overflow.

2. Update `src/app/(main)/products/page.jsx`:
   - Wrap the page content in a flex row: `<div className="flex gap-6">`.
   - Render `ProductCategorySidebar` on the left (hidden on mobile via `hidden lg:block`).
   - Move the existing product grid into `<div className="flex-1 min-w-0">`.
   - Wire `onCategorySelect` to update URL query params: set `?categoryId=X` via `router.replace`. When "All Products" is clicked, remove the categoryId param.
   - Reduce the grid max columns from 4 to 3 when sidebar is visible (the `flex-1` container is narrower). Use `xl:grid-cols-3` instead of `xl:grid-cols-4` for the grid container, or make it responsive to available width.
   - Pass the current `categoryId` from searchParams to the sidebar as `activeCategoryId` prop.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Category sidebar visible on desktop with parent/sub categories, search filtering, active highlight, and URL-based product filtering. Hidden on mobile.</done>
</task>

<task type="auto">
  <name>Task 2: Start Deal button on product detail and /deals/new sellerId support</name>
  <files>
    src/app/(main)/product/[productId]/page.jsx,
    src/app/(main)/deals/new/page.jsx
  </files>
  <action>
1. Update product detail page (`src/app/(main)/product/[productId]/page.jsx`):
   - Add a "Start Deal" button visible only when:
     - User is authenticated (use auth context).
     - User is NOT the product owner (`product.userId !== currentUser.uid`).
   - If user IS the owner, they see the existing Edit/Delete buttons (no change).
   - Button navigates to `/deals/new?productId=${product.id}&sellerId=${product.userId}`.
   - Style: gold accent button (`bg-[#FFD700] !text-black font-semibold`) consistent with platform CTA pattern.
   - Place the button prominently in the product info section (near price or below description).

2. Extend `/deals/new/page.jsx` to support the `sellerId` query param:
   - Currently the page reads `conversationId` and `productId` from searchParams and requires both.
   - Add `sellerId` param reading: `const sellerId = searchParams.get('sellerId')`.
   - Modify the validation guard: accept EITHER `(conversationId && productId)` OR `(productId && sellerId)` as valid entry conditions.
   - When `sellerId` is present but `conversationId` is absent:
     - Fetch the product by `productId` to get name, price, quantity for pre-fill (same as existing path).
     - Fetch the seller's user doc by `sellerId` to get seller name for display.
     - Skip any conversation-dependent logic. Set `conversationId` to null.
     - On form submit: pass `null` for conversationId to `createDeal` CF (or adjust the CF call to make conversationId optional). The deal can exist without an originating conversation.
   - Keep the existing `conversationId + productId` path fully intact — no regressions.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>Start Deal button on product detail page navigates to /deals/new with productId and sellerId. The deals/new page handles the new entry path, pre-filling product data without requiring a conversation.</done>
</task>

</tasks>

<verification>
- Visit /products — sidebar visible on desktop with categories, hidden on mobile
- Click a category — URL updates with categoryId, products filter
- Search categories in sidebar — list filters in real-time
- Visit /product/[id] as non-owner — "Start Deal" button visible
- Visit /product/[id] as owner — "Start Deal" button NOT visible, Edit/Delete shown
- Click "Start Deal" — navigates to /deals/new?productId=X&sellerId=Y with pre-filled form
</verification>

<success_criteria>
- Category sidebar shows parent/sub categories with search, active highlight, and URL filtering
- Products grid adjusts to 3 columns when sidebar is visible
- Start Deal button only visible to authenticated non-owners
- /deals/new page accepts productId+sellerId without conversationId and pre-fills product data
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-product-and-rfq-features/16-02-SUMMARY.md`
</output>
