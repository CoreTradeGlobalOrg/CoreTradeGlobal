---
phase: 16-product-and-rfq-features
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/request/RequestForm/RequestForm.jsx
  - src/presentation/components/features/request/QuotesSection/QuotesSection.jsx
autonomous: true
requirements:
  - PROD-06
  - PROD-07
  - PROD-10

must_haves:
  truths:
    - "User cannot accidentally close the RFQ creation modal by clicking outside it"
    - "User sees an explicit Cancel button inside the RFQ modal to dismiss it"
    - "User can see enhanced quote details with better formatting in the QuotesSection"
  artifacts:
    - path: "src/presentation/components/features/request/RequestForm/RequestForm.jsx"
      provides: "RFQ form with cancel button"
      contains: "onCancel"
    - path: "src/presentation/components/features/request/QuotesSection/QuotesSection.jsx"
      provides: "Enhanced quote details display"
      min_lines: 60
  key_links:
    - from: "RequestForm parent (requests page)"
      to: "Modal component"
      via: "disableBackdropClose prop or onClose={() => {}}"
      pattern: "onClose.*\\{\\}"
---

<objective>
Fix the RFQ creation modal to prevent accidental closing on outside click and enhance the QuotesSection with more detailed quote information and better formatting. Confirm PROD-10 (RFQ notifications) is already implemented.

Purpose: The modal fix prevents data loss during RFQ creation. The QuotesSection enhancement gives users better visibility into quote details.
Output: Updated RequestForm with cancel button, modal backdrop close prevention, enhanced QuotesSection.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-product-and-rfq-features/16-CONTEXT.md
@.planning/phases/16-product-and-rfq-features/16-RESEARCH.md

@src/presentation/components/features/request/RequestForm/RequestForm.jsx
@src/presentation/components/features/request/QuotesSection/QuotesSection.jsx

<interfaces>
<!-- Modal component backdrop click pattern -->
From research:
```javascript
// Modal currently has onClick={onClose} on backdrop div
// Fix: pass onClose={() => {}} at the call site for RFQ modal
// Do NOT change the global Modal component — other modals expect click-to-close
```

<!-- RequestForm props -->
From src/presentation/components/features/request/RequestForm/RequestForm.jsx:
```javascript
// Accepts onSubmit, onCancel props
// Rendered inside Modal on requests page
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: RFQ modal outside-click prevention and cancel button</name>
  <files>
    src/presentation/components/features/request/RequestForm/RequestForm.jsx
  </files>
  <action>
1. Find where RequestForm is rendered inside a Modal component (likely in the requests listing page or wherever the "Create RFQ" button lives).

2. At that call site, change the Modal's `onClose` prop to `() => {}` (no-op) to prevent backdrop click from closing. This is a targeted fix — do NOT modify the global Modal component, as other modals rely on backdrop-click-to-close behavior.

3. In `RequestForm.jsx`:
   - Ensure the form has an explicit "Cancel" button (secondary style, e.g., `bg-transparent border border-gray-500 text-gray-300`) alongside the Submit button.
   - The Cancel button calls `onCancel` prop (which the parent should wire to close the modal).
   - If `onCancel` prop is not currently accepted, add it.

4. The Modal should still have its X close button in the header (if it has one). The change only prevents the transparent backdrop area from triggering close.

5. Verify by reading the Modal component to understand how onClose is wired to the backdrop. The fix must be at the call site only.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>RFQ modal does not close on outside click. Explicit Cancel button inside the form closes it. Other modals in the app are unaffected.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance QuotesSection with richer quote details</name>
  <files>
    src/presentation/components/features/request/QuotesSection/QuotesSection.jsx
  </files>
  <action>
1. Read the existing `QuotesSection.jsx` to understand current quote display structure.

2. Enhance the quote display with additional fields and better formatting:
   - Add supplier/provider company name and country (if available in quote data).
   - Show quote validity period with visual countdown or expiry date.
   - Display price with currency formatting.
   - Show transport mode (for logistics) or coverage type (for insurance) more prominently.
   - Add any additional fields present in the quote data that are not currently displayed (e.g., terms, notes, timeline).
   - Use consistent card styling with the platform theme: dark card backgrounds, gold accents for key values, clear section headers.

3. Improve the visual formatting:
   - Use a grid or structured layout within each quote card (not just a flat list of text).
   - Add subtle dividers between quote sections.
   - Highlight the best quote (lowest price or best value) with a subtle gold border or badge.
   - Ensure the expand/collapse functionality still works smoothly.

4. Keep the existing layout structure (QuotesSection below RFQ details) — no layout change per user decision.

Note: PROD-10 (RFQ notifications) was already implemented in Phase 12 via the `onRFQCreated` Cloud Function. No work needed — this plan covers it for requirement traceability only.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>QuotesSection displays enriched quote information with better formatting, structured layout, and visual hierarchy. Existing expand/collapse works.</done>
</task>

</tasks>

<verification>
- Open RFQ creation modal — clicking outside does NOT close it
- Cancel button inside the modal closes it properly
- Other modals in the app still close on backdrop click (no regression)
- RFQ detail page shows QuotesSection with enhanced formatting and additional fields
</verification>

<success_criteria>
- RFQ modal backdrop click prevented at call site (not globally)
- Explicit Cancel button in RequestForm triggers modal close
- QuotesSection shows supplier info, validity, formatted pricing, and structured layout
- PROD-10 confirmed as already implemented (Phase 12 onRFQCreated CF)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/16-product-and-rfq-features/16-03-SUMMARY.md`
</output>
