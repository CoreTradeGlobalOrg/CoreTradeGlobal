---
phase: 02-deal-creation-and-negotiation-s1
plan: "03"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(main)/deals/[dealId]/page.jsx
  - src/presentation/components/features/deal/DealPage/DealPage.jsx
  - src/presentation/components/features/deal/ProductHero/ProductHero.jsx
  - src/presentation/components/features/deal/OfferTimeline/OfferTimeline.jsx
  - src/presentation/components/features/deal/OfferCard/OfferCard.jsx
  - src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx
  - src/presentation/components/features/deal/DealSidebar/DealSidebar.jsx
  - src/presentation/components/features/deal/CountdownTimer/CountdownTimer.jsx
  - src/presentation/hooks/deal/useDeal.js
  - src/presentation/hooks/deal/useDealActions.js
autonomous: true
requirements:
  - NEGO-01
  - NEGO-03
  - NEGO-04

must_haves:
  truths:
    - "Both parties see the full offer history timeline with all rounds, timestamps, amounts, and terms — in real-time"
    - "Changed fields between rounds are visually highlighted in offer cards"
    - "Counter-offer form is visible only when it is the current user's turn to respond"
    - "The counter-offer form pre-fills from the most recent offer"
    - "Countdown timer on the deal page shows time remaining and changes color when running low"
    - "Right sidebar shows party info, progress tracker, and current offer summary"
    - "Older offer rounds (5+) collapse with 'Show earlier offers' toggle"
  artifacts:
    - path: "src/app/(main)/deals/[dealId]/page.jsx"
      provides: "Deal page route"
      contains: "DealPage"
    - path: "src/presentation/components/features/deal/DealPage/DealPage.jsx"
      provides: "Main deal layout orchestrating all sub-components"
      contains: "OfferTimeline"
    - path: "src/presentation/components/features/deal/OfferTimeline/OfferTimeline.jsx"
      provides: "Card-based offer history with buyer/seller/system card types"
      contains: "OfferCard"
    - path: "src/presentation/components/features/deal/OfferCard/OfferCard.jsx"
      provides: "Single offer card with color accent (green=buyer, gold=seller, dashed=system)"
      contains: "estimatedTotal"
    - path: "src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx"
      provides: "Form for counter-offers, pre-filled from latest offer"
      contains: "useForm"
    - path: "src/presentation/components/features/deal/DealSidebar/DealSidebar.jsx"
      provides: "Party info cards, progress tracker, current offer summary"
      contains: "progress"
    - path: "src/presentation/hooks/deal/useDeal.js"
      provides: "Real-time subscription to deal + offers"
      contains: "onSnapshot"
    - path: "src/presentation/hooks/deal/useDealActions.js"
      provides: "CF calls for accept, reject, withdraw, counter-offer"
      contains: "httpsCallable"
  key_links:
    - from: "src/presentation/hooks/deal/useDeal.js"
      to: "deals/{dealId} + deals/{dealId}/offers"
      via: "DealRepository.subscribeToDeal + OfferRepository.subscribeToOffers"
      pattern: "subscribeToDeal.*subscribeToOffers"
    - from: "src/presentation/hooks/deal/useDealActions.js"
      to: "functions/index.js"
      via: "httpsCallable for submitCounterOffer, acceptOffer, rejectOffer, withdrawOffer"
      pattern: "httpsCallable.*acceptOffer|rejectOffer|withdrawOffer|submitCounterOffer"
    - from: "src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx"
      to: "useDealActions"
      via: "submitCounterOffer call on form submit"
      pattern: "submitCounterOffer.*dealId"
---

<objective>
Build the complete deal negotiation page with offer timeline, counter-offer form, sidebar, and real-time updates.

Purpose: This is the core negotiation experience — where buyers and sellers exchange offers, view history, and make decisions. It delivers the offer timeline (NEGO-03), real-time updates (NEGO-04), and the interactive counter-offer flow (NEGO-01).

Output: Deal page at /deals/[dealId] with ProductHero, OfferTimeline (color-coded cards with changed-field highlighting), CounterOfferForm (turn-based), DealSidebar (party info + progress + summary), CountdownTimer, and useDeal/useDealActions hooks.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-CONTEXT.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-RESEARCH.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-01-SUMMARY.md
@src/core/constants/dealConstants.js
@src/core/constants/incoterms.js
@src/core/validation/offerSchema.js
@src/domain/entities/Deal.js
@src/domain/entities/Offer.js
@src/data/repositories/DealRepository.js
@src/data/repositories/OfferRepository.js
@src/core/di/container.js
@src/core/config/firebase.config.js
@src/presentation/contexts/MessagesContext.jsx
@other_items/S1-teklif,muzakere-.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deal page layout with offer timeline, counter-offer form, sidebar, and countdown timer</name>
  <files>
    src/app/(main)/deals/[dealId]/page.jsx
    src/presentation/components/features/deal/DealPage/DealPage.jsx
    src/presentation/components/features/deal/ProductHero/ProductHero.jsx
    src/presentation/components/features/deal/OfferTimeline/OfferTimeline.jsx
    src/presentation/components/features/deal/OfferCard/OfferCard.jsx
    src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx
    src/presentation/components/features/deal/DealSidebar/DealSidebar.jsx
    src/presentation/components/features/deal/CountdownTimer/CountdownTimer.jsx
    src/presentation/hooks/deal/useDeal.js
    src/presentation/hooks/deal/useDealActions.js
  </files>
  <action>
    **1. Create `src/presentation/hooks/deal/useDeal.js`:**
    - Returns `{ deal, offers, loading, error }`
    - Takes `dealId` param
    - Gets DealRepository and OfferRepository from DI container
    - Sets up two parallel `onSnapshot` subscriptions: one on deal doc, one on offers subcollection (ordered by round asc)
    - Maps Firestore snapshots to Deal and Offer entities
    - Unsubscribes both on unmount
    - Follow the pattern from 02-RESEARCH.md Pattern 3 (mirrors MessagesContext)

    **2. Create `src/presentation/hooks/deal/useDealActions.js`:**
    - Returns `{ submitCounterOffer, acceptOffer, rejectOffer, withdrawOffer, loading, error }`
    - Each action calls the corresponding Cloud Function via `httpsCallable(functions, 'functionName')`
    - `submitCounterOffer(dealId, offerData, expectedRound)` — calls CF with offer data
    - `acceptOffer(dealId, offerId)` — calls CF
    - `rejectOffer(dealId, offerId)` — calls CF
    - `withdrawOffer(dealId, offerId)` — calls CF
    - Each action sets loading true during call, catches errors and sets error state
    - Use toast notifications on success ("Offer accepted!" etc.) and error ("Failed to accept offer: ...") using react-hot-toast (existing pattern)

    **3. Create `src/app/(main)/deals/[dealId]/page.jsx`:**
    - 'use client' page reading `params.dealId`
    - Auth-protected: redirect if not authenticated
    - Access check: if deal loads and user is neither buyer nor seller, show "Access denied" or redirect to /deals
    - Uses `useDeal(dealId)` hook
    - Renders `<DealPage>` component passing deal, offers, and actions
    - Loading skeleton while deal loads
    - 404 handling if deal not found

    **4. Create `src/presentation/components/features/deal/DealPage/DealPage.jsx`:**
    - Main layout component orchestrating all sub-components
    - Desktop: two-column layout — main content (left, ~70%) + sidebar (right, ~30%)
    - Mobile responsive: panels stack vertically (per user decision)
    - Structure:
      - `<ProductHero>` at top spanning full width
      - `<CountdownTimer>` below hero (per user decision: on deal page, not navbar)
      - Main column: `<OfferTimeline>` + `<CounterOfferForm>` (or "Waiting..." message)
      - Right sidebar: `<DealSidebar>`
    - If deal is in terminal state (accepted/rejected/expired/withdrawn), show status banner and hide counter-offer form
    - Pass `useDealActions` to form and action buttons

    **5. Create `src/presentation/components/features/deal/ProductHero/ProductHero.jsx`:**
    - Shows product context at the top of the deal page
    - Product image (from deal.productImage), product name, category badge
    - Link to product detail page: `/product/${deal.productId}`
    - Compact design — hero section, not a full product card

    **6. Create `src/presentation/components/features/deal/OfferTimeline/OfferTimeline.jsx`:**
    - Takes `offers` array and `deal` object as props
    - Renders offer cards in chronological order (round ascending)
    - **Collapse logic (per user decision):** If more than 4 offers, show only last 3 expanded. Earlier offers collapsed behind a "Show earlier offers (N)" toggle button. Toggle reveals all.
    - **System event cards:** For deal status changes (deal started, expired, withdrawn) — these are rendered inline in the timeline based on deal status and timestamps. System cards have dashed border per mockup. "Deal Initiated" card at round 0 (before first offer).
    - **Terminal state card:** When deal is accepted/rejected/expired, show as the final card in timeline with appropriate styling (green for accepted, red for rejected, gray for expired)
    - Each offer rendered as `<OfferCard>`
    - **Changed field highlighting:** Compare each offer to the previous offer. For any field that changed (price, quantity, incoterm, etc.), pass a `changedFields` array to the OfferCard. Visual indication: colored badge or arrow showing old->new value.

    **7. Create `src/presentation/components/features/deal/OfferCard/OfferCard.jsx`:**
    - Takes `offer`, `previousOffer` (for diff), `currentUserUid`, `isLatest` props
    - Color accent based on `offer.role`: buyer = green accent border/header, seller = gold accent border/header (per user decision, matching brand)
    - System event cards: dashed border, neutral color
    - Content: Round number, submitted by (party name), timestamp, all offer terms (price, qty, unit, incoterm + named place, delivery deadline, payment terms, insurance preference, notes)
    - **Estimated total** displayed: `price x quantity = total` (per user decision)
    - **Changed fields highlighting:** For each field in `changedFields`, show with a highlight color, arrow, or badge indicating it changed from the previous round's value
    - If `isLatest` and deal is negotiating: show action buttons for the receiver (Accept / Reject) and for the sender (Withdraw)
    - Action buttons call `useDealActions` methods
    - Action buttons only visible to the appropriate party (accept/reject for receiver = currentTurnUid, withdraw for sender = submittedBy)

    **8. Create `src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx`:**
    - Only rendered when `deal.currentTurnUid === currentUser.uid` AND `deal.status === 'negotiating'` (per user decision: form visible only when it's the current user's turn)
    - Pre-fills ALL fields from the most recent offer (per user decision: party modifies only what they want to change)
    - Uses `react-hook-form` with `offerSchema` validation
    - Same field layout as DealForm but labeled "Counter-Offer" and submit button says "Submit Counter-Offer"
    - Include the Incoterms pill selector and named place autocomplete (reuse `<IncotermsSelector>` and `<NamedPlaceInput>` from Plan 02)
    - On submit: calls `submitCounterOffer(dealId, formData, deal.round)` from useDealActions
    - Loading spinner on submit button
    - When NOT the user's turn: show "Waiting for [otherParty name] to respond..." message with timestamp of when the last offer was submitted

    **9. Create `src/presentation/components/features/deal/DealSidebar/DealSidebar.jsx`:**
    - Always visible on the right side (per user decision)
    - Three sections:

    **Party Info Cards:**
    - Two cards (buyer + seller) showing: company name, location (country), "Member since" date, verified badge if applicable
    - Highlight the current user's card
    - Fetch user data for both parties (use UserRepository from DI container)

    **Progress Tracker:**
    - Vertical step indicator: Negotiation -> Agreement -> Quotes -> Tracking
    - "Negotiation" step is active/current for this phase
    - Other steps shown as upcoming/locked (Phase 3-6 content)
    - Visual: colored circle for active, gray circles for future steps, connecting lines

    **Current Offer Summary:**
    - Latest terms at a glance: price x qty = total, Incoterm, named place, payment terms
    - Round number: "Round N"
    - Whose turn it is

    **10. Create `src/presentation/components/features/deal/CountdownTimer/CountdownTimer.jsx`:**
    - Takes `expiresAt` (Firestore Timestamp) as prop
    - Live countdown updating every second (use `setInterval` or `requestAnimationFrame`)
    - Display format: "Xd Xh Xm Xs remaining" or "Xh Xm Xs" if < 24h
    - **Color change (per user decision):** Normal = neutral/green, < 4h = yellow/warning, < 1h = red/urgent
    - When expired: show "Expired" badge in red
    - Clean up interval on unmount
  </action>
  <verify>
    - `/deals/[dealId]` page renders without errors with mock/real data
    - `useDeal` hook subscribes to both deal and offers collections
    - `useDealActions` calls all 4 Cloud Functions correctly
    - OfferTimeline renders cards in round order with correct color coding (buyer=green, seller=gold)
    - CounterOfferForm pre-fills from latest offer and only shows on user's turn
    - CountdownTimer counts down and changes color at thresholds
    - DealSidebar shows party info, progress tracker, and current summary
    - Collapsed offers toggle works for 5+ rounds
    - Changed fields are visually highlighted between rounds
    - Mobile responsive: panels stack vertically on small screens
  </verify>
  <done>
    - Deal page at /deals/[dealId] shows complete negotiation view
    - ProductHero shows product context with image, name, category
    - OfferTimeline shows all rounds with buyer (green) / seller (gold) / system (dashed) cards
    - Changed fields between rounds visually highlighted
    - Older rounds (5+) collapsed with toggle
    - CounterOfferForm visible only on user's turn, pre-filled from latest offer
    - "Waiting for [Party]..." message when not user's turn
    - Accept/Reject buttons on latest offer for receiver, Withdraw button for sender
    - DealSidebar with party info, progress tracker (negotiation active), current offer summary
    - CountdownTimer with color change at 4h and 1h thresholds
    - All data real-time via Firestore onSnapshot subscriptions
  </done>
</task>

<task type="auto">
  <name>Task 2: Online presence indicator and notification sound</name>
  <files>
    src/presentation/components/features/deal/DealPage/DealPage.jsx
    src/presentation/hooks/deal/useDealPresence.js
  </files>
  <action>
    **1. Create `src/presentation/hooks/deal/useDealPresence.js`:**
    - Implements the Firestore heartbeat pattern from 02-RESEARCH.md Pattern 7
    - On mount: writes `{ viewingDealId: dealId, viewingDealSince: serverTimestamp() }` to `users/{uid}` doc
    - Sets up a heartbeat interval (every 30 seconds) to update `viewingDealSince`
    - On unmount: writes `{ viewingDealId: null, viewingDealSince: null }` to clean up
    - Also subscribes to the OTHER participant's user doc to check if they're viewing the deal
    - Returns `{ otherPartyViewing }` boolean (true if other party's `viewingDealId === dealId` AND `viewingDealSince` is recent, i.e. < 60 seconds)
    - Note: The heartbeat approach has a known limitation — if browser crashes, cleanup won't run. The CF-side check in Plan 04 uses the 60-second staleness window to handle this.

    **2. Update `DealPage.jsx` to show presence indicator:**
    - Use `useDealPresence(dealId)` hook
    - Show a small green dot + "[Party name] is viewing" indicator near the party info or in the sidebar
    - When other party is not viewing, hide the indicator (don't show "offline")

    **3. Add notification sound on new offer arrival:**
    - In `useDeal.js` or `DealPage.jsx`, detect when a new offer appears in the offers array (compare previous length to current)
    - When a new offer is detected AND the offer was NOT submitted by the current user: play a subtle notification sound
    - Use the Web Audio API for a simple beep/chime, or a small .mp3 file in `/public/sounds/` (Claude's discretion on exact sound — a simple short chime is appropriate)
    - Only play if the document is visible (use `document.hidden` check to avoid playing when tab is in background)
    - Keep the sound subtle and brief
  </action>
  <verify>
    - Presence indicator shows when another user is viewing the same deal (test with two browser tabs/users)
    - Heartbeat writes to Firestore every 30 seconds
    - Cleanup runs on unmount (viewingDealId set to null)
    - Notification sound plays when a new offer from the other party arrives
    - Sound does not play for own offers or when tab is hidden
  </verify>
  <done>
    - Green dot + "[Name] is viewing" shows when other party is on the deal page
    - Heartbeat pattern keeps presence fresh (30s interval)
    - Presence cleans up on page leave
    - Subtle notification sound plays when new offer arrives from other party
    - Sound respects tab visibility (no sound when tab is hidden)
  </done>
</task>

</tasks>

<verification>
1. Deal page renders correctly with product hero, timeline, sidebar, and countdown
2. Offer cards show correct color coding (buyer=green, seller=gold, system=dashed)
3. Changed fields between rounds are visually highlighted
4. Counter-offer form only appears on the correct party's turn
5. Counter-offer pre-fills from latest offer
6. Accept/Reject/Withdraw buttons appear for correct parties only
7. Real-time: new offers appear in timeline automatically (no refresh)
8. Countdown timer counts down and changes color at thresholds
9. Presence indicator works between two users
10. Notification sound plays on new offer from other party
</verification>

<success_criteria>
- A buyer/seller can view the complete negotiation history in real-time on the deal page
- Changed fields between rounds are clearly visible
- The counter-offer form appears only for the right party at the right time
- The sidebar provides useful context (party info, progress, current summary) at all times
- The countdown timer provides clear visual urgency as expiry approaches
- Online presence indicator shows when the other party is actively viewing
- All UI adapts to mobile viewport (panels stack vertically)
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-creation-and-negotiation-s1/02-03-SUMMARY.md`
</output>
