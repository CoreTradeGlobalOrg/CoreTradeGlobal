---
phase: 02-deal-creation-and-negotiation-s1
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/hooks/deal/useDealActions.js
autonomous: true
gap_closure: true
requirements:
  - NEGO-01
  - NEGO-02
  - NEGO-03
  - NEGO-04
  - NEGO-05
  - NEGO-06
  - NEGO-07

must_haves:
  truths:
    - "Counter-offer submissions reach the Cloud Function with offer terms intact (not undefined)"
    - "The seller can submit a counter-offer and both parties see updated offer timeline"
  artifacts:
    - path: "src/presentation/hooks/deal/useDealActions.js"
      provides: "Correctly named parameter in submitCounterOffer httpsCallable call"
      contains: "fn({ dealId, offer: offerData, expectedRound })"
  key_links:
    - from: "src/presentation/hooks/deal/useDealActions.js"
      to: "functions/index.js (submitCounterOffer)"
      via: "httpsCallable with { dealId, offer, expectedRound }"
      pattern: "offer: offerData"
---

<objective>
Fix the parameter name mismatch between the client-side `useDealActions.submitCounterOffer` hook and the `submitCounterOffer` Cloud Function that causes all counter-offers to fail.

Purpose: The client sends `{ dealId, offerData, expectedRound }` but the CF destructures `{ dealId, offer, expectedRound }` from `request.data`. The CF receives `offer = undefined` and immediately throws `invalid-argument`. This blocks Success Criterion 2 (counter-offer exchange).

Output: Working counter-offer flow where offer terms are correctly transmitted from client to Cloud Function.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-VERIFICATION.md
@src/presentation/hooks/deal/useDealActions.js
@functions/index.js (line 1078: const { dealId, offer, expectedRound } = request.data)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix submitCounterOffer parameter name mismatch</name>
  <files>src/presentation/hooks/deal/useDealActions.js</files>
  <action>
In `src/presentation/hooks/deal/useDealActions.js`, line 47, change:

```javascript
await fn({ dealId, offerData, expectedRound });
```

to:

```javascript
await fn({ dealId, offer: offerData, expectedRound });
```

This renames the `offerData` key to `offer` in the httpsCallable payload so it matches the Cloud Function's expected parameter name at `functions/index.js` line 1078: `const { dealId, offer, expectedRound } = request.data`.

Do NOT change the function parameter name `offerData` in the function signature on line 42 — only change the key name in the object passed to `fn()`. The local variable stays `offerData` for clarity; the wire format key becomes `offer`.

Do NOT modify `functions/index.js` — the CF already uses the correct key name `offer`.
  </action>
  <verify>
1. Read `src/presentation/hooks/deal/useDealActions.js` and confirm line 47 now reads `await fn({ dealId, offer: offerData, expectedRound });`
2. Run `grep -n "offerData" src/presentation/hooks/deal/useDealActions.js` — should show the parameter on line 42 and the renamed key on line 47 (as `offer: offerData`)
3. Run `grep -n "offer," functions/index.js | head -5` — confirm CF still reads `offer` from `request.data` (unchanged)
  </verify>
  <done>
The httpsCallable call in useDealActions.js sends `{ dealId, offer: offerData, expectedRound }` matching the Cloud Function's expected `{ dealId, offer, expectedRound }` destructuring. Counter-offer submissions will now transmit offer terms correctly to the server.
  </done>
</task>

</tasks>

<verification>
1. `src/presentation/hooks/deal/useDealActions.js` line 47 contains `offer: offerData` (not bare `offerData`)
2. `functions/index.js` line 1078 still reads `const { dealId, offer, expectedRound } = request.data` (unchanged)
3. The key names match across client and server: both use `offer` as the wire key
4. No other files reference `offerData` as a wire key to Cloud Functions (this was the only call site)
</verification>

<success_criteria>
- The parameter name mismatch between useDealActions.js and the submitCounterOffer Cloud Function is resolved
- Counter-offer data will arrive at the CF as `request.data.offer` (not `undefined`)
- Success Criterion 2 from VERIFICATION.md is unblocked: "The seller can submit a counter-offer modifying any terms — both parties see the full offer history timeline"
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-creation-and-negotiation-s1/02-07-SUMMARY.md`
</output>
