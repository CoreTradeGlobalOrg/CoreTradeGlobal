---
phase: 02-deal-creation-and-negotiation-s1
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/app/(main)/messages/[conversationId]/page.jsx
  - src/app/(main)/deals/new/page.jsx
  - src/presentation/components/features/deal/DealForm/DealForm.jsx
  - src/presentation/components/features/deal/IncotermsSelector/IncotermsSelector.jsx
  - src/presentation/components/features/deal/NamedPlaceInput/NamedPlaceInput.jsx
  - src/presentation/hooks/deal/useCreateDeal.js
  - src/app/(main)/deals/page.jsx
  - src/presentation/components/features/deal/DealList/DealList.jsx
  - src/presentation/components/features/deal/DealCard/DealCard.jsx
  - src/presentation/hooks/deal/useDeals.js
autonomous: true
requirements:
  - NEGO-01
  - NEGO-02
  - NEGO-04

must_haves:
  truths:
    - "A user can click 'Initiate Deal' in a product-based chat and land on a deal creation page with product context pre-filled"
    - "The offer form includes all 11 Incoterms 2020 as selectable pills with tooltips and dynamically changing named place labels"
    - "Named place input provides autocomplete from UN/LOCODE dataset"
    - "Submitting the form calls createDeal CF and navigates to the deal page"
    - "A user can view all their deals (as buyer or seller) on the My Deals page with real-time status updates"
  artifacts:
    - path: "src/app/(main)/deals/new/page.jsx"
      provides: "Deal creation page with pre-fill from product"
      contains: "DealForm"
    - path: "src/presentation/components/features/deal/DealForm/DealForm.jsx"
      provides: "Full offer form with all fields from CONTEXT.md"
      contains: "useForm"
    - path: "src/presentation/components/features/deal/IncotermsSelector/IncotermsSelector.jsx"
      provides: "Selectable pills for all 11 Incoterms with tooltips"
      contains: "INCOTERMS_2020"
    - path: "src/presentation/components/features/deal/NamedPlaceInput/NamedPlaceInput.jsx"
      provides: "Autocomplete input using UN/LOCODE dataset"
      contains: "locode"
    - path: "src/app/(main)/deals/page.jsx"
      provides: "My Deals list page"
      contains: "DealList"
    - path: "src/presentation/hooks/deal/useDeals.js"
      provides: "Real-time subscription hook for user's deals"
      contains: "subscribeToDeals"
  key_links:
    - from: "src/app/(main)/messages/[conversationId]/page.jsx"
      to: "/deals/new?conversationId=...&productId=..."
      via: "Link component with query params"
      pattern: "Initiate Deal.*deals/new"
    - from: "src/presentation/hooks/deal/useCreateDeal.js"
      to: "functions/index.js (createDeal)"
      via: "httpsCallable"
      pattern: "httpsCallable.*createDeal"
    - from: "src/presentation/hooks/deal/useDeals.js"
      to: "src/data/repositories/DealRepository.js"
      via: "subscribeToDeals from DI container"
      pattern: "getDealRepository.*subscribeToDeals"
---

<objective>
Build the deal creation flow (from chat to deal) and the My Deals list page with real-time updates.

Purpose: Users can initiate deals from product conversations with a full Incoterms-aware offer form, and track all their active deals in a list view. This delivers the entry point for the entire negotiation workflow.

Output: Deal creation page with Incoterms pills + UN/LOCODE autocomplete, "Initiate Deal" button in chat, My Deals list page with live status.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-CONTEXT.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-RESEARCH.md
@.planning/phases/02-deal-creation-and-negotiation-s1/02-01-SUMMARY.md
@src/app/(main)/messages/[conversationId]/page.jsx
@src/app/(main)/messages/[conversationId]/conversation.css
@src/presentation/components/features/product/ProductForm/ProductForm.jsx
@src/presentation/hooks/product/useCreateProduct.js
@src/core/config/firebase.config.js
@src/core/di/container.js
@src/core/constants/dealConstants.js
@src/core/constants/incoterms.js
@src/core/constants/currencies.js
@src/core/validation/offerSchema.js
@src/domain/entities/Deal.js
@src/data/repositories/DealRepository.js
@other_items/S1-teklif,muzakere-.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initiate Deal button in chat + Deal creation page with full offer form</name>
  <files>
    src/app/(main)/messages/[conversationId]/page.jsx
    src/app/(main)/deals/new/page.jsx
    src/presentation/components/features/deal/DealForm/DealForm.jsx
    src/presentation/components/features/deal/IncotermsSelector/IncotermsSelector.jsx
    src/presentation/components/features/deal/NamedPlaceInput/NamedPlaceInput.jsx
    src/presentation/hooks/deal/useCreateDeal.js
  </files>
  <action>
    **1. Add "Initiate Deal" button to `src/app/(main)/messages/[conversationId]/page.jsx`:**
    - In the conversation header area (near the top where user name / back arrow are), add an "Initiate Deal" Link button
    - Only show when: `conversation.metadata?.productId && conversation.type === 'direct'`
    - Links to `/deals/new?conversationId=${conversationId}&productId=${conversation.metadata.productId}`
    - Use Lucide `Handshake` icon (or similar trade icon) + text "Initiate Deal"
    - Style as a secondary/outline button matching existing UI patterns (check existing button styles in the page)

    **2. Create `src/presentation/hooks/deal/useCreateDeal.js`:**
    - Follow useCreateProduct.js pattern
    - Returns `{ createDeal, loading, error }`
    - `createDeal(data)` calls `httpsCallable(functions, 'createDeal')` with `{ conversationId, productId, initialOffer: data }`
    - On success, navigates to `/deals/${result.data.dealId}`
    - Uses `functions` from firebase.config.js (same pattern as existing CF calls like inviteUser)

    **3. Create `src/app/(main)/deals/new/page.jsx`:**
    - 'use client' page that reads `searchParams` for `conversationId` and `productId`
    - Fetches product data using ProductRepository from DI container to pre-fill form
    - Fetches conversation to show context (who you're dealing with)
    - Shows a header: "Create New Deal" with product name and other party's company name
    - Renders `<DealForm>` component with pre-filled data from product (price, currency, default quantity)
    - Auth-protected: redirect to login if not authenticated
    - Loading state while fetching product data

    **4. Create `src/presentation/components/features/deal/DealForm/DealForm.jsx`:**
    - Use `react-hook-form` with Zod resolver and `offerSchema` from Plan 01
    - Pre-fill from product: price (product.price), currency (product.currency or default), quantity (1)
    - Fields layout (adapt mockup to existing design system per user decision):
      - **Row 1:** Price per unit (number input) + Currency selector (dropdown from currencies.js) + Conversion rate (shown only if currency differs from product base currency)
      - **Row 2:** Quantity (number input) + Unit selector (dropdown: kg, ton, pieces, metre, m2, containers from dealConstants.js)
      - **Row 3:** Incoterms selector (`<IncotermsSelector>` component — pill-based)
      - **Row 4:** Named place (`<NamedPlaceInput>` component — autocomplete, label changes based on selected Incoterm)
      - **Row 5:** Delivery deadline (date picker, must be future date) + Payment terms (dropdown: Cash, 30-Day, 60-Day, 90-Day, LC, DAP)
      - **Row 6:** Insurance preference (radio group: Seller Provides / Buyer Provides / No Insurance — auto-set based on Incoterm with note)
      - **Row 7:** Offer expiry (number input defaulting to 72 hours, with label "Your offer expires in X hours")
      - **Row 8:** Notes (textarea, optional, max 2000 chars)
      - **Row 9:** File attachments (optional — use existing Firebase Storage upload pattern if available, or a simple file input that stores URLs. Follow FirebaseStorageDataSource pattern for uploads to `deals/{dealId}/attachments/`)
      - **Estimated Total:** Live calculation displayed = price * quantity
    - Submit button: "Submit Offer" with loading spinner while createDeal CF runs
    - Insurance preference auto-sets based on selected Incoterm (e.g., CIF -> seller_provides) with an explanatory note like "CIF requires seller to provide insurance"
    - Validation errors shown inline per field (react-hook-form pattern)

    **5. Create `src/presentation/components/features/deal/IncotermsSelector/IncotermsSelector.jsx`:**
    - Import `INCOTERMS_2020` from constants
    - Render all 11 terms as selectable pill buttons in a flex-wrap container
    - Selected pill has filled/active style (primary color), unselected has outline style
    - Each pill shows the code (e.g., "FOB") with a tooltip on hover showing the full label + description
    - Use existing tooltip pattern if available, otherwise a simple CSS tooltip or title attribute
    - On selection, call `onChange(code)` prop to update form value
    - Controlled component: `value` prop determines which pill is active

    **6. Create `src/presentation/components/features/deal/NamedPlaceInput/NamedPlaceInput.jsx`:**
    - Install `@geoapify/un-locode` in the Next.js app (not functions/) per research
    - Import UN/LOCODE dataset and filter as user types
    - Show autocomplete dropdown with matching locations (city, country, LOCODE)
    - Limit results to top 10 matches for performance
    - Label and placeholder change dynamically based on selected Incoterm (passed via props): use `namedPlaceLabel` and `namedPlacePlaceholder` from the INCOTERMS_2020 entry
    - Controlled input with `value` and `onChange` props
    - Allow freeform text if no match selected (users may type custom locations)
    - Debounce filtering to 200ms for smooth typing experience
  </action>
  <verify>
    - `npm run build` passes (or at least `next lint` passes on the new files)
    - Grep "Initiate Deal" in messages/[conversationId]/page.jsx confirms button added
    - Grep "createDeal" in useCreateDeal.js confirms httpsCallable integration
    - Grep "INCOTERMS_2020" in IncotermsSelector.jsx confirms all 11 terms imported
    - Grep "offerSchema" in DealForm.jsx confirms Zod validation connected
    - The /deals/new page renders without errors when accessed with valid query params
  </verify>
  <done>
    - "Initiate Deal" button visible in product-based chat conversations, links to /deals/new with conversationId and productId
    - /deals/new page fetches product data and pre-fills the offer form
    - DealForm has all required fields: price, quantity, unit, currency, conversionRate, Incoterms (pill selector), named place (autocomplete), delivery deadline, payment terms, insurance preference (auto-set by Incoterm), expiry hours, notes, attachments
    - IncotermsSelector renders 11 pills with tooltips
    - NamedPlaceInput provides autocomplete from UN/LOCODE
    - Form submission calls createDeal CF and navigates to deal page on success
    - Estimated total (price * quantity) displayed live
  </done>
</task>

<task type="auto">
  <name>Task 2: My Deals list page with real-time status updates</name>
  <files>
    src/app/(main)/deals/page.jsx
    src/presentation/components/features/deal/DealList/DealList.jsx
    src/presentation/components/features/deal/DealCard/DealCard.jsx
    src/presentation/hooks/deal/useDeals.js
  </files>
  <action>
    **1. Create `src/presentation/hooks/deal/useDeals.js`:**
    - Returns `{ deals, loading, error }`
    - Gets DealRepository from DI container
    - Calls `dealRepository.subscribeToDeals(user.uid, callback)` for real-time updates
    - Unsubscribes on unmount
    - Depends on `useAuth()` for user.uid
    - Sorts merged deals by updatedAt descending

    **2. Create `src/app/(main)/deals/page.jsx`:**
    - 'use client' page
    - Auth-protected: redirect to login if not authenticated
    - Page title: "My Deals"
    - Uses `useDeals()` hook to get real-time deal list
    - Optional tab filtering: "All", "Active" (negotiating), "Completed" (accepted/rejected/expired/withdrawn)
    - Empty state: "No deals yet. Start a deal from any product conversation." with link to /messages
    - Renders `<DealList>` component
    - Loading skeleton while deals load

    **3. Create `src/presentation/components/features/deal/DealList/DealList.jsx`:**
    - Takes `deals` array prop
    - Maps deals to `<DealCard>` components
    - Grid or list layout (adapt to existing product list patterns — probably a card grid)

    **4. Create `src/presentation/components/features/deal/DealCard/DealCard.jsx`:**
    - Takes a Deal entity as prop
    - Shows: product image (thumbnail), product name, other party's name/company, current status badge (color-coded: green for negotiating, blue for accepted, red for rejected, gray for expired/withdrawn), latest offer summary (price x quantity = total, Incoterm, round number), last updated timestamp
    - Click navigates to `/deals/${deal.id}`
    - Indicate whose turn it is: "Your turn" badge if currentTurnUid matches current user, "Waiting for [Party]" otherwise
    - Use existing card patterns from ProductCard or similar components in the codebase

    **Add navigation link:**
    - The navbar likely already has deal-related navigation from Phase 1 patterns. If not, add a "My Deals" link to the navbar for members. Check existing Navbar component and add to member-visible links if missing. Use Lucide `Handshake` or `FileText` icon.
  </action>
  <verify>
    - `/deals` page renders without errors
    - `useDeals` hook subscribes and unsubscribes properly (check for cleanup in useEffect)
    - DealCard shows all required info: product, parties, status, turn indicator, latest terms
    - Real-time: when a deal updates in Firestore, the list reflects the change without page refresh
  </verify>
  <done>
    - My Deals page at /deals shows all deals where current user is buyer or seller
    - Deals update in real-time via Firestore listeners (NEGO-04)
    - Each deal card shows product context, status, latest offer summary, and turn indicator
    - Tab filtering for active vs completed deals
    - Empty state for users with no deals
    - Navigation link in navbar (if not already present)
  </done>
</task>

</tasks>

<verification>
1. "Initiate Deal" button appears in product-based chat conversations
2. /deals/new page pre-fills from product and shows complete offer form
3. All 11 Incoterms selectable as pills with tooltips
4. Named place autocomplete works with UN/LOCODE data
5. Form submission creates deal via CF and navigates to deal page
6. /deals page shows all user's deals with real-time updates
7. Deal cards show correct status, turn indicator, and latest terms
</verification>

<success_criteria>
- A member can navigate from a product chat to deal creation, fill the form with Incoterms + all terms, and submit
- The deal appears immediately in the My Deals list for both parties
- Real-time status updates work without page refresh on the deals list
- Insurance preference auto-sets based on Incoterm selection
- Named place label changes dynamically based on selected Incoterm
</success_criteria>

<output>
After completion, create `.planning/phases/02-deal-creation-and-negotiation-s1/02-02-SUMMARY.md`
</output>
