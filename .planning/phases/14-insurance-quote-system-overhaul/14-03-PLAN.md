---
phase: 14-insurance-quote-system-overhaul
plan: 03
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/ExclusionsSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/ConditionsPrecedentSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/ClaimsHandlingSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/PremiumAdditionsSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/sections/QuoteStatusSection.jsx
  - src/presentation/components/features/provider/QuoteFormInsurance/QuoteSummaryModal.jsx
autonomous: true
requirements: [INS-06, INS-07, INS-08, INS-09, INS-10, INS-11]

must_haves:
  truths:
    - "Exclusions section shows standard checkbox items from STANDARD_EXCLUSIONS constant plus a free text area for deal-specific exclusions"
    - "Conditions Precedent section shows standard checkbox items from STANDARD_CONDITIONS_PRECEDENT constant plus a free text area"
    - "Claims Handling section has jurisdiction dropdown, response time dropdown, and contact email field"
    - "Premium additions section has rate % field and payment terms dropdown"
    - "Quote Status section has Indicative/Firm toggle (Indicative default), binding conditions textarea appears when Firm selected, Message to Buyer field below"
    - "Quote Summary Modal shows a structured summary of all filled sections when provider clicks Submit Quote"
  artifacts:
    - path: "src/presentation/components/features/provider/QuoteFormInsurance/sections/ExclusionsSection.jsx"
      provides: "Exclusions checkbox list + free text"
    - path: "src/presentation/components/features/provider/QuoteFormInsurance/sections/QuoteStatusSection.jsx"
      provides: "Indicative/Firm toggle + binding conditions + message to buyer"
    - path: "src/presentation/components/features/provider/QuoteFormInsurance/QuoteSummaryModal.jsx"
      provides: "Pre-submit confirmation modal"
  key_links:
    - from: "QuoteSummaryModal.jsx"
      to: "QuoteFormInsurance.jsx"
      via: "receives watchedValues from parent's watch()"
      pattern: "watchedValues"
    - from: "QuoteStatusSection.jsx"
      to: "quoteConstants.js"
      via: "imports QUOTE_BINDING_STATUS"
      pattern: "QUOTE_BINDING_STATUS"
---

<objective>
Create the 5 shared form sections (Exclusions, Conditions Precedent, Claims Handling, Premium Additions, Quote Status) and the Quote Summary Modal. These are standalone sub-components that will be wired into QuoteFormInsurance in Plan 04.

Purpose: These sections complete the insurance quote form content below the risk type accordions. The summary modal provides pre-submit confirmation per user decision.
Output: 5 section sub-components + 1 modal component, all ready for integration.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/14-insurance-quote-system-overhaul/14-CONTEXT.md
@.planning/phases/14-insurance-quote-system-overhaul/14-RESEARCH.md
@.planning/phases/14-insurance-quote-system-overhaul/14-01-SUMMARY.md

@src/core/constants/quoteConstants.js

<interfaces>
<!-- Constants from Plan 01 -->

From src/core/constants/quoteConstants.js (after Plan 01):
```javascript
export const STANDARD_EXCLUSIONS = [{ value: 'inherent_vice', label: '...' }, ...]; // 7 items
export const STANDARD_CONDITIONS_PRECEDENT = [{ value: 'survey_required', label: '...' }, ...]; // 6 items
export const CLAIMS_JURISDICTION = [{ value: 'english_law', label: 'English Law (London)' }, ...]; // 6 items
export const CLAIMS_RESPONSE_TIME = [{ value: '24h', label: '24 Hours' }, ...]; // 5 items
export const PREMIUM_PAYMENT_TERMS = [{ value: 'advance', label: 'Payment in Advance' }, ...]; // 5 items
export const QUOTE_BINDING_STATUS = { INDICATIVE: 'indicative', FIRM: 'firm' };
export const ICC_COVERAGE = { A, B, C };
```

Expected Zod schema field names (to be added to QuoteFormInsurance schema in Plan 04):
```
exclusions: { standardItems: string[], customText?: string }
conditionsPrecedent: { standardItems: string[], customText?: string }
claimsHandling: { jurisdiction: string, responseTime: string, contactEmail?: string }
premiumAdditions: { ratePercent?: number, paymentTerms?: string }
quoteStatus: { status: 'indicative'|'firm', bindingConditions?: string, messageToBuyer?: string }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Exclusions, Conditions Precedent, Claims Handling, and Premium Additions section components</name>
  <files>
    src/presentation/components/features/provider/QuoteFormInsurance/sections/ExclusionsSection.jsx,
    src/presentation/components/features/provider/QuoteFormInsurance/sections/ConditionsPrecedentSection.jsx,
    src/presentation/components/features/provider/QuoteFormInsurance/sections/ClaimsHandlingSection.jsx,
    src/presentation/components/features/provider/QuoteFormInsurance/sections/PremiumAdditionsSection.jsx
  </files>
  <action>
All components are `'use client'`, receive props `{ register, errors, watch, control }` (whichever subset they need), and use the project's dark theme styling.

**ExclusionsSection.jsx** — field prefix: `exclusions.*`
- Header: "Exclusions" with an info subtitle "Select standard exclusions that apply to this quote"
- Render each `STANDARD_EXCLUSIONS` item as a checkbox. Use `register('exclusions.standardItems')` — react-hook-form handles array values from checkboxes with same name when each has a `value` prop.
- Pattern: `<input type="checkbox" value={item.value} {...register('exclusions.standardItems')} />` with the item label next to it
- Style checkboxes identically to existing War/Strikes clause checkboxes (p-3, rounded-lg, border, bg-[#0F1C2E], accent-orange-400)
- Below checkboxes: `<textarea {...register('exclusions.customText')} />` with label "Additional Exclusions (optional)" and placeholder "Enter any deal-specific exclusions..."
- Error display for standardItems array if needed

**ConditionsPrecedentSection.jsx** — field prefix: `conditionsPrecedent.*`
- Identical structure to ExclusionsSection but uses `STANDARD_CONDITIONS_PRECEDENT` constants
- Header: "Conditions Precedent"
- Checkboxes: `register('conditionsPrecedent.standardItems')`
- Free text: `register('conditionsPrecedent.customText')` with label "Additional Conditions (optional)"

**ClaimsHandlingSection.jsx** — field prefix: `claimsHandling.*`
- Header: "Claims Handling"
- `jurisdiction` — select dropdown using `CLAIMS_JURISDICTION` constants. Required. Use the same select styling with custom chevron bg from existing form.
- `responseTime` — select dropdown using `CLAIMS_RESPONSE_TIME` constants. Required.
- `contactEmail` — email input, optional. Label: "Claims Contact Email (optional)". Placeholder: "claims@company.com"
- Layout: jurisdiction and responseTime in a 2-column grid, contactEmail full width below

**PremiumAdditionsSection.jsx** — field prefix: `premiumAdditions.*`
- Header: "Premium Additions"
- `ratePercent` — number input (step 0.01, min 0). Label: "Premium Rate (% of insured value)". Optional.
- `paymentTerms` — select dropdown using `PREMIUM_PAYMENT_TERMS` constants. Label: "Payment Terms". Optional.
- Layout: 2-column grid row

All sections should have a consistent section header style: `text-sm font-semibold text-white mb-3` with a subtle border-t separator above each section `border-t border-[#2A3B52] pt-4 mt-4`.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && ls src/presentation/components/features/provider/QuoteFormInsurance/sections/ExclusionsSection.jsx src/presentation/components/features/provider/QuoteFormInsurance/sections/ConditionsPrecedentSection.jsx src/presentation/components/features/provider/QuoteFormInsurance/sections/ClaimsHandlingSection.jsx src/presentation/components/features/provider/QuoteFormInsurance/sections/PremiumAdditionsSection.jsx && echo "All 4 shared section files exist"</automated>
  </verify>
  <done>Four shared section components exist with correct field name prefixes, dark theme styling, and all dropdowns/checkboxes use constants from quoteConstants.js.</done>
</task>

<task type="auto">
  <name>Task 2: Create QuoteStatusSection and QuoteSummaryModal components</name>
  <files>
    src/presentation/components/features/provider/QuoteFormInsurance/sections/QuoteStatusSection.jsx,
    src/presentation/components/features/provider/QuoteFormInsurance/QuoteSummaryModal.jsx
  </files>
  <action>
**QuoteStatusSection.jsx** — field prefix: `quoteStatus.*`
Props: `{ register, errors, watch }`

- Header: "Quote Status & Communication"
- **Indicative/Firm toggle**: Two styled radio-card buttons side by side (similar to ICC coverage cards but horizontal).
  - Indicative (default): "Your quote is for informational purposes. Terms may change upon formal review."
  - Firm: "Your quote is legally binding under the specified conditions."
  - Field: `register('quoteStatus.status')` with values 'indicative' and 'firm'
  - Visual: Indicative = yellow/amber accent when selected, Firm = green accent when selected

- **Binding Conditions** (conditional): Only shown when `watch('quoteStatus.status') === 'firm'`
  - `<textarea {...register('quoteStatus.bindingConditions')} />` — Label: "Binding Conditions (optional per Claude's discretion — keeping it optional to avoid superRefine complexity; providers' professional obligation covers enforcement)"
  - Placeholder: "Specify conditions under which this quote is binding..."
  - Use framer-motion AnimatePresence for show/hide

- **Message to Buyer**: Always shown, below binding conditions area
  - `<textarea {...register('quoteStatus.messageToBuyer')} />` — Label: "Message to Buyer (optional)"
  - Placeholder: "Any message or context for the buyer..."
  - This is separate from the existing "Notes" field per user decision

**QuoteSummaryModal.jsx**
Props: `{ isOpen, onClose, onConfirm, watchedValues, isLoading, commercialRiskEnabled, politicalRiskEnabled }`

- Full-viewport fixed overlay modal (z-50, bg-black/60 backdrop, centered content card)
- Content card: bg-[#0D1822] with max-w-2xl, max-h-[80vh] overflow-y-auto, rounded-xl border
- Header: "Review Quote Summary" with X close button
- Body: Structured sections showing only filled data from `watchedValues`:

  1. **Cargo/Marine Insurance** (always shown):
     - ICC Coverage, Premium, Coverage Amount, Deductible %, Loss Covered %, Claims Payment, Policy Dates, Coverage Scope
     - Display in a 2-column key-value layout

  2. **Commercial Risk** (only if `commercialRiskEnabled` and `watchedValues.commercialRisk`):
     - Coverage Limit, Currency, Loss %, Coverage Basis, Waiting Period

  3. **Political Risk** (only if `politicalRiskEnabled` and `watchedValues.politicalRisk`):
     - Coverage Limit, Currency, Loss %, Selected Perils (comma-separated labels)

  4. **Exclusions** (only if any selected or custom text):
     - List of selected standard exclusions (look up labels from STANDARD_EXCLUSIONS)
     - Custom text if provided

  5. **Conditions Precedent** (only if any):
     - Same pattern as exclusions

  6. **Claims Handling** (only if jurisdiction set):
     - Jurisdiction, Response Time, Contact Email

  7. **Premium Additions** (only if rate or terms set):
     - Rate %, Payment Terms

  8. **Quote Status**:
     - Indicative or Firm (with binding conditions if firm)
     - Message to Buyer if provided

  9. **Quote Validity + Currency** (always): validity hours, currency

- Footer: two buttons
  - "Go Back" (secondary, calls onClose)
  - "Confirm & Submit" (primary orange, calls onConfirm, shows Loader2 spinner when isLoading)

For value formatting in the modal, use helper functions: formatCurrency for amounts, look up labels from constant arrays for dropdown values.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && ls src/presentation/components/features/provider/QuoteFormInsurance/sections/QuoteStatusSection.jsx src/presentation/components/features/provider/QuoteFormInsurance/QuoteSummaryModal.jsx && echo "QuoteStatusSection and QuoteSummaryModal exist"</automated>
  </verify>
  <done>QuoteStatusSection renders Indicative/Firm toggle with conditional binding conditions and Message to Buyer. QuoteSummaryModal shows structured review of all filled sections with confirm/cancel buttons.</done>
</task>

</tasks>

<verification>
- All 5 section files + 1 modal file exist in the correct directories
- Components import from quoteConstants.js correctly
- QuoteStatusSection conditionally shows binding conditions when Firm is selected
- QuoteSummaryModal displays only non-empty sections
</verification>

<success_criteria>
1. ExclusionsSection: renders 7 standard exclusion checkboxes + custom text area
2. ConditionsPrecedentSection: renders 6 standard condition checkboxes + custom text area
3. ClaimsHandlingSection: jurisdiction dropdown (6 options), response time dropdown (5 options), email input
4. PremiumAdditionsSection: rate % input + payment terms dropdown (5 options)
5. QuoteStatusSection: Indicative/Firm toggle, conditional binding conditions, message to buyer
6. QuoteSummaryModal: structured summary of all sections, confirm/cancel buttons, loading state
</success_criteria>

<output>
After completion, create `.planning/phases/14-insurance-quote-system-overhaul/14-03-SUMMARY.md`
</output>
