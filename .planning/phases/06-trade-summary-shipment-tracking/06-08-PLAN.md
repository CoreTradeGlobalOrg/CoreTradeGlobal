---
phase: 06-trade-summary-shipment-tracking
plan: "08"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/deal/TradeSummary/TradeRouteMap.jsx
autonomous: true
requirements: [TRACK-01]
gap_closure: true

must_haves:
  truths:
    - "Trade Route Map displays recognizable continental outlines resembling a real world map"
  artifacts:
    - path: "src/presentation/components/features/deal/TradeSummary/TradeRouteMap.jsx"
      provides: "Natural Earth simplified SVG paths for realistic continent shapes"
      min_lines: 50
  key_links:
    - from: "TradeRouteMap.jsx"
      to: "TradeSummaryTab.jsx"
      via: "import and render in sidebar"
      pattern: "TradeRouteMap"
---

<objective>
Replace the crude polygon world map with realistic Natural Earth continent outlines.

Purpose: Test 8 passed functionally but user reported the map "looks like shit" — the 6 crude hardcoded polygons (5-7 vertices each) need replacement with recognizable continent shapes.
Output: A visually credible dark-themed world map SVG with realistic continental outlines.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-trade-summary-shipment-tracking/06-UAT.md

<interfaces>
From src/presentation/components/features/deal/TradeSummary/TradeRouteMap.jsx (current):
- Dark-themed SVG component (bg-[#0b1626])
- Gold origin pin (left side), blue destination pin (right side)
- Dashed quadratic bezier route line connecting pins
- Legend at bottom
- 180px height
- Uses 6 crude hardcoded SVG polygons as continent shapes
- Pin positions are fixed at roughly (80,80) and (320,90)

The component is rendered inside TradeSummaryTab.jsx in the right sidebar column.
Props: none currently (purely decorative with static pins).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace crude polygons with Natural Earth simplified SVG continent paths</name>
  <files>src/presentation/components/features/deal/TradeSummary/TradeRouteMap.jsx</files>
  <action>
Replace the existing TradeRouteMap.jsx with a new version that has realistic continent outlines:

1. Use simplified Natural Earth 110m-resolution SVG path data for the 7 continents. These are heavily simplified versions (50-100 vertices per continent) that look recognizable at the small display size (180px height) without being excessively large. The total SVG path data should be under 15KB.

2. Use an Equirectangular (Plate Carree) projection which maps longitude to x and latitude to y linearly. This is the simplest projection and works well for a small decorative map:
   - x = (longitude + 180) / 360 * viewBoxWidth
   - y = (90 - latitude) / 180 * viewBoxHeight

3. ViewBox should be approximately 800x400 (2:1 ratio matching equirectangular). The component display height stays at 180px with width auto-filling the container.

4. Continent paths should use fill="#1A283B" (slightly lighter than bg) with stroke="#2A3B52" stroke-width="0.5" for subtle borders — matching the existing dark theme.

5. Keep the existing decorative elements:
   - Gold origin pin (circle + line) on the left portion of the map
   - Blue destination pin on the right portion
   - Dashed bezier curve connecting them
   - Legend at the bottom

6. Pin positions remain static (decorative only). Place the origin pin roughly at Western Europe coordinates (10, 48) mapped to SVG space, and destination pin roughly at East Asia (120, 35). These are approximate — the map is for visual context only.

7. Keep the dark background (bg-[#0b1626]), rounded corners, and existing Tailwind classes for the outer container.

APPROACH: Include the simplified continent path strings directly as constants in the component file. Do NOT add any npm dependencies or external SVG files. The paths should be simplified enough to be readable inline (~5-8KB of path data total for all continents). Focus on recognizable shapes — Africa, the Americas, Eurasia, Australia — not cartographic precision. Omit Antarctica.

The key continents to include with approximate simplified paths:
- North America (including Central America and Caribbean outline)
- South America
- Europe + Asia (can be one Eurasia path or split)
- Africa
- Australia/Oceania (just Australia mainland + basic outline)
- Greenland (optional, adds realism)
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>TradeRouteMap displays realistic continent outlines with Natural Earth simplified paths. Dark theme preserved. Gold/blue pins and route line intact. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` passes with no errors
- TradeRouteMap.jsx contains SVG path elements (not polygon) with realistic vertex counts
- Component maintains dark theme styling and pin decorations
</verification>

<success_criteria>
- World map shows recognizable continental shapes (not crude polygons)
- Dark theme colors preserved (#0b1626 background, #1A283B fill, #2A3B52 stroke)
- Existing decorative elements (pins, route line, legend) preserved
- File size reasonable (under 20KB including path data)
</success_criteria>

<output>
After completion, create `.planning/phases/06-trade-summary-shipment-tracking/06-08-SUMMARY.md`
</output>
