---
phase: 06-trade-summary-shipment-tracking
plan: 10
type: execute
wave: 2
depends_on: ["06-09"]
files_modified:
  - src/app/(main)/deals/page.jsx
  - src/presentation/components/features/provider/InsuranceCoverageTab.jsx
  - src/presentation/hooks/deal/useDeal.js
autonomous: true
requirements: [TRACK-03]
gap_closure: true

must_haves:
  truths:
    - "New Deal button links to /products with black text on gold background"
    - "InsuranceCoverageTab confirm button shows loading state and prevents double-click"
    - "useDeal hook handles deal subscription errors gracefully instead of hanging"
  artifacts:
    - path: "src/app/(main)/deals/page.jsx"
      provides: "Browse Marketplace button with correct href and text color"
      contains: "href=\"/products\""
    - path: "src/presentation/components/features/provider/InsuranceCoverageTab.jsx"
      provides: "Per-card confirming state to prevent double-click"
      contains: "confirming"
    - path: "src/presentation/hooks/deal/useDeal.js"
      provides: "Error callback on deal subscription preventing infinite loading"
      contains: "dealLoaded = true"
  key_links:
    - from: "deals/page.jsx Browse Marketplace button"
      to: "/products route"
      via: "Link href"
      pattern: "href.*products"
---

<objective>
Fix minor UAT issues: New Deal button href pointing to /products (not /marketplace which doesn't exist), InsuranceCoverageTab double-click prevention, and useDeal error handling for graceful loading states.

Purpose: Test 10 reported the button showed an error because /deals/new requires params. Plan 06-07 changed it to /marketplace but the actual products route is /products. Also addresses two defensive fixes found during UAT debugging that prevent edge-case hangs.

Output: Polished deals page button, resilient insurance coverage confirmation, robust deal loading.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-trade-summary-shipment-tracking/06-07-SUMMARY.md
@.planning/phases/06-trade-summary-shipment-tracking/06-UAT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix deals page button href and add defensive error handling</name>
  <files>src/app/(main)/deals/page.jsx, src/presentation/components/features/provider/InsuranceCoverageTab.jsx, src/presentation/hooks/deal/useDeal.js</files>
  <action>
  1. In src/app/(main)/deals/page.jsx, find the Browse Marketplace Link component (~line 330). Change `href="/marketplace"` to `href="/products"`. The /marketplace route does not exist; /products is the actual marketplace products listing page. Also ensure the className has `!text-black` (with ! important modifier) instead of just `text-black` to override any Tailwind dark mode or parent specificity that may turn text yellow/white.

  2. In src/presentation/components/features/provider/InsuranceCoverageTab.jsx, find the CoverageCard component. Add a local `confirming` state using `useState(false)` to prevent double-click on the Confirm Coverage button:
     - Import `useState` from 'react' if not already imported
     - Add `const [confirming, setConfirming] = useState(false)` inside CoverageCard
     - In handleConfirm: guard with `if (isCoverageActive || actionLoading || confirming) return;`
     - Set `setConfirming(true)` before the try block
     - Add `finally { setConfirming(false); }` to reset on completion or error

  3. In src/presentation/hooks/deal/useDeal.js, find the subscribeToDeal call (~line 111). Add an error callback as the third argument to handle subscription failures gracefully:
     ```javascript
     }, (err) => {
       console.error('[DEBUG] useDeal: DEAL DOC subscription failed:', err);
       dealLoaded = true;
       checkLoaded();
     });
     ```
     This prevents infinite loading when the deal subscription fails (e.g., permission error on a deleted/inaccessible deal).
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -n 'href="/products"' "src/app/(main)/deals/page.jsx" && echo "---" && grep -n "confirming" src/presentation/components/features/provider/InsuranceCoverageTab.jsx && echo "---" && grep -n "dealLoaded = true" src/presentation/hooks/deal/useDeal.js</automated>
  </verify>
  <done>Browse Marketplace button links to /products with black text. InsuranceCoverageTab has double-click prevention. useDeal handles subscription errors without infinite loading.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes without errors
2. On /deals page, "Browse Marketplace" button has black text on gold background and links to /products
3. InsuranceCoverageTab Confirm Coverage button prevents rapid double-clicks
4. useDeal hook sets loading=false even when deal subscription fails
</verification>

<success_criteria>
- Browse Marketplace button href is /products (not /marketplace)
- Button text is reliably black on gold background via !text-black
- InsuranceCoverageTab has per-card confirming state guard
- useDeal error callback sets dealLoaded and calls checkLoaded
- Test 10 UAT gap resolved
</success_criteria>

<output>
After completion, create `.planning/phases/06-trade-summary-shipment-tracking/06-10-SUMMARY.md`
</output>
