---
phase: 06-trade-summary-shipment-tracking
plan: 04
type: execute
wave: 3
depends_on: ["06-01", "06-02", "06-03"]
files_modified:
  - src/presentation/components/features/deal/TradeSummary/OrderTimeline.jsx
  - src/presentation/components/features/deal/TradeSummary/ETACountdown.jsx
  - src/presentation/components/features/deal/DealSidebar/DealSidebar.jsx
  - src/presentation/components/features/deal/DealCard/DealCard.jsx
  - src/app/(main)/deals/page.jsx
  - src/app/admin/page.jsx
autonomous: true
requirements: [TRACK-03, TRACK-04]

must_haves:
  truths:
    - "Order timeline shows deal milestones (Negotiated, Contract Approved, Providers Selected) with timestamps"
    - "Order timeline shows shipment milestones (Preparing through Delivered) from logistics provider updates"
    - "Order timeline shows insurance Coverage Active milestone"
    - "Deal milestones are clickable — navigate to relevant page (contract, quotes)"
    - "Timeline updates in real-time via Firestore onSnapshot"
    - "DealCard shows tracking status badge and ETA when available"
    - "Member deals page has status summary cards and active shipments highlight"
    - "Admin dashboard shows platform-wide trade stats"
    - "Legacy deals without statusHistory infer milestones from existing timestamps"
  artifacts:
    - path: "src/presentation/components/features/deal/TradeSummary/OrderTimeline.jsx"
      provides: "Vertical milestone timeline with deal + shipment + insurance milestones"
      min_lines: 60
    - path: "src/presentation/components/features/deal/TradeSummary/ETACountdown.jsx"
      provides: "Countdown timer from ETA date"
      min_lines: 15
  key_links:
    - from: "OrderTimeline.jsx"
      to: "deal.statusHistory + shipmentUpdates"
      via: "Merges deal milestones and shipment milestones into unified sorted timeline"
      pattern: "statusHistory.*shipmentUpdates.*sort"
    - from: "DealCard.jsx"
      to: "deal.currentShipmentStatus"
      via: "Denormalized field on deal doc for badge display"
      pattern: "currentShipmentStatus"
    - from: "DealSidebar.jsx"
      to: "OrderTimeline.jsx"
      via: "Renders timeline in sidebar when summary tab active or deal is post-contract"
      pattern: "OrderTimeline"
---

<objective>
Build the order timeline, enhance DealCard/DealSidebar, and add dashboard enhancements for members and admin.

Purpose: Delivers TRACK-03 (order timeline with milestones) and the remaining TRACK-04 (member dashboard stats, admin stats, DealCard badges). This is the final plan tying together all Phase 6 data and UI.

Output: OrderTimeline component with three milestone categories, ETACountdown timer, enhanced DealCard with tracking badge, member deals page with stats cards, admin dashboard with trade stats.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trade-summary-shipment-tracking/06-CONTEXT.md
@.planning/phases/06-trade-summary-shipment-tracking/06-RESEARCH.md
@.planning/phases/06-trade-summary-shipment-tracking/06-01-SUMMARY.md
@.planning/phases/06-trade-summary-shipment-tracking/06-02-SUMMARY.md
@.planning/phases/06-trade-summary-shipment-tracking/06-03-SUMMARY.md

<interfaces>
<!-- From Plans 01-03 -->

From src/core/constants/shipmentConstants.js:
```javascript
export const SHIPMENT_STATUS = { ... };
export const SHIPMENT_STATUS_LABELS = { ... };
```

From src/core/constants/dealConstants.js:
```javascript
export const DEAL_STATUS = { ..., DELIVERED: 'delivered' };
```

From deal document (statusHistory field, added by Plan 01 CFs):
```javascript
deal.statusHistory = [
  { status: 'accepted', timestamp: Timestamp, actorId, actorName, note },
  { status: 'contract_approved', ... },
  { status: 'providers_selected', ... },
  { status: 'delivered', ... },
]
```

From deal document (denormalized fields from Plan 01):
```javascript
deal.currentShipmentStatus  // string | null
deal.shipmentEtaDate         // Timestamp | null
```

From ShipmentUpdate entity:
```javascript
// Fields: id, dealId, status, timestamp, actorId, actorName, providerType, note, containerNumber, trackingRef, etaDate, dealBuyerId, dealSellerId
```

From DealSidebar.jsx — ProgressTracker section:
```javascript
// Currently shows: 4-step vertical stepper (Negotiation, Agreement, Quotes, Tracking)
// getActiveStep(status) returns current step
```

From DealCard.jsx:
```javascript
// Shows deal summary card with status badge
// statusConfig object maps DEAL_STATUS to badge colors/labels
```

From deals page (src/app/(main)/deals/page.jsx):
```javascript
// Lists user's deals with DealCard components
// Has COMPLETED_STATUSES array for filtering
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: OrderTimeline and ETACountdown components, DealSidebar integration</name>
  <files>
    src/presentation/components/features/deal/TradeSummary/OrderTimeline.jsx,
    src/presentation/components/features/deal/TradeSummary/ETACountdown.jsx,
    src/presentation/components/features/deal/DealSidebar/DealSidebar.jsx
  </files>
  <action>
    1. **OrderTimeline.jsx** — Vertical milestone timeline:
       - Props: `{ deal, shipmentUpdates, dealId }`
       - Three milestone categories merged into a single sorted timeline:
         a. **Deal milestones** — from `deal.statusHistory` array. Map status values to labels: 'negotiating' -> 'Deal Created', 'accepted' -> 'Offer Accepted', 'contract_approved' -> 'Contract Approved', 'providers_selected' -> 'Providers Selected', 'delivered' -> 'Trade Delivered'. Each shows timestamp + actor name. Deal milestones are CLICKABLE: 'accepted'/'contract_approved' links to `/deals/{dealId}/contract`, 'providers_selected' links to `/deals/{dealId}/quotes`.
         b. **Shipment milestones** — from `shipmentUpdates` array (ShipmentUpdate entities from useTradeSummary). Each shows: SHIPMENT_STATUS_LABELS[status], timestamp, actor name, optional note. Shipment milestones are NOT clickable.
         c. **Insurance milestone** — filter shipmentUpdates for `providerType === 'insurance'` and `status === 'coverage_active'`. Shows "Coverage Active" with timestamp and provider name. Not clickable.
       - **Legacy deals without statusHistory**: If `deal.statusHistory` is undefined/empty, infer milestones from existing timestamps: `deal.createdAt` -> 'Deal Created', check for contract existence via deal status (if CONTRACT_APPROVED or later, show 'Contract Approved' with `deal.updatedAt` as rough timestamp), similar for PROVIDERS_SELECTED. Show these inferred milestones with "(estimated)" tag.
       - Merge all milestones into one array, sort by timestamp ascending
       - Visual: Vertical line on the left, circle marker per milestone (filled for completed, hollow for future/pending), gold (#FFD700) for completed, muted for pending
       - Future milestones: Show remaining statuses as greyed-out pending items below the completed ones (if deal is IN_TRANSIT, show OUT_FOR_DELIVERY and DELIVERED as pending)
       - Real-time: Component re-renders when deal or shipmentUpdates props change (driven by onSnapshot in parent hook)

    2. **ETACountdown.jsx** — ETA countdown display:
       - Props: `{ etaDate }`
       - If etaDate is null/undefined: render nothing
       - If etaDate is in the past: show "Arrived" or "ETA passed"
       - If etaDate is in the future: show countdown using `date-fns` `formatDistanceToNow(etaDate, { addSuffix: true })` — e.g., "in 3 days"
       - Updates every minute using setInterval
       - Styled as a small badge/pill: bg-blue-900/20 text-blue-400 with clock icon

    3. **DealSidebar.jsx** — Integrate OrderTimeline:
       - Read existing DealSidebar to understand ProgressTracker location
       - When deal status is CONTRACT_APPROVED, PROVIDERS_SELECTED, or DELIVERED: replace or extend the ProgressTracker section with OrderTimeline
       - For earlier statuses (NEGOTIATING, ACCEPTED): keep existing ProgressTracker (4-step stepper)
       - Pass deal and shipmentUpdates to OrderTimeline. To get shipmentUpdates in sidebar: accept them as a prop from the parent page, or subscribe to shipmentTracking inside DealSidebar using ShipmentRepository (prefer prop passing from parent to avoid duplicate subscriptions — check if useTradeSummary data is available from parent)
       - Add ETACountdown below timeline if deal has shipmentEtaDate
       - Keep existing PartyCard and OfferSummary sections unchanged
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next lint --quiet 2>&1 | head -20</automated>
  </verify>
  <done>
    - OrderTimeline renders deal, shipment, and insurance milestones in chronological order
    - Deal milestones are clickable with navigation to contract/quotes pages
    - Legacy deals without statusHistory show inferred milestones with "(estimated)" tag
    - ETACountdown shows live countdown from ETA date
    - DealSidebar shows OrderTimeline for post-contract deals, ProgressTracker for negotiation phase
  </done>
</task>

<task type="auto">
  <name>Task 2: DealCard tracking badge, member deals page stats, admin dashboard stats</name>
  <files>
    src/presentation/components/features/deal/DealCard/DealCard.jsx,
    src/app/(main)/deals/page.jsx,
    src/app/admin/page.jsx
  </files>
  <action>
    1. **DealCard.jsx** — Add tracking status badge and ETA:
       - Read existing DealCard to understand statusConfig structure
       - Add DELIVERED to statusConfig: `{ label: 'Delivered', color: 'text-green-400', bg: 'bg-green-900/20', border: 'border-green-700/30' }`
       - If `deal.currentShipmentStatus` exists (denormalized field from CF): show a small tracking badge below the status badge with SHIPMENT_STATUS_LABELS text and appropriate color
       - If `deal.shipmentEtaDate` exists: show ETA as "ETA: {formatDistanceToNow}" next to or below tracking badge using date-fns
       - Keep existing DealCard layout — these are small additive badges, not a redesign

    2. **deals/page.jsx** — Member dashboard enhancements:
       - Read existing deals page structure
       - Add DELIVERED to COMPLETED_STATUSES array (or equivalent filter)
       - Add **deal status summary cards** at top of page: compute counts from existing deals list:
         - X deals negotiating
         - Y awaiting contract
         - Z in transit (status === PROVIDERS_SELECTED AND currentShipmentStatus in [PREPARING, PICKED_UP, IN_TRANSIT, AT_CUSTOMS, OUT_FOR_DELIVERY])
         - W delivered (status === DELIVERED)
       - Summary cards styled as horizontal row of stat cards: icon + count + label, dark theme, responsive (2x2 grid on mobile, 4 across on desktop)
       - Add **active shipments highlight section** below stats cards: filter deals where status is PROVIDERS_SELECTED and currentShipmentStatus exists — show these prominently with tracking badge and ETA. Use existing DealCard (now enhanced with tracking badge) in a separate section titled "Active Shipments"
       - Add **quick action buttons**: "Create New Deal" (links to /deals/new)
       - Keep existing deal list below as the main content

    3. **admin/page.jsx** — Platform-wide trade stats:
       - Read existing admin page structure
       - Add a "Trade Overview" stats section showing platform-wide numbers:
         - Total deals (query all deals, count)
         - Active shipments (deals where status === PROVIDERS_SELECTED)
         - Completed deliveries (deals where status === DELIVERED)
       - Use a simple Firestore query (or call an existing admin data hook if one exists)
       - Admin can already access any deal page — just add these aggregate stats
       - Styled as stat cards matching admin dashboard theme
       - NOTE: These are approximate counts for dashboard display — no need for server-side aggregation functions. A client-side query with limit is acceptable for an admin viewing low-volume trade data.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - DealCard shows DELIVERED status config and tracking badge with ETA when available
    - Deals page has status summary cards (negotiating, awaiting contract, in transit, delivered)
    - Deals page has active shipments highlight section
    - Admin dashboard shows total deals, active shipments, completed deliveries stats
    - DELIVERED added to completed statuses filter
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- OrderTimeline renders milestones in chronological order with correct categories
- DealCard correctly shows tracking badge and ETA
- Deals page stats cards compute correct counts from deal data
- Admin dashboard displays platform-wide trade stats
</verification>

<success_criteria>
- Order timeline shows all three milestone categories with real-time updates
- Deal milestones are clickable, shipment milestones are not
- Legacy deals gracefully degrade with inferred milestones
- Member dashboard provides at-a-glance trade status overview
- Admin dashboard adds trade stats without disrupting existing admin functionality
- All TRACK-03 and TRACK-04 requirements addressed
</success_criteria>

<output>
After completion, create `.planning/phases/06-trade-summary-shipment-tracking/06-04-SUMMARY.md`
</output>
