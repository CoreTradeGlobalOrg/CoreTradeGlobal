---
phase: 06-trade-summary-shipment-tracking
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/presentation/hooks/provider/useActiveShipments.js
  - src/presentation/components/features/provider/ActiveShipmentsTab.jsx
  - src/presentation/components/features/provider/ShipmentUpdateForm.jsx
  - src/presentation/components/features/provider/InsuranceCoverageTab.jsx
  - src/app/(main)/provider/dashboard/page.jsx
autonomous: true
requirements: [TRACK-02, TRACK-04]

must_haves:
  truths:
    - "Logistics provider can see deals where they are the selected provider with current shipment status"
    - "Logistics provider can submit a shipment status update with status dropdown, note, container number, tracking ref, and ETA"
    - "Insurance provider can see deals where they are selected and confirm coverage with a single action"
    - "Insurance coverage confirmation is idempotent — button disables after first confirmation"
    - "Provider dashboard has new Active Shipments section/tab"
  artifacts:
    - path: "src/presentation/components/features/provider/ActiveShipmentsTab.jsx"
      provides: "Logistics provider active shipments list with status update forms"
      min_lines: 40
    - path: "src/presentation/components/features/provider/ShipmentUpdateForm.jsx"
      provides: "Status dropdown, note, container number, tracking ref, ETA date form"
      min_lines: 30
    - path: "src/presentation/components/features/provider/InsuranceCoverageTab.jsx"
      provides: "Insurance provider coverage confirmation list"
      min_lines: 30
  key_links:
    - from: "ShipmentUpdateForm.jsx"
      to: "functions/index.js (submitShipmentUpdate)"
      via: "httpsCallable('submitShipmentUpdate')"
      pattern: "httpsCallable.*submitShipmentUpdate"
    - from: "InsuranceCoverageTab.jsx"
      to: "functions/index.js (confirmInsuranceCoverage)"
      via: "httpsCallable('confirmInsuranceCoverage')"
      pattern: "httpsCallable.*confirmInsuranceCoverage"
    - from: "useActiveShipments.js"
      to: "quoteRequests collection"
      via: "Query where providerUid == uid AND status == 'selected'"
      pattern: "providerUid.*selected"
---

<objective>
Build the provider-facing shipment update UX on the existing provider dashboard.

Purpose: Delivers the write side of TRACK-02 (providers submit status updates) and part of TRACK-04 (provider dashboard enhancements). Logistics providers get a form to submit shipment status updates; insurance providers get a single-action coverage confirmation button.

Output: ActiveShipmentsTab with ShipmentUpdateForm for logistics providers, InsuranceCoverageTab for insurance providers, useActiveShipments hook, provider dashboard page integration.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trade-summary-shipment-tracking/06-CONTEXT.md
@.planning/phases/06-trade-summary-shipment-tracking/06-RESEARCH.md
@.planning/phases/06-trade-summary-shipment-tracking/06-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 -->

From src/core/constants/shipmentConstants.js:
```javascript
export const SHIPMENT_STATUS = {
  PREPARING: 'preparing', PICKED_UP: 'picked_up', IN_TRANSIT: 'in_transit',
  AT_CUSTOMS: 'at_customs', OUT_FOR_DELIVERY: 'out_for_delivery',
  DELIVERED: 'delivered', COVERAGE_ACTIVE: 'coverage_active',
};
export const SHIPMENT_STATUS_LABELS = { ... };
```

From functions/index.js (Plan 01):
```javascript
exports.submitShipmentUpdate = onCall(async (request) => { ... })
exports.confirmInsuranceCoverage = onCall(async (request) => { ... })
```

From existing provider dashboard pattern:
```javascript
// src/app/(main)/provider/dashboard/page.jsx
// Currently renders ProviderDashboard component with kanban board for quotes
// Uses useAuthContext for current user, checks providerType
```

From existing QuoteRequestRepository (used for finding selected requests):
```javascript
// Query pattern for finding provider's selected requests
const q = query(
  collection(db, 'quoteRequests'),
  where('providerUid', '==', uid),
  where('status', '==', 'selected')
);
```

From existing httpsCallable pattern:
```javascript
import { getFunctions, httpsCallable } from 'firebase/functions';
const functions = getFunctions();
const submitShipmentUpdate = httpsCallable(functions, 'submitShipmentUpdate');
await submitShipmentUpdate({ dealId, status, note, ... });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useActiveShipments hook and ShipmentUpdateForm</name>
  <files>
    src/presentation/hooks/provider/useActiveShipments.js,
    src/presentation/components/features/provider/ShipmentUpdateForm.jsx
  </files>
  <action>
    1. **useActiveShipments.js** — Hook for provider shipment data:
       - Takes `uid` and `providerType` ('logistics' | 'insurance_provider' — note the role constant format)
       - Queries quoteRequests where `providerUid == uid` AND `status == 'selected'` AND `providerType` matches (use 'logistics' or 'insurance' based on the user's role — map 'logistics_provider' -> 'logistics', 'insurance_provider' -> 'insurance')
       - For each selected quoteRequest, enriches with deal data: subscribe to the deal doc using dealId from quoteRequest
       - For logistics: also subscribe to shipmentTracking subcollection for each deal to get latest status
       - Returns: `{ shipments: [{ quoteRequest, deal, latestShipmentUpdate, shipmentUpdates }], loading, error }`
       - Cleanup: unsubscribe all listeners on unmount
       - Also returns `submitUpdate` function (for logistics) that calls `httpsCallable('submitShipmentUpdate')`
       - Also returns `confirmCoverage` function (for insurance) that calls `httpsCallable('confirmInsuranceCoverage')`
       - Both action functions: set loading state during call, handle errors with try/catch, show toast on success/error

    2. **ShipmentUpdateForm.jsx** — Status update form for logistics providers:
       - Props: `{ dealId, currentStatus, onSubmit, loading }`
       - Status dropdown: shows SHIPMENT_STATUS values (excluding COVERAGE_ACTIVE) as options, with current status pre-selected or next logical status
       - Disable statuses that are "before" the current status (can't go backward: if IN_TRANSIT, disable PREPARING and PICKED_UP)
       - Note field: optional textarea for provider comments
       - Container number field: text input, required on first update (PICKED_UP), optional afterward
       - Tracking reference field: text input, required on first update (PICKED_UP), optional afterward
       - ETA date field: date input for estimated delivery date
       - Submit button: calls onSubmit with form data, shows loading spinner during submission
       - Success: reset form, show toast "Shipment status updated"
       - Error: show toast with error message
       - Dark theme styling consistent with provider dashboard
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next lint --quiet 2>&1 | head -20</automated>
  </verify>
  <done>
    - useActiveShipments queries selected quoteRequests and enriches with deal + shipment data
    - ShipmentUpdateForm renders status dropdown with forward-only progression
    - Container number and tracking ref required on first update
    - Submit calls httpsCallable submitShipmentUpdate
  </done>
</task>

<task type="auto">
  <name>Task 2: ActiveShipmentsTab, InsuranceCoverageTab, and provider dashboard integration</name>
  <files>
    src/presentation/components/features/provider/ActiveShipmentsTab.jsx,
    src/presentation/components/features/provider/InsuranceCoverageTab.jsx,
    src/app/(main)/provider/dashboard/page.jsx
  </files>
  <action>
    1. **ActiveShipmentsTab.jsx** — Logistics provider's active shipments view:
       - Props: `{ uid }`
       - Uses useActiveShipments hook with providerType='logistics'
       - Shows a list of deals where the logistics provider is selected
       - Each deal card shows: product name, buyer/seller names, current shipment status badge, container number, tracking ref, ETA countdown (using date-fns formatDistanceToNow)
       - Expandable: each card expands to show ShipmentUpdateForm
       - Delivery stats at top: "X in transit, Y delivered this month" (computed from shipments list)
       - Empty state: "No active shipments" with truck icon
       - Loading skeleton while data loads

    2. **InsuranceCoverageTab.jsx** — Insurance provider's coverage view:
       - Props: `{ uid }`
       - Uses useActiveShipments hook with providerType='insurance'
       - Shows list of deals where insurance provider is selected
       - Each deal card shows: product name, buyer/seller names, deal status, coverage status
       - "Confirm Coverage" button: calls confirmCoverage(dealId). Button shows loading spinner during call.
       - After coverage confirmed (check if shipmentUpdates includes a COVERAGE_ACTIVE entry), button changes to "Coverage Active" with green checkmark, disabled
       - Empty state: "No deals requiring coverage confirmation"

    3. **Provider dashboard page.jsx** — Integrate new tabs:
       - Read existing provider dashboard page to understand current structure
       - Add tab switcher or section: existing quote requests view + new "Active Shipments" tab
       - For logistics_provider role: show ActiveShipmentsTab
       - For insurance_provider role: show InsuranceCoverageTab
       - Tab names: "Quote Requests" (existing) and "Active Shipments" (new)
       - Default tab: "Quote Requests" (preserve existing behavior)
       - Pass current user uid from useAuthContext to new tabs
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - Logistics provider dashboard has "Active Shipments" tab showing selected deals with update forms
    - Insurance provider dashboard has "Active Shipments" tab showing deals with "Confirm Coverage" button
    - Coverage confirmation is idempotent — button disables after confirmation
    - Provider dashboard page integrates both new tabs alongside existing quote requests
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- Provider dashboard shows new Active Shipments tab
- ShipmentUpdateForm enforces forward-only status progression
- InsuranceCoverageTab disables button after coverage confirmed
</verification>

<success_criteria>
- Logistics providers can submit shipment status updates through their dashboard
- Insurance providers can confirm coverage with a single click
- Both provider types see their selected deals clearly
- Status updates flow through Cloud Functions (no direct Firestore writes)
- Dark theme consistent with existing provider dashboard styling
</success_criteria>

<output>
After completion, create `.planning/phases/06-trade-summary-shipment-tracking/06-03-SUMMARY.md`
</output>
