---
phase: 06-trade-summary-shipment-tracking
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx
autonomous: true
gap_closure: true
requirements:
  - TRACK-01
  - TRACK-02
  - TRACK-03
  - TRACK-04

must_haves:
  truths:
    - "OrderTimeline is visible on the Trade Summary tab without switching tabs"
    - "Deal milestones are clickable from the Trade Summary tab right sidebar"
    - "Shipment and insurance milestones appear in the timeline on the summary view"
  artifacts:
    - path: "src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx"
      provides: "OrderTimeline rendered in right sidebar below TradeRouteMap"
      contains: "OrderTimeline"
  key_links:
    - from: "src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx"
      to: "src/presentation/components/features/deal/TradeSummary/OrderTimeline.jsx"
      via: "import and JSX render in right sidebar column"
      pattern: "import.*OrderTimeline.*from.*OrderTimeline"
    - from: "useTradeSummary hook return"
      to: "OrderTimeline props"
      via: "shipmentUpdates destructured from hook and passed as prop"
      pattern: "shipmentUpdates.*OrderTimeline"
---

<objective>
Wire OrderTimeline into TradeSummaryTab right sidebar to close the verification gap where
users on PROVIDERS_SELECTED and DELIVERED deals auto-switch to the summary tab but cannot
see the order timeline without manually switching back to the negotiation tab.

Purpose: The OrderTimeline component (362 lines) is fully built and the useTradeSummary hook
already fetches shipmentUpdates, but TradeSummaryTab never renders OrderTimeline. This is a
single-file wiring fix.

Output: Updated TradeSummaryTab.jsx with OrderTimeline rendered below TradeRouteMap in the
right sidebar column.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-trade-summary-shipment-tracking/06-VERIFICATION.md

<interfaces>
<!-- OrderTimeline component signature (from OrderTimeline.jsx line 319): -->
```jsx
export function OrderTimeline({ deal, shipmentUpdates = [], dealId })
```

<!-- useTradeSummary hook return (from useTradeSummary.js lines 136-148): -->
```js
return {
  deal,
  contract,
  selectedInsuranceQuote,
  selectedLogisticsQuote,
  shipmentUpdates,    // ShipmentUpdate[] — already fetched, not destructured in TradeSummaryTab
  latestShipment,
  legalEngagement,
  loading,
  error,
};
```

<!-- Current TradeSummaryTab destructures from useTradeSummary (line 64-72): -->
```js
const {
  deal,
  contract,
  selectedInsuranceQuote,
  selectedLogisticsQuote,
  latestShipment,
  legalEngagement,
  loading,
} = useTradeSummary(dealId, currentUserUid);
// NOTE: shipmentUpdates is NOT destructured — must be added
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire OrderTimeline into TradeSummaryTab right sidebar</name>
  <files>src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx</files>
  <action>
In TradeSummaryTab.jsx, make these three targeted changes:

1. Add import for OrderTimeline at the top (after the LegalConsultingSection import):
   `import { OrderTimeline } from './OrderTimeline';`

2. Add `shipmentUpdates` to the destructured return from useTradeSummary (line 64-72). It should be:
   `const { deal, contract, selectedInsuranceQuote, selectedLogisticsQuote, latestShipment, shipmentUpdates, legalEngagement, loading } = useTradeSummary(dealId, currentUserUid);`

3. In the right sidebar column (currently lines 133-139), render OrderTimeline below TradeRouteMap. Replace the comment "future timeline placeholder" with the actual component. The sidebar div should become:
   ```jsx
   {/* Right sidebar — map + order timeline */}
   <div className="w-full lg:w-72 xl:w-80 flex-shrink-0 space-y-4">
     <TradeRouteMap
       originName={originName}
       destinationName={destinationName}
     />
     <OrderTimeline
       deal={deal}
       shipmentUpdates={shipmentUpdates}
       dealId={dealId}
     />
   </div>
   ```

No other changes needed. Do not modify any other file.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -n "OrderTimeline" src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx && grep -n "shipmentUpdates" src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx && npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>TradeSummaryTab.jsx imports OrderTimeline, destructures shipmentUpdates from useTradeSummary, and renders OrderTimeline in the right sidebar below TradeRouteMap. Build succeeds with no errors.</done>
</task>

</tasks>

<verification>
1. `grep -c "OrderTimeline" src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx` returns 2+ (import + JSX usage)
2. `grep "shipmentUpdates" src/presentation/components/features/deal/TradeSummary/TradeSummaryTab.jsx` shows destructuring and prop passing
3. `npx next build` completes without errors
4. The "future timeline placeholder" comment is removed
</verification>

<success_criteria>
- OrderTimeline is rendered in the TradeSummaryTab right sidebar below TradeRouteMap
- shipmentUpdates from useTradeSummary is passed to OrderTimeline
- deal and dealId props are passed to OrderTimeline
- Build succeeds with no import or prop errors
- Users on PROVIDERS_SELECTED/DELIVERED deals see the OrderTimeline without tab switching
</success_criteria>

<output>
After completion, create `.planning/phases/06-trade-summary-shipment-tracking/06-05-SUMMARY.md`
</output>
