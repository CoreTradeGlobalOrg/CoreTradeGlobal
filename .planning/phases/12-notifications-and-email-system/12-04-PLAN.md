---
phase: 12-notifications-and-email-system
plan: 04
type: execute
wave: 2
depends_on:
  - 12-02
files_modified:
  - functions/index.js
  - firestore.rules
  - src/hooks/usePushNotifications.js
  - public/firebase-messaging-sw.js
  - src/presentation/components/features/notifications/NotificationListener.jsx
autonomous: true
requirements:
  - NOTIF-05
  - NOTIF-06

must_haves:
  truths:
    - "Admin receives push notification when a new member registers"
    - "All members receive push notification when a new RFQ is created"
    - "Providers receive push notification when a quote request is broadcast to them"
    - "All new push events respect Phase 10 notification preferences"
    - "FCM push notifications are delivered successfully (token lifecycle working)"
  artifacts:
    - path: "functions/index.js"
      provides: "onRFQCreated trigger, admin member notification, provider quote broadcast push"
      contains: "onDocumentCreated"
    - path: "firestore.rules"
      provides: "Updated notification type allowlist with deal, legal, announcement, rfq_created"
  key_links:
    - from: "functions/index.js (onRFQCreated)"
      to: "users collection (role query)"
      via: "Query all members, send in-app + push to each"
      pattern: "role.*member"
    - from: "functions/index.js (broadcastQuoteRequests)"
      to: "messaging.send"
      via: "Push notification to provider after quote request creation"
      pattern: "messaging\\.send"
---

<objective>
Add four new push notification event triggers (new member registered, RFQ created, quote broadcast, system announcement) and debug/fix the existing FCM setup. Update Firestore rules notification type allowlist.

Purpose: Push notifications currently only cover deal events. This plan extends coverage to member registration, RFQ, and quote broadcast events, and ensures the FCM pipeline actually delivers.
Output: New CF triggers, FCM fix, updated Firestore rules.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notifications-and-email-system/12-CONTEXT.md
@.planning/phases/12-notifications-and-email-system/12-RESEARCH.md
@.planning/phases/12-notifications-and-email-system/12-02-SUMMARY.md

@functions/index.js
@firestore.rules
@src/hooks/usePushNotifications.js
@public/firebase-messaging-sw.js
@src/presentation/components/features/notifications/NotificationListener.jsx

<interfaces>
<!-- Key patterns from functions/index.js -->

sendDealNotifications pattern (3-channel):
1. Create in-app notification doc in users/{uid}/notifications
2. FCM push via messaging.send({ token, data }) with preference check
3. Email via sendDealEmail with preference check
All non-blocking (try/catch), all OUTSIDE transactions.

Invalid FCM token cleanup:
- On messaging/invalid-registration-token or messaging/registration-token-not-registered
- Delete fcmToken field from user doc

Notification preference check:
- `userData.preferences?.deals?.push !== false` (default true)
- Category names: deals, messages, legal, providers, system

Firestore rules notification type allowlist (line ~87):
['message', 'quote', 'system', 'admin', 'new_message', 'conversation_created',
 'quote_received', 'quote_accepted', 'quote_rejected', 'new_user_approval']
Missing: 'deal', 'legal', 'announcement', 'rfq_created'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Debug and fix FCM push notification pipeline</name>
  <files>src/hooks/usePushNotifications.js, public/firebase-messaging-sw.js, src/presentation/components/features/notifications/NotificationListener.jsx</files>
  <action>
1. **Diagnostic checklist** — inspect these files and verify:
   - `usePushNotifications.js`: Does `getToken()` use `NEXT_PUBLIC_FIREBASE_VAPID_KEY` from env? Is the VAPID key correctly read (`process.env.NEXT_PUBLIC_FIREBASE_VAPID_KEY`)? Is the service worker registration path correct (`/firebase-messaging-sw.js`)?
   - `public/firebase-messaging-sw.js`: Does `importScripts` load the correct Firebase SDK version? Is `firebase.initializeApp(firebaseConfig)` called with the correct project config? Is `firebase.messaging()` properly initialized?
   - `NotificationListener.jsx`: Does `onMessage` handler correctly process incoming foreground messages? Does it create the toast/notification display?

2. **Common FCM issues to fix** (check and fix all that apply):
   - VAPID key: Ensure `NEXT_PUBLIC_FIREBASE_VAPID_KEY` is referenced in `getToken({ vapidKey: ... })`. If the env var name is wrong or the vapidKey option is missing, fix it.
   - Service worker scope: Ensure the SW at `/firebase-messaging-sw.js` is registered with the correct scope. If `navigator.serviceWorker.register` uses a wrong path, fix it.
   - Token lifecycle: Ensure `getToken` result is saved to Firestore (`users/{uid}.fcmToken`). Verify the save happens after permission grant AND on app reload (token can change).
   - Background handler: Ensure `firebase-messaging-sw.js` has `messaging.onBackgroundMessage` handler that shows a notification via `self.registration.showNotification`.
   - Foreground handler: Ensure `NotificationListener.jsx` uses `onMessage(messaging, callback)` from firebase/messaging SDK v9 modular API.

3. **Fix any issues found.** Document the root cause in a code comment. If no bugs found (the issue was env config), add a comment noting that VAPID_KEY must be set in .env.local and production env.

4. Ensure the `NotificationListener.jsx` foreground handler properly dispatches for ALL notification types including the new ones being added (deal, legal, announcement, rfq_created). The handler should be generic enough to show any notification with a title and body.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>FCM pipeline verified: VAPID key correctly used in getToken, service worker properly initialized, foreground and background handlers work for all notification types. Root cause of any issues documented in code comments.</done>
</task>

<task type="auto">
  <name>Task 2: Add new push notification triggers and update Firestore rules</name>
  <files>functions/index.js, firestore.rules</files>
  <action>
1. **New member registered -> admin push**: Add logic at the end of the `inviteUser` CF (or in an appropriate existing trigger) to notify admins when a new user account is created. Query users where `role === 'admin'`, create in-app notification (type: 'new_user_approval') and send FCM push to each admin with fcmToken. Check `preferences?.system?.push !== false`. Use the established `sendDealNotifications` pattern: in-app + push, non-blocking, outside transactions.

2. **New RFQ created -> all members push**: Add an `onDocumentCreated` trigger on `requests/{requestId}` (or the collection path where RFQ/quote request docs are created). When triggered:
   - Query all users with `role === 'member'`
   - For each member: create in-app notification (type: 'rfq_created', title: 'New RFQ Available', link to relevant page) and send FCM push
   - Check `preferences?.providers?.push !== false` for push (RFQ is a platform event visible to all members)
   - Check `preferences?.providers?.email !== false` for email (send via buildBrandedEmailHtml from Plan 02)
   - Use invalid FCM token cleanup pattern on send errors

3. **Quote request broadcast -> provider push**: In the `broadcastQuoteRequests` CF (line ~2584), after creating each `quoteRequests` doc, add push notification to the target provider:
   - Read provider user doc for fcmToken and preferences
   - Create in-app notification (type: 'quote_received') in provider's notifications subcollection
   - Send FCM push if `preferences?.providers?.push !== false`
   - Non-blocking try/catch, invalid token cleanup

4. **Update Firestore rules notification type allowlist**: In `firestore.rules`, find the notification type allowlist (line ~87) and add the missing types:
   ```
   'deal', 'legal', 'announcement', 'rfq_created'
   ```
   Keep existing types. The full updated list should be:
   `['message', 'quote', 'system', 'admin', 'new_message', 'conversation_created', 'quote_received', 'quote_accepted', 'quote_rejected', 'new_user_approval', 'deal', 'legal', 'announcement', 'rfq_created']`

5. All notification side effects called OUTSIDE Firestore transactions (established pattern). All FCM sends wrapped in try/catch with invalid token cleanup.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -c "rfq_created\|announcement" firestore.rules | xargs -I{} test {} -ge 2 && echo "PASS: rules updated" || echo "FAIL: rules not updated"</automated>
  </verify>
  <done>Three new push event triggers working (new member -> admin, RFQ -> members, quote broadcast -> provider). Firestore rules notification type allowlist includes deal, legal, announcement, rfq_created. All push sends respect notification preferences and include invalid token cleanup.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- `grep "rfq_created" firestore.rules` shows the type in the allowlist
- `grep "announcement" firestore.rules` shows the type in the allowlist
- `grep "onDocumentCreated.*requests" functions/index.js` shows RFQ trigger
- FCM code uses VAPID key correctly in getToken
</verification>

<success_criteria>
- FCM push pipeline is functional (token obtained, messages delivered foreground and background)
- Admin receives in-app + push when new member registers
- All members receive in-app + push when new RFQ is created
- Providers receive in-app + push when quote request is broadcast to them
- All push events respect Phase 10 notification preferences
- Firestore rules allow all notification types used by Cloud Functions
</success_criteria>

<output>
After completion, create `.planning/phases/12-notifications-and-email-system/12-04-SUMMARY.md`
</output>
