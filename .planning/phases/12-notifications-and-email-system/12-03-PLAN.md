---
phase: 12-notifications-and-email-system
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/presentation/components/features/news/NewsDetailClient.jsx
autonomous: true
requirements:
  - NOTIF-08

must_haves:
  truths:
    - "News detail page has a LinkedIn share button that opens the LinkedIn share dialog"
    - "News detail page has a copy button that copies article title + snippet + URL to clipboard"
    - "Share buttons appear only on the detail page, not on homepage news cards"
  artifacts:
    - path: "src/presentation/components/features/news/NewsDetailClient.jsx"
      provides: "LinkedIn share and copy-to-clipboard buttons"
  key_links:
    - from: "NewsDetailClient.jsx"
      to: "linkedin.com/sharing/share-offsite"
      via: "window.open with encoded article URL"
      pattern: "share-offsite"
---

<objective>
Add LinkedIn share button and copy-to-clipboard button to the news detail page.

Purpose: Enable users to share news articles on LinkedIn and copy formatted article text for other platforms.
Output: Two new buttons on NewsDetailClient.jsx.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-notifications-and-email-system/12-CONTEXT.md

@src/presentation/components/features/news/NewsDetailClient.jsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add LinkedIn share and copy-to-clipboard buttons to news detail page</name>
  <files>src/presentation/components/features/news/NewsDetailClient.jsx</files>
  <action>
1. In `NewsDetailClient.jsx`, add two buttons near the article title or at the top of the article content area:
   - **LinkedIn button**: Lucide `Linkedin` icon. On click:
     ```javascript
     const articleUrl = `${window.location.origin}/news/${newsId}`;
     const url = `https://www.linkedin.com/sharing/share-offsite/?url=${encodeURIComponent(articleUrl)}`;
     window.open(url, '_blank', 'noopener,noreferrer');
     ```
   - **Copy button**: Lucide `Copy` icon (switches to `Check` icon for 2 seconds after copy). On click:
     ```javascript
     const articleUrl = `${window.location.origin}/news/${newsId}`;
     const copyText = `${article.title}\n\n${article.content?.slice(0, 200)}...\n\n${articleUrl}`;
     navigator.clipboard.writeText(copyText).then(() => {
       toast.success('Copied to clipboard');
     });
     ```
   - Use `useState` for the copy icon toggle (Copy -> Check for 2s via setTimeout)

2. Styling: Buttons side by side, subtle dark bg with gold hover, rounded, small icon size (18-20px). Place them in a row below the article title or next to the date. Match the dark theme and glass-card aesthetic.

3. Import `Linkedin`, `Copy`, `Check` from lucide-react. Import `toast` from react-hot-toast.

4. These buttons appear ONLY on the detail page (this component) — do NOT add to homepage news cards.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>LinkedIn share button opens LinkedIn share dialog with article URL. Copy button copies formatted text (title + snippet + URL) to clipboard with toast feedback. Buttons only on detail page.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- LinkedIn button present on news detail page
- Copy button present on news detail page
- No share buttons on homepage news cards
</verification>

<success_criteria>
- LinkedIn share button opens LinkedIn share dialog in new tab with article URL
- Copy button copies article title + first 200 chars + URL to clipboard
- Toast shows "Copied to clipboard" on successful copy
- Copy icon switches to check icon for 2 seconds after copy
- Buttons styled consistently with dark theme and gold hover accents
</success_criteria>

<output>
After completion, create `.planning/phases/12-notifications-and-email-system/12-03-SUMMARY.md`
</output>
