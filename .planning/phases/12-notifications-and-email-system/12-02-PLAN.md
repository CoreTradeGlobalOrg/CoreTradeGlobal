---
phase: 12-notifications-and-email-system
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - functions/index.js
autonomous: true
requirements:
  - NOTIF-03
  - NOTIF-04

must_haves:
  truths:
    - "All automated emails are sent from noreply@coretradeglobal.com"
    - "All system emails use a consistent branded HTML template with CTG logo, gold accents, and unsubscribe footer"
    - "Message notification emails are throttled to max 1 per user per day"
  artifacts:
    - path: "functions/index.js"
      provides: "buildBrandedEmailHtml helper, updated sender addresses, message email throttle"
      contains: "buildBrandedEmailHtml"
  key_links:
    - from: "sendDealEmail"
      to: "buildBrandedEmailHtml"
      via: "Shared template wrapper replacing buildDealEmailHtml"
      pattern: "buildBrandedEmailHtml"
    - from: "sendMessageNotification"
      to: "lastMessageEmailSentAt"
      via: "24h throttle check before sending message digest email"
      pattern: "lastMessageEmailSentAt"
---

<objective>
Refactor email infrastructure: change sender address to noreply@, create a shared branded HTML email template replacing duplicated builders, and add message email throttling (max 1/day per user).

Purpose: Consistent professional email branding across all system emails, and prevent message notification email spam.
Output: Updated functions/index.js with buildBrandedEmailHtml, sender address change, and message email throttle.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notifications-and-email-system/12-CONTEXT.md
@.planning/phases/12-notifications-and-email-system/12-RESEARCH.md

@functions/index.js

<interfaces>
<!-- Key locations in functions/index.js -->

From functions/index.js:
- Line ~131: `inviteUser` CF — sends sign-in link email via Resend with `from: 'CoreTradeGlobal <info@coretradeglobal.com>'`
- Line ~1683: `sendDealEmail(to, subject, html)` — helper that sends via Resend with `from: 'CoreTradeGlobal <info@coretradeglobal.com>'`
- Line ~1767: `buildDealEmailHtml(heading, body, ctaLabel, ctaUrl)` — HTML email builder for deal emails
- Line ~3879: `buildLegalEmailHtml(heading, body, ctaLabel, ctaUrl)` — duplicate HTML builder for legal emails
- Line ~935: `sendMessageNotification` — currently sends FCM push only, no email
- Line ~1805-1895: `sendDealNotifications` — 3-channel orchestrator pattern (in-app -> push -> email)

Resend domain: coretradeglobal.com is already verified. Changing From address to noreply@ at same domain requires no DNS changes.

Phase 9 unsubscribe: Signed HMAC token system exists. Unsubscribe footer link format: /unsubscribe?email={email}&sig={sig}
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create shared branded email template and update sender address</name>
  <files>functions/index.js</files>
  <action>
1. Create a new `buildBrandedEmailHtml(body, ctaLabel, ctaUrl, footerNote)` function that produces a responsive HTML email template with:
   - Header: CTG logo text "CoreTradeGlobal" in white on dark (#0F1C2E) background with gold (#FFD700) accent bar below
   - Body area: Dark background (#1A2332), white text, renders the `body` HTML string
   - CTA button: Gold (#FFD700) background, black text, links to `ctaUrl` with label `ctaLabel` (optional — skip if not provided)
   - Footer: Light gray text, "You received this email because you have an account on CoreTradeGlobal." + unsubscribe link text (link to `/unsubscribe` — the actual signed URL will be generated per-recipient by callers if needed, otherwise just link to the settings notification preferences page `/settings`)
   - `footerNote` is optional additional text above the standard footer line
   - Use inline CSS only (email clients strip <style> tags)
   - Table-based layout for email client compatibility

2. Update all callers of `buildDealEmailHtml` and `buildLegalEmailHtml` to use `buildBrandedEmailHtml` instead. The parameters differ slightly — map them:
   - Old: `buildDealEmailHtml(heading, body, ctaLabel, ctaUrl)` — wrap heading into body: `<h2>${heading}</h2>${body}`
   - Do the same for `buildLegalEmailHtml`
   - After updating all call sites, remove both old functions

3. Change sender address from `'CoreTradeGlobal <info@coretradeglobal.com>'` to `'CoreTradeGlobal <noreply@coretradeglobal.com>'` in:
   - `sendDealEmail` function (covers deal, legal, and all emails routed through it)
   - `inviteUser` CF (sends directly via Resend, separate From address)
   - Any other direct Resend `.send()` calls — search for `info@coretradeglobal.com` to find all

4. The domain coretradeglobal.com is already verified in Resend. No DNS changes needed for noreply@ at the same domain.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -c "info@coretradeglobal.com" functions/index.js && echo "FAIL: old sender still present" || echo "PASS: no old sender found"</automated>
  </verify>
  <done>All emails use noreply@coretradeglobal.com sender. buildBrandedEmailHtml is the single email template. Old buildDealEmailHtml and buildLegalEmailHtml are removed. All call sites updated.</done>
</task>

<task type="auto">
  <name>Task 2: Add message email throttling in sendMessageNotification</name>
  <files>functions/index.js</files>
  <action>
1. In the `sendMessageNotification` Cloud Function (currently FCM-push-only), add email sending with a 24-hour per-user throttle:
   - After the existing FCM push logic, add an email section
   - Read `userData.lastMessageEmailSentAt` from the user doc (already fetched for FCM token)
   - Check `userData.preferences?.messages?.email !== false` (default true — follows established preference check pattern)
   - If `lastMessageEmailSentAt` exists and `Date.now() - lastMessageEmailSentAt.toMillis() < 86400000` (24h), skip email with a console.log throttle message
   - If not throttled: send email via `sendDealEmail(userData.email, 'You have new messages on CoreTradeGlobal', html)` using `buildBrandedEmailHtml` with a simple body ("You have unread messages. Visit CoreTradeGlobal to read them.") and CTA "View Messages" linking to the conversations page
   - After successful send, update user doc: `db.collection('users').doc(recipientId).update({ lastMessageEmailSentAt: Timestamp.now() })`
   - Accept low-probability race condition for concurrent sends (documented in RESEARCH.md Pitfall 3) — the cost of a Firestore transaction is not worth it for a 1/day throttle
   - Wrap in try/catch — email failure must not fail the CF (non-blocking, same as sendDealEmail pattern)

2. Add comment explaining the throttle: "Max 1 'new messages' email per user per day across all conversations. Race-safe enough for a daily throttle — rare double-send is acceptable."
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && grep -c "lastMessageEmailSentAt" functions/index.js | xargs -I{} test {} -ge 2 && echo "PASS: throttle implemented" || echo "FAIL: throttle not found"</automated>
  </verify>
  <done>sendMessageNotification sends throttled email (max 1/day per user). Email respects preferences.messages.email preference. Throttle tracking uses lastMessageEmailSentAt field on user doc.</done>
</task>

</tasks>

<verification>
- `grep -c "info@coretradeglobal.com" functions/index.js` returns 0
- `grep -c "noreply@coretradeglobal.com" functions/index.js` returns >= 2
- `grep -c "buildBrandedEmailHtml" functions/index.js` returns >= 3 (definition + call sites)
- `grep -c "buildDealEmailHtml\|buildLegalEmailHtml" functions/index.js` returns 0 (old functions removed)
- `grep -c "lastMessageEmailSentAt" functions/index.js` returns >= 2
</verification>

<success_criteria>
- All automated emails sent from noreply@coretradeglobal.com
- Single buildBrandedEmailHtml template with CTG branding (logo header, gold accents, dark theme, footer)
- Old duplicate email builders removed
- Message notification emails throttled to max 1 per user per 24 hours
- Throttle respects notification preferences
</success_criteria>

<output>
After completion, create `.planning/phases/12-notifications-and-email-system/12-02-SUMMARY.md`
</output>
