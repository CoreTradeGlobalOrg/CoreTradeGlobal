---
phase: 12-notifications-and-email-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/notifications/page.jsx
  - src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx
  - src/presentation/components/features/notifications/NotificationCenterPage/NotificationFilterTabs.jsx
  - src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterItem.jsx
  - src/presentation/components/features/notifications/NotificationCenterPage/NotificationBulkActions.jsx
  - src/presentation/components/common/NotificationBell/NotificationBell.jsx
  - src/data/repositories/NotificationRepository.js
  - src/middleware.js
autonomous: true
requirements:
  - NOTIF-01
  - NOTIF-02

must_haves:
  truths:
    - "User can access /notifications from 'View all' link in bell dropdown"
    - "Notification center shows chronological list with type filter tabs (All, Deals, Messages, Legal, Providers, System)"
    - "User can scroll down to load older notifications via infinite scroll"
    - "User can select individual notifications with checkboxes and batch mark-read or delete"
    - "Bell dropdown shows 10 items instead of 5"
    - "Clicking a notification marks it as read and navigates to the relevant page"
  artifacts:
    - path: "src/app/(main)/notifications/page.jsx"
      provides: "Route shell for /notifications"
    - path: "src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx"
      provides: "Orchestrator with tabs, infinite scroll, bulk actions"
      min_lines: 80
    - path: "src/presentation/components/common/NotificationBell/NotificationBell.jsx"
      provides: "Extended bell with 10 items and View all link"
    - path: "src/data/repositories/NotificationRepository.js"
      provides: "Cursor-based pagination method getByUserIdAfter"
  key_links:
    - from: "NotificationBell.jsx"
      to: "/notifications"
      via: "View all link at bottom of dropdown"
      pattern: "href.*notifications"
    - from: "NotificationCenterPage.jsx"
      to: "NotificationRepository.js"
      via: "getByUserIdAfter for infinite scroll pagination"
      pattern: "getByUserIdAfter"
    - from: "NotificationCenterPage.jsx"
      to: "MessagesContext"
      via: "useMessages for real-time notification window"
      pattern: "useMessages"
---

<objective>
Build the full-page notification center at /notifications with filter tabs, infinite scroll, and bulk actions, and extend the NotificationBell dropdown from 5 to 10 items with a "View all" link.

Purpose: Users currently see only 5 notifications in a dropdown. This plan gives them a full page to browse, filter, and manage all their notification history.
Output: /notifications page, extended bell dropdown, cursor pagination in NotificationRepository.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-notifications-and-email-system/12-CONTEXT.md
@.planning/phases/12-notifications-and-email-system/12-RESEARCH.md

@src/presentation/components/common/NotificationBell/NotificationBell.jsx
@src/data/repositories/NotificationRepository.js
@src/presentation/context/MessagesContext.jsx
@src/hooks/useScrollLoadMore.js
@src/middleware.js

<interfaces>
<!-- Key types and contracts the executor needs -->

From src/data/repositories/NotificationRepository.js:
- `getByUserId(userId, limitCount = 50)` — one-time query, returns array
- `subscribeToUserNotifications(userId, onData, onError)` — real-time, limit 50
- `markAsRead(userId, notificationId)` — sets read: true
- `markAllAsRead(userId)` — batch update all unread
- `deleteNotification(userId, notificationId)` — single delete
- `deleteAllNotifications(userId)` — batch delete all

From src/presentation/context/MessagesContext.jsx:
- `useMessages()` hook provides `{ notifications, unreadCount, ... }`
- Subscription limit is currently 50 (hardcoded in subscribeToUserNotifications)

From src/presentation/components/common/NotificationBell/NotificationBell.jsx:
- Line 107: `const recentNotifications = notifications.slice(0, 5);`
- Has type-specific icon mapping and click handler with navigation

From src/data/datasources/FirestoreDataSource.js:
- `querySubcollection` supports `startAfter` parameter (line 164)
- `subscribeToSubcollection` does NOT support `startAfter`

Notification type-to-tab mapping (from RESEARCH.md):
```
deal -> deals, legal -> legal, message/new_message/conversation_created -> messages,
quote/quote_received/quote_accepted/quote_rejected -> providers,
system/admin/new_user_approval/announcement/rfq_created -> system
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add cursor pagination to NotificationRepository and extend NotificationBell to 10 items with "View all" link</name>
  <files>src/data/repositories/NotificationRepository.js, src/presentation/components/common/NotificationBell/NotificationBell.jsx, src/middleware.js</files>
  <action>
1. In `NotificationRepository.js`, add a new method `getByUserIdAfter(userId, afterDoc, limitCount = 30)` that calls `this.firestoreDataSource.querySubcollection` with `startAfter: afterDoc`, `orderBy: [['createdAt', 'desc']]`, and `limit: limitCount`. This enables cursor-based pagination for the notification center's infinite scroll. Do NOT modify `subscribeToUserNotifications` — it stays real-time for the bell dropdown.

2. In `NotificationBell.jsx`, change `notifications.slice(0, 5)` to `notifications.slice(0, 10)` so the dropdown shows 10 items. Add a "View all" link at the bottom of the dropdown list (below the notifications, above or replacing the existing bottom area) that navigates to `/notifications` using Next.js `Link`. Style it as a centered text link with gold accent color, matching the existing dropdown theme.

3. In `src/middleware.js`, add `/notifications` to the `protectedRoutes` array so only authenticated users can access it.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>NotificationRepository has getByUserIdAfter method. Bell dropdown shows 10 items with "View all" link to /notifications. /notifications is protected in middleware.</done>
</task>

<task type="auto">
  <name>Task 2: Build the /notifications page with filter tabs, infinite scroll, and bulk actions</name>
  <files>src/app/(main)/notifications/page.jsx, src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterPage.jsx, src/presentation/components/features/notifications/NotificationCenterPage/NotificationFilterTabs.jsx, src/presentation/components/features/notifications/NotificationCenterPage/NotificationCenterItem.jsx, src/presentation/components/features/notifications/NotificationCenterPage/NotificationBulkActions.jsx</files>
  <action>
1. Create `src/app/(main)/notifications/page.jsx` as a thin server component shell that renders `NotificationCenterPage` (loaded via `next/dynamic` with `ssr: false` to avoid hydration issues with real-time subscriptions). Add auth guard using `useEffect + router.replace('/login')` if no user (defense in depth with middleware).

2. Create `NotificationCenterPage.jsx` as the orchestrator:
   - Use `useMessages()` from MessagesContext to get the real-time notification window as page 1.
   - Use `useScrollLoadMore` (existing Phase 7 hook) with an `IntersectionObserver` sentinel div for infinite scroll.
   - When sentinel is visible, call `NotificationRepository.getByUserIdAfter(userId, lastDoc, 30)` using the last notification from current list as cursor. Append results to a `historyNotifications` state array. Track `hasMore` (false when fetch returns < 30).
   - Combine `notifications` (from context, real-time) with `historyNotifications` (from pagination, static) for display. Deduplicate by ID.
   - Active tab state filters the combined list using the type-to-tab mapping:
     ```
     const TYPE_TO_TAB = {
       deal: 'deals', legal: 'legal',
       message: 'messages', new_message: 'messages', conversation_created: 'messages',
       quote: 'providers', quote_received: 'providers', quote_accepted: 'providers', quote_rejected: 'providers',
       system: 'system', admin: 'system', new_user_approval: 'system', announcement: 'system', rfq_created: 'system',
     };
     ```
   - Selection state: `const [selected, setSelected] = useState(new Set())` for checkbox batch operations.
   - Pass appropriate props to sub-components.

3. Create `NotificationFilterTabs.jsx`:
   - Tabs: All | Deals | Messages | Legal | Providers | System
   - Use the existing tab styling pattern from admin page (gold underline for active tab, dark bg)
   - `activeTab` and `onTabChange` props

4. Create `NotificationCenterItem.jsx`:
   - Single notification row with: checkbox (left), type-specific icon (reuse mapping from NotificationBell), title (bold if unread), message snippet, relative timestamp (right), small gold accent dot for unread.
   - Clicking the row (not checkbox) calls `markAsRead` and navigates to `notification.link` using router.push.
   - Read notifications have dimmer text (opacity-60 or similar).
   - Checkbox toggles selection via parent callback.

5. Create `NotificationBulkActions.jsx`:
   - Sticky bar at top (below tabs): "Mark all as read" button, "Delete all" button, and when `selected.size > 0`: "Mark selected as read ({n})" and "Delete selected ({n})" buttons.
   - "Mark all as read" calls `notificationRepository.markAllAsRead(userId)`.
   - "Delete all" calls `notificationRepository.deleteAllNotifications(userId)` with a confirmation prompt.
   - Batch selected uses `Promise.all` with individual `markAsRead` or `deleteNotification` calls, then clears selection.
   - Show react-hot-toast success feedback after each bulk operation.

6. Empty state: When no notifications exist (after filtering), show bell icon (Lucide `Bell`) + "No notifications yet" centered text, matching the glass-card dark theme.

7. Styling: Use `bg-[rgba(255,255,255,0.03)]` glass-card pattern, gold accents for active states, dark theme consistent with rest of app. Page title "Notifications" at top.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Full /notifications page renders with filter tabs, infinite scroll loads older notifications, bulk mark-read/delete works, individual checkbox selection works, empty state displays correctly, clicking notification marks as read and navigates.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- /notifications page renders when navigated to from bell dropdown "View all" link
- Bell dropdown shows 10 items instead of 5
- Filter tabs filter notifications by type category
- Infinite scroll loads older notifications when scrolling down
- Bulk actions (mark all read, delete all, batch selected) work correctly
</verification>

<success_criteria>
- Bell dropdown shows 10 notifications with "View all" link at bottom
- /notifications page loads with filter tabs (All, Deals, Messages, Legal, Providers, System)
- Infinite scroll loads older notifications via cursor pagination
- Individual and bulk mark-read/delete operations work
- Unread notifications have bold title + accent dot; read are dimmer
- Empty state shows bell icon + message
- Clicking a notification marks as read and navigates to relevant page
</success_criteria>

<output>
After completion, create `.planning/phases/12-notifications-and-email-system/12-01-SUMMARY.md`
</output>
