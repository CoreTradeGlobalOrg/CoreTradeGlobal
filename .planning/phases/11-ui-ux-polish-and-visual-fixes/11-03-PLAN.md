---
phase: 11-ui-ux-polish-and-visual-fixes
plan: 03
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/app/(main)/fairs/page.js
  - src/presentation/components/features/deal/DealFormFields.jsx
  - src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx
  - src/presentation/components/features/admin/FairsManager/FairForm.jsx
  - src/presentation/components/features/provider/ShipmentUpdateForm.jsx
  - src/presentation/components/features/request/SubmitQuoteDialog/SubmitQuoteDialog.jsx
autonomous: true
requirements:
  - UI-07
  - UI-08
  - UI-09
  - UI-10

must_haves:
  truths:
    - "Fairs listing page shows ongoing fairs pinned first, then upcoming, then past in a collapsed section"
    - "Past fairs are in a collapsible section (collapsed by default) with a chevron toggle"
    - "Fairs listing page cards have the same gold card treatment as homepage cards"
    - "Fairs listing page fair cards show country flag in date box"
    - "All native date inputs are replaced with the existing DatePicker component"
    - "Delete buttons across the platform have clear, unambiguous wording"
  artifacts:
    - path: "src/app/(main)/fairs/page.js"
      provides: "Sorted fairs with collapsible past section and gold cards"
      contains: "pastExpanded"
    - path: "src/presentation/components/features/deal/DealFormFields.jsx"
      provides: "DatePicker instead of native date input"
      contains: "DatePicker"
    - path: "src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx"
      provides: "DatePicker instead of native date input"
      contains: "DatePicker"
  key_links:
    - from: "fairs/page.js"
      to: "CountryFlag component"
      via: "import and render in listing page fair cards"
      pattern: "CountryFlag"
    - from: "DealFormFields.jsx"
      to: "DatePicker component"
      via: "import replacing native input"
      pattern: "DatePicker"
---

<objective>
Improve fairs page UX with sorting and collapsible past section, replace all native date inputs with the project's DatePicker component, and clarify delete button wording.

Purpose: Fairs page currently shows all fairs in a flat grid — ongoing and past mixed together. Sorting and collapsing past fairs improves signal-to-noise. Native date inputs look inconsistent with the dark theme; the existing DatePicker matches CTG design. Delete button labels need clarity to prevent user confusion.

Output: Polished fairs page, consistent date pickers, clear delete wording.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-CONTEXT.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-RESEARCH.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-01-SUMMARY.md

@src/app/(main)/fairs/page.js
@src/presentation/components/features/deal/DealFormFields.jsx
@src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx
@src/presentation/components/features/admin/FairsManager/FairForm.jsx
@src/presentation/components/features/provider/ShipmentUpdateForm.jsx
@src/presentation/components/features/request/SubmitQuoteDialog/SubmitQuoteDialog.jsx
@src/presentation/components/common/DatePicker/DatePicker.jsx

<interfaces>
<!-- DatePicker component (already exists) -->
import { DatePicker } from '@/presentation/components/common/DatePicker/DatePicker';
// Props: value (Date object), onChange (Date => void), theme variant, portal rendering
// CRITICAL: DatePicker works with Date objects, NOT YYYY-MM-DD strings.
// When replacing native <input type="date">, add conversion:
//   - String to Date: new Date(stringValue + 'T00:00:00')
//   - Date to String: date.toISOString().split('T')[0]
// If using react-hook-form, wrap with Controller component.

<!-- CountryFlag component -->
import { CountryFlag } from '@/presentation/components/common/CountryFlag/CountryFlag';

<!-- Lucide icons for collapsible section -->
import { ChevronDown, ChevronUp } from 'lucide-react';

<!-- Shared gold card CSS classes (from Plan 01 globals.css) -->
.card-surface, .card-border-gold, .card-bottom-gold, .card-hover-gold
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fairs listing page — sort by status, collapsible past section, gold card treatment, country flag</name>
  <files>src/app/(main)/fairs/page.js</files>
  <action>
1. In `src/app/(main)/fairs/page.js`:
   - Import `useState` if not already imported.
   - Import `ChevronDown, ChevronUp` from `lucide-react`.
   - Import `CountryFlag` from `@/presentation/components/common/CountryFlag/CountryFlag`.

2. After fetching/receiving the fairs data, partition into three buckets using the fair's date fields:
   - Determine fair status: compare `startDate` and `endDate` against `new Date()`. If `endDate < now` => past. If `startDate <= now && endDate >= now` => ongoing. If `startDate > now` => upcoming.
   - `ongoing`: sorted by `startDate` ascending (soonest first).
   - `upcoming`: sorted by `startDate` ascending (soonest first).
   - `past`: sorted by `startDate` descending (newest first).
   - Use the existing date parsing approach from the codebase (Firestore Timestamps may need `.toDate()` conversion).

3. Add state: `const [pastExpanded, setPastExpanded] = useState(false);`

4. Render the main grid with `[...ongoing, ...upcoming]`. These are the active fairs the user cares about.

5. Below the main grid, render the past fairs collapsible section:
   ```jsx
   {past.length > 0 && (
     <div className="mt-12">
       <div className="flex items-center gap-4 mb-6">
         <hr className="flex-1 border-[rgba(255,255,255,0.1)]" />
         <button
           onClick={() => setPastExpanded(v => !v)}
           className="flex items-center gap-2 text-[#A0A0A0] text-sm hover:text-white transition-colors"
         >
           Past Fairs ({past.length})
           {pastExpanded ? <ChevronUp className="w-4 h-4" /> : <ChevronDown className="w-4 h-4" />}
         </button>
         <hr className="flex-1 border-[rgba(255,255,255,0.1)]" />
       </div>
       {pastExpanded && (
         <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-8">
           {past.map(fair => /* render fair card */)}
         </div>
       )}
     </div>
   )}
   ```

6. Apply gold card treatment to the listing page fair cards. The listing page has its own inline card JSX (different from homepage FairsSection). Apply the same gold styles:
   - Gold border: `border: 1px solid rgba(255, 215, 0, 0.2)`
   - Lighter bg: `rgba(255, 255, 255, 0.06)`
   - Gold bottom border: `border-bottom: 2px solid rgba(255, 215, 0, 0.4)`
   - Hover glow: `border-color: rgba(255, 215, 0, 0.5); box-shadow: 0 0 12px rgba(255, 215, 0, 0.1)` with 300ms transition
   - Use the shared CSS classes from Plan 01 (card-border-gold, card-hover-gold, etc.) or apply equivalent inline.

7. Add CountryFlag to the listing page fair card date box (same pattern as homepage FairCard in Plan 02):
   ```jsx
   {fair.country && <CountryFlag countryCode={fair.country} size={20} />}
   ```

PITFALL: The listing page and homepage have DUAL FairCard implementations. Both must be updated. Plan 02 handles homepage FairsSection.jsx. This task handles fairs/page.js. Ensure visual parity between both.

8. Ensure the existing text search filter still works — it should filter from the pre-sorted arrays (ongoing, upcoming, past). Apply search filter BEFORE partitioning, or partition first and filter each bucket.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Fairs listing page shows ongoing fairs first, then upcoming, then past in a collapsible section. Past section is collapsed by default with chevron toggle. All listing page fair cards have gold treatment and country flag. Text search still works. No build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Replace native date inputs with DatePicker and clarify delete button wording</name>
  <files>src/presentation/components/features/deal/DealFormFields.jsx, src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx, src/presentation/components/features/admin/FairsManager/FairForm.jsx, src/presentation/components/features/provider/ShipmentUpdateForm.jsx, src/presentation/components/features/request/SubmitQuoteDialog/SubmitQuoteDialog.jsx</files>
  <action>
**DatePicker replacements:**

For each of the 5 files below, replace native `<input type="date">` with the project's `DatePicker` component:

1. `src/presentation/components/features/deal/DealFormFields.jsx` — Find `<input type="date">` for offer expiry date. Import DatePicker. If using react-hook-form, wrap with `Controller`:
   ```jsx
   <Controller
     name="expiryDate"
     control={control}
     render={({ field }) => (
       <DatePicker
         value={field.value ? new Date(field.value) : null}
         onChange={(date) => field.onChange(date ? date.toISOString().split('T')[0] : '')}
       />
     )}
   />
   ```
   If using manual useState, adapt similarly — key is converting between Date objects (DatePicker) and strings (form state/Firestore).

2. `src/presentation/components/features/deal/CounterOfferForm/CounterOfferForm.jsx` — Same pattern: find native date input, replace with DatePicker + Controller wrapper.

3. `src/presentation/components/features/admin/FairsManager/FairForm.jsx` — Fair start/end date inputs. Replace both with DatePicker.

4. `src/presentation/components/features/provider/ShipmentUpdateForm.jsx` — ETA date input. Replace with DatePicker.

5. `src/presentation/components/features/request/SubmitQuoteDialog/SubmitQuoteDialog.jsx` — Quote validity date. Replace with DatePicker.

CRITICAL: DatePicker uses Date objects. Native inputs use YYYY-MM-DD strings. Add conversion adapters:
- Input to DatePicker: `new Date(stringValue + 'T00:00:00')` (add time to avoid timezone shift)
- DatePicker to form: `date.toISOString().split('T')[0]`
- Handle null/empty gracefully — pass `null` to DatePicker when no date is set.

Read each file first to understand whether it uses react-hook-form (Controller) or manual useState, and adapt accordingly.

**Delete button wording:**

Scan and update delete button labels for clarity across the platform. Key files to check:
- `DangerSection.jsx` (settings page) — "Delete Account" should read "Permanently Delete Account" or similar
- Admin pages: NewsManager, FairsManager, ProductList — ensure delete buttons say "Delete [item name]" not just "Delete" or ambiguous "Remove"
- ProductForm — if there's a delete/remove product action, clarify wording
- Any modal confirmation dialogs — ensure they clearly state what is being deleted

Use your judgment for specific rewording. The goal is that no user could accidentally click a delete button thinking it does something else. Typical pattern: "Delete [Noun]" or "Remove [Noun]" with a confirmation step.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>All 5 files with native date inputs now use the project's DatePicker component. Date conversion adapters handle string-to-Date and Date-to-string correctly. Delete buttons across the platform have clear, unambiguous labels. No build errors.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- No `<input type="date">` remains in the 5 target files — all replaced with DatePicker
- Fairs page shows ongoing/upcoming in main grid and past in collapsible section
- `pastExpanded` state controls the collapsible section
- CountryFlag rendered in fairs listing page cards
- Delete buttons have clear descriptive labels
</verification>

<success_criteria>
- Fairs page shows sorted fairs with collapsible past section
- All date inputs use consistent DatePicker component matching CTG dark theme
- Delete buttons clearly communicate their destructive action
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-ux-polish-and-visual-fixes/11-03-SUMMARY.md`
</output>
