---
phase: 11-ui-ux-polish-and-visual-fixes
plan: 02
type: execute
wave: 2
depends_on: ["11-01"]
files_modified:
  - src/presentation/components/homepage/Fairs/FairsSection.jsx
  - src/presentation/components/homepage/Products/FeaturedProducts.jsx
  - src/presentation/components/homepage/News/NewsSection.jsx
  - src/presentation/components/homepage/RFQs/HomepageRFQCard.jsx
  - src/presentation/components/homepage/Companies/CompaniesSection.jsx
  - src/presentation/components/homepage/RFQs/FeaturedRFQs.jsx
  - src/app/globals.css
autonomous: true
requirements:
  - UI-04
  - UI-05
  - UI-06

must_haves:
  truths:
    - "All homepage cards (fair, news, product, RFQ, company) have gold border with 20% opacity"
    - "All homepage cards have lighter background at rgba(255,255,255,0.06)"
    - "All homepage cards have gold bottom border"
    - "All homepage cards glow on hover (border 50% + box-shadow) with 300ms transition"
    - "Fair cards show country flag in the date box area"
    - "News cards match fair card gold treatment"
    - "Product image placeholder shows Package icon SVG instead of emoji"
    - "Image loading shows shimmer animation instead of spinner"
    - "Featured card scroll containers have user-select: none"
  artifacts:
    - path: "src/presentation/components/homepage/Fairs/FairsSection.jsx"
      provides: "Fair cards with gold border treatment and country flag"
      contains: "CountryFlag"
    - path: "src/presentation/components/homepage/Products/FeaturedProducts.jsx"
      provides: "Product placeholder with Package icon, shimmer loading"
      contains: "Package"
    - path: "src/app/globals.css"
      provides: "Updated .fair-card, .rfq-card, .hp-rfq-card with gold borders"
  key_links:
    - from: "FairsSection.jsx"
      to: "CountryFlag component"
      via: "import and render in date box"
      pattern: "CountryFlag"
    - from: "FeaturedProducts.jsx"
      to: "lucide-react Package"
      via: "import Package icon"
      pattern: "Package"
    - from: "globals.css"
      to: "all card components"
      via: "shared CSS classes card-surface, card-border-gold, card-hover-gold"
      pattern: "card-border-gold"
---

<objective>
Apply gold card visual refresh across all homepage card types and improve image loading states.

Purpose: Create a cohesive premium visual identity across all homepage cards with gold borders, lighter backgrounds, hover glow effects, country flags on fair cards, and professional image placeholders/loading states.

Output: All homepage cards visually unified with gold accent treatment.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-CONTEXT.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-RESEARCH.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-01-SUMMARY.md

@src/presentation/components/homepage/Fairs/FairsSection.jsx
@src/presentation/components/homepage/Products/FeaturedProducts.jsx
@src/presentation/components/homepage/News/NewsSection.jsx
@src/presentation/components/homepage/RFQs/HomepageRFQCard.jsx
@src/presentation/components/homepage/Companies/CompaniesSection.jsx
@src/presentation/components/homepage/RFQs/FeaturedRFQs.jsx
@src/app/globals.css

<interfaces>
<!-- Plan 01 created these shared CSS classes in globals.css -->
.card-surface { background: rgba(255, 255, 255, 0.06); }
.card-border-gold { border: 1px solid rgba(255, 215, 0, 0.2); }
.card-hover-gold { transition: border-color 300ms ease, box-shadow 300ms ease; }
.card-hover-gold:hover { border-color: rgba(255, 215, 0, 0.5); box-shadow: 0 0 12px rgba(255, 215, 0, 0.1); }
.card-bottom-gold { border-bottom: 2px solid rgba(255, 215, 0, 0.4); }
.animate-shimmer { shimmer animation for loading states }

<!-- CountryFlag component (already exists in codebase) -->
import { CountryFlag } from '@/presentation/components/common/CountryFlag/CountryFlag';
// Usage: <CountryFlag countryCode="US" size={20} />
// Handles missing/invalid codes gracefully (returns null or globe emoji)

<!-- Package icon from lucide-react (already installed) -->
import { Package } from 'lucide-react';
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply gold card treatment to fair cards, news cards, and RFQ cards via CSS + add country flag to fair cards</name>
  <files>src/app/globals.css, src/presentation/components/homepage/Fairs/FairsSection.jsx, src/presentation/components/homepage/News/NewsSection.jsx, src/presentation/components/homepage/RFQs/HomepageRFQCard.jsx</files>
  <action>
1. In `src/app/globals.css`:
   - Update the existing `.fair-card` styles: change the border from `rgba(255,255,255,0.1)` to `rgba(255, 215, 0, 0.2)`, bump background inner opacity from `0.03` to `0.06`, add gold bottom border `border-bottom: 2px solid rgba(255, 215, 0, 0.4)`, update hover to `border-color: rgba(255, 215, 0, 0.5); box-shadow: 0 0 12px rgba(255, 215, 0, 0.1);` with `transition: border-color 300ms ease, box-shadow 300ms ease`.
   - Update the existing `.rfq-card` and `.hp-rfq-card` styles with the same gold border, lighter bg, gold bottom border, and hover glow treatment.
   - Update any existing `.news-card` styles (or add if inline) with the same gold treatment.

2. In `src/presentation/components/homepage/Fairs/FairsSection.jsx`:
   - Import `CountryFlag` from `@/presentation/components/common/CountryFlag/CountryFlag`.
   - In the FairCard component, find the date box area (`.fair-date-box`) and add the country flag above the date:
     ```jsx
     <div className="fair-date-box flex flex-col items-center gap-1">
       {fair.country && <CountryFlag countryCode={fair.country} size={20} />}
       <span className="fair-date-day">{startDateInfo.day}</span>
       <span className="fair-date-month">{startDateInfo.month}</span>
     </div>
     ```
   - If the fair data does not have a `country` field, the `CountryFlag` simply won't render (conditional).

3. In `src/presentation/components/homepage/News/NewsSection.jsx`:
   - Find the news card JSX and apply gold card classes. If the card uses inline styles, replace with the shared CSS classes (`card-surface card-border-gold card-bottom-gold card-hover-gold`) or apply the same gold border/bg/hover values inline.

4. In `src/presentation/components/homepage/RFQs/HomepageRFQCard.jsx`:
   - If not already using `.hp-rfq-card` class (which was updated in globals.css), apply the gold treatment. Verify the card uses the CSS class or apply shared classes.

PITFALL: Do NOT apply `border-image` for the gradient bottom border — it's incompatible with `border-radius`. Use solid gold bottom border: `border-bottom: 2px solid rgba(255, 215, 0, 0.4)`.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Fair cards show gold border + country flag in date box. News cards and RFQ cards have matching gold treatment. All cards have lighter bg, gold border, gold bottom, and hover glow. No build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Product placeholder SVG, shimmer loading, company card gold treatment, and user-select: none on scroll containers</name>
  <files>src/presentation/components/homepage/Products/FeaturedProducts.jsx, src/presentation/components/homepage/Companies/CompaniesSection.jsx, src/presentation/components/homepage/RFQs/FeaturedRFQs.jsx</files>
  <action>
1. In `src/presentation/components/homepage/Products/FeaturedProducts.jsx`:
   - Import `Package` from `lucide-react`.
   - Find the `ProductCardImage` component (or equivalent image placeholder). Replace the `📦` emoji fallback with:
     ```jsx
     <div className="w-full h-full flex flex-col items-center justify-center bg-gradient-to-br from-[#1A283B] to-[#0F1B2B] gap-2">
       <Package className="w-10 h-10 text-[rgba(255,255,255,0.2)]" />
       <span className="text-[10px] text-[rgba(255,255,255,0.3)] font-medium uppercase tracking-wider">No image</span>
     </div>
     ```
   - Replace any existing loading spinner in the image loading state with the shimmer animation: add `animate-shimmer` class to the loading placeholder div (the class was defined in Plan 01's globals.css changes).
   - Apply gold card treatment to product cards: add `card-border-gold card-bottom-gold card-hover-gold` classes (or equivalent inline styles matching the gold border spec) to the product card container.

2. In `src/presentation/components/homepage/Companies/CompaniesSection.jsx`:
   - Find the company card JSX and apply gold card treatment: gold border `rgba(255, 215, 0, 0.2)`, lighter bg `rgba(255, 255, 255, 0.06)`, gold bottom border, hover glow. Use shared CSS classes where possible.

3. In `src/presentation/components/homepage/RFQs/FeaturedRFQs.jsx`:
   - Add `user-select: none` (via `select-none` Tailwind class) to the outer horizontal scroll container div. This prevents accidental text selection during horizontal drag-scrolling on desktop.
   - Apply this ONLY to the scroll container wrapper, NOT to individual card content (users should still be able to copy text when clicking into cards).

4. Also apply `select-none` to any other featured horizontal scroll containers (FeaturedProducts scroll row, FeaturedCompanies scroll row if they exist) — same pattern, same reason.

PITFALL: `user-select: none` must ONLY go on the outer scroll container, not on card content. Users need to be able to copy product names, prices, etc.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Product placeholder shows Package icon SVG (no emoji). Image loading uses shimmer animation. Company cards have gold treatment. Featured scroll containers have user-select: none. All homepage cards now visually consistent with gold accent theme. No build errors.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- No `📦` emoji remains in FeaturedProducts.jsx — replaced with Package icon SVG
- CountryFlag imported and rendered in FairsSection.jsx fair card date box
- All card types (fair, news, product, RFQ, company) have gold border treatment
- `select-none` applied to scroll containers only (not card content)
- `.animate-shimmer` class used for image loading states
</verification>

<success_criteria>
- All homepage cards display unified gold border, lighter background, gold bottom border, and hover glow
- Fair cards show country flag in date box
- Product placeholder is professional SVG with Package icon
- Image loading shows shimmer animation
- Scroll containers prevent accidental text selection
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-ux-polish-and-visual-fixes/11-02-SUMMARY.md`
</output>
