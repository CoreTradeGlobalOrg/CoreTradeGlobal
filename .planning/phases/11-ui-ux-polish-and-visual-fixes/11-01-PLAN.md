---
phase: 11-ui-ux-polish-and-visual-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(main)/layout.jsx
  - src/presentation/components/homepage/Navbar/Navbar.jsx
  - src/presentation/components/homepage/Globe/GlobeCanvas.jsx
  - src/presentation/components/homepage/Hero/HeroSection.jsx
  - src/app/globals.css
autonomous: true
requirements:
  - UI-01
  - UI-02
  - UI-03

must_haves:
  truths:
    - "Currency ticker is visible on all pages as a thin bar above the navbar"
    - "Ticker scrolls away with the page while navbar stays fixed/sticky"
    - "Ticker appears on mobile above hamburger navbar"
    - "Hero slogan h1 displays silver metallic gradient text"
    - "Globe rotates 20% slower than before"
    - "Navbar is shorter after ticker removal (target ~48px height)"
    - "Scroll-to anchor links land below the fixed navbar, not hidden behind it"
  artifacts:
    - path: "src/app/(main)/layout.jsx"
      provides: "CurrencyTicker rendered above Navbar"
      contains: "CurrencyTicker"
    - path: "src/presentation/components/homepage/Navbar/Navbar.jsx"
      provides: "Navbar without CurrencyTicker, reduced height"
    - path: "src/app/globals.css"
      provides: "hero-slogan silver gradient, scroll-padding-top, navbar height update"
      contains: "hero-slogan"
  key_links:
    - from: "src/app/(main)/layout.jsx"
      to: "CurrencyTicker component"
      via: "import and render above Navbar"
      pattern: "CurrencyTicker.*Navbar"
---

<objective>
Restructure the navbar/ticker layout and polish the hero section.

Purpose: Move the CurrencyTicker from inside the navbar to a dedicated thin bar above it (visible site-wide, scrolls away), reduce navbar height, add silver metallic gradient to the hero slogan, and slow down the globe rotation.

Output: Restructured layout with ticker-above-navbar pattern, polished hero section.
</objective>

<execution_context>
@/Users/wenubey/.claude/get-shit-done/workflows/execute-plan.md
@/Users/wenubey/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-CONTEXT.md
@.planning/phases/11-ui-ux-polish-and-visual-fixes/11-RESEARCH.md

@src/app/(main)/layout.jsx
@src/presentation/components/homepage/Navbar/Navbar.jsx
@src/presentation/components/homepage/Globe/GlobeCanvas.jsx
@src/presentation/components/homepage/Hero/HeroSection.jsx
@src/app/globals.css

<interfaces>
<!-- Layout structure (layout.jsx) -->
Currently renders: ScrollToTop > Navbar > ErrorBoundary > children > Footer > widgets
CurrencyTicker import exists in Navbar.jsx line 19:
  import { CurrencyTicker } from '@/presentation/components/homepage/CurrencyTicker/CurrencyTicker';
CurrencyTicker rendered inside Navbar.jsx around line 439.

<!-- Navbar scroll behavior -->
navRef tracks <nav> element height via ResizeObserver, sets --navbar-height CSS variable.
After ticker removal, --navbar-height will naturally shrink (~28px less).
globals.css line 10 has fallback: --navbar-height: 128px (needs update to ~100px).

<!-- GlobeCanvas -->
autoRotateSpeed={2.5} at line 335 of GlobeCanvas.jsx.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move CurrencyTicker to layout and reduce navbar height</name>
  <files>src/app/(main)/layout.jsx, src/presentation/components/homepage/Navbar/Navbar.jsx, src/app/globals.css</files>
  <action>
1. In `src/app/(main)/layout.jsx`:
   - Import CurrencyTicker: `import { CurrencyTicker } from '@/presentation/components/homepage/CurrencyTicker/CurrencyTicker';`
   - Render `<CurrencyTicker />` BEFORE `<Navbar />` in the JSX (between ScrollToTop and Navbar). The ticker lives outside the nav element so it scrolls away with the page.

2. In `src/presentation/components/homepage/Navbar/Navbar.jsx`:
   - Remove the CurrencyTicker import (line 19).
   - Remove the `<CurrencyTicker />` render (around line 439). Search for the exact location and remove.
   - The navbar should NOT render the ticker anymore — it lives in the layout.

3. In `src/app/globals.css`:
   - Update the `--navbar-height` fallback value from `128px` to `100px` (line 10 area). The ResizeObserver will correct it client-side, but the fallback should reflect navbar-only height.
   - Reduce `.navbar-content` padding to achieve ~48px total navbar height. Find the existing padding/height rules and reduce vertical padding (e.g., `py-2` equivalent or direct padding reduction).
   - Add `scroll-padding-top` on the `html` element:
     ```css
     html {
       scroll-padding-top: calc(var(--navbar-height) + 16px);
     }
     ```
   - Ensure the ticker bar styling works on mobile — the existing `text-[10px] sm:text-xs` and `whitespace-nowrap` on the marquee should keep it single-line. Verify no wrapping occurs.

CRITICAL: After moving the ticker out of `<nav>`, the `--navbar-height` variable tracked by ResizeObserver on `navRef` will naturally exclude the ticker height. This is intentional per user decisions — the ticker scrolls away, only the navbar is part of the fixed offset. Verify that pages using `pt-[var(--navbar-height)]` still display correctly (content below fixed navbar, not hidden behind it).
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>CurrencyTicker renders above the navbar on all pages via the layout. Ticker scrolls away with the page. Navbar is ~48px tall. Scroll-padding-top prevents anchor links from hiding behind the navbar. No build errors.</done>
</task>

<task type="auto">
  <name>Task 2: Hero slogan silver gradient and globe speed reduction</name>
  <files>src/app/globals.css, src/presentation/components/homepage/Hero/HeroSection.jsx, src/presentation/components/homepage/Globe/GlobeCanvas.jsx</files>
  <action>
1. In `src/app/globals.css`, add the `.hero-slogan` class:
   ```css
   .hero-slogan {
     background: linear-gradient(180deg, #E8E8E8 0%, #C0C0C0 50%, #A0A0A0 100%);
     -webkit-background-clip: text;
     -webkit-text-fill-color: transparent;
     background-clip: text;
   }
   ```

2. In `src/presentation/components/homepage/Hero/HeroSection.jsx` (around line 181):
   - Find the main h1 slogan element and add the `hero-slogan` class to it.
   - Only the h1 slogan gets the silver treatment. The subtitle stays its current color. Do NOT apply to subtitle or any other text.

3. In `src/presentation/components/homepage/Globe/GlobeCanvas.jsx` (line 335):
   - Change `autoRotateSpeed={2.5}` to `autoRotateSpeed={2.0}`.

4. In `src/app/globals.css`, add the shimmer animation keyframe and utility class (used by Plan 02 for image loading, but define it here since we're already editing globals.css):
   ```css
   @keyframes shimmer {
     0%   { background-position: -400px 0; }
     100% { background-position: 400px 0; }
   }

   .animate-shimmer {
     background: linear-gradient(
       90deg,
       rgba(255, 255, 255, 0.03) 25%,
       rgba(255, 255, 255, 0.08) 50%,
       rgba(255, 255, 255, 0.03) 75%
     );
     background-size: 800px 100%;
     animation: shimmer 1.5s infinite;
   }
   ```

5. Also in `src/app/globals.css`, add the shared card utility classes (used by Plan 02, but define once here):
   ```css
   .card-surface {
     background: rgba(255, 255, 255, 0.06);
   }

   .card-border-gold {
     border: 1px solid rgba(255, 215, 0, 0.2);
   }

   .card-hover-gold {
     transition: border-color 300ms ease, box-shadow 300ms ease;
   }
   .card-hover-gold:hover {
     border-color: rgba(255, 215, 0, 0.5);
     box-shadow: 0 0 12px rgba(255, 215, 0, 0.1);
   }
   ```

NOTE on gold gradient bottom border: Per research, `border-image` + `border-radius` are incompatible in CSS. Use a solid gold bottom border instead: `border-bottom: 2px solid rgba(255, 215, 0, 0.4)`. Apply this in the shared card classes or per-card as `.card-bottom-gold { border-bottom: 2px solid rgba(255, 215, 0, 0.4); }`.
  </action>
  <verify>
    <automated>cd /Users/wenubey/Desktop/CTG/core-trade-global && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>Hero h1 slogan displays silver metallic gradient text. Globe rotates at speed 2.0 (was 2.5). Shimmer animation keyframe and shared card utility classes defined in globals.css. No build errors.</done>
</task>

</tasks>

<verification>
- `npx next build` completes without errors
- CurrencyTicker import removed from Navbar.jsx, added to layout.jsx
- `autoRotateSpeed` is `2.0` in GlobeCanvas.jsx
- `.hero-slogan` class exists in globals.css with silver gradient
- `scroll-padding-top` rule exists on `html` in globals.css
- `.card-surface`, `.card-border-gold`, `.card-hover-gold`, `.animate-shimmer` defined in globals.css
</verification>

<success_criteria>
- Ticker bar visible above the navbar on all pages, scrolls away while navbar stays fixed
- Hero slogan has silver metallic gradient
- Globe rotates slower
- Shared card CSS utilities ready for Plan 02 consumption
- Build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/11-ui-ux-polish-and-visual-fixes/11-01-SUMMARY.md`
</output>
